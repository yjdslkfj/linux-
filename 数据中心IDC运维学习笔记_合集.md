# 数据中心（IDC）IT 运维工程师学习笔记（三合一合集）

> 本文件由三份学习笔记合并而成，**正文内容未做任何改动，仅对目录进行了汇总与调整**。
>
> - 来源一：数据中心 IDC 运维学习笔记（第 01~30 讲）—— 数据中心全景·硬件篇·网络基础篇
> - 来源二：数据中心 IDC 运维学习笔记（第 31~70 讲）—— 路由·ACL·NAT·网络架构·交换机·服务器硬件·Linux
> - 来源三：数据中心 IT 运维学习笔记 —— Linux 系统管理·综合布线·管理制度·运维流程

---

# 总目录

## 第一部分　数据中心全景 + 硬件篇（笔记一，第 01~15 讲）

- [一、认识数据中心](#p1-1)
- [二、数据中心的物理世界：园区、分区与基础设施](#p1-2)
- [三、必须掌握的行业名词](#p1-3)
- [四、IT 系统组成与机柜](#p1-4)
- [五、服务器硬件详解](#p1-5)
- [六、网络设备：交换机与路由器](#p1-6)
- [七、光纤与光缆](#p1-7)

## 第二部分　网络基础篇（笔记一，第 16~30 讲）

- [八、光模块](#p2-1)
- [九、网线及辅材](#p2-2)
- [十、OSI 与 TCP/IP 参考模型](#p2-3)
- [十一、二层交换原理](#p2-4)
- [十二、IP 地址、进制转换与子网划分](#p2-5)
- [十三、TCP 与 UDP 通信](#p2-6)
- [十四、VLAN 虚拟局域网](#p2-7)
- [十五、路由基础](#p2-8)
- [附：全课程知识框架总结](#p2-9)

## 第三部分　预备知识～交换机·服务器·Linux（笔记二，第 31~70 讲）

- [第〇篇　预备基础知识（零基础必读）](#p3-1)
- [第一篇　路由技术：RIP 与 OSPF](#p3-2)
- [第二篇　ACL 访问控制列表](#p3-3)
- [第三篇　NAT 网络地址转换](#p3-4)
- [第四篇　数据中心网络架构](#p3-5)
- [第五篇　交换机配置与管理](#p3-6)
- [第六篇　服务器硬件：基本操作、BIOS、BMC 带外管理、RAID](#p3-7)
- [第七篇　Linux 系统管理](#p3-8)
- [附录 A　英文术语速查表](#p3-9)
- [附录 B　常用命令速查表](#p3-10)
- [附录 C　课程作业自测参考答案](#p3-11)

## 第四部分　Linux 管理与综合布线等（笔记三）

- [第 0 章　预备知识（写给零基础的你）](#p4-0)
- [第 1 章　文件归档与压缩](#p4-1)
- [第 2 章　用户与用户组管理](#p4-2)
- [第 3 章　文件权限管理](#p4-3)
- [第 4 章　磁盘与文件系统管理](#p4-4)
- [第 5 章　网络配置与管理](#p4-5)
- [第 6 章　防火墙与 SELinux](#p4-6)
- [第 7 章　运行级别与 root 密码重置](#p4-7)
- [第 8 章　SSH 远程登录管理](#p4-8)
- [第 9 章　综合布线基础](#p4-9)
- [第 10 章　线缆布放流程与规范](#p4-10)
- [第 11 章　标签粘贴规范](#p4-11)
- [第 12 章　综合布线项目验收标准](#p4-12)
- [第 13 章　数据中心管理规定](#p4-13)
- [第 14 章　数据中心日常运维流程](#p4-14)
- [第 15 章　服务器操作流程](#p4-15)
- [第 16 章　网络设备操作流程](#p4-16)
- [附录 A　术语中英文对照总表](#p4-a)
- [附录 B　常用命令速查表](#p4-b)
- [附录 C　课程自测题与参考答案](#p4-c)
- [结语：从初级到进阶](#p4-end)

---

---
<!-- ============ 笔记一：数据中心全景 + 硬件篇 / 网络基础篇（第 01~30 讲） ============ -->

# 数据中心（IDC）IT 运维工程师课程 学习笔记

> 本笔记根据《数据中心（IDC）IT 运维工程师课程》第 01~30 讲字幕整理编写，按照知识体系重新组织（非逐讲记录），面向网络初学者。所有关键缩写均标注英文全称，并适当补充课程中未展开的背景知识。

**目录**

- 第一部分（对应第 01~15 讲）：数据中心全景 + 硬件篇
  - 一、认识数据中心
  - 二、数据中心的物理世界：园区、分区与基础设施
  - 三、必须掌握的行业名词
  - 四、IT 系统组成与机柜
  - 五、服务器硬件详解
  - 六、网络设备：交换机与路由器
  - 七、光纤与光缆
- 第二部分（对应第 16~30 讲）：网络基础篇
  - 八、光模块
  - 九、网线及辅材
  - 十、OSI 与 TCP/IP 参考模型
  - 十一、二层交换原理
  - 十二、IP 地址、进制转换与子网划分
  - 十三、TCP 与 UDP 通信
  - 十四、VLAN 虚拟局域网
  - 十五、路由基础

---

# 第一部分：数据中心全景 + 硬件篇（第 01~15 讲）

<a id="p1-1"></a>
## 一、认识数据中心

### 1.1 什么是数据中心

- **DC（Data Center，数据中心）**：专门设计用于容纳服务器、存储和网络设备的专用场地，为关键业务提供应用程序、数据和内容服务。
- **IDC（Internet Data Center，互联网数据中心）**：可以理解为"Internet 的场地租赁"概念，是面向互联网业务的 DC。日常工作中说的"数据中心"大多指 IDC。

IDC 本质上是一个**服务平台**，对外提供四类能力：

1. 稳定可靠的基础设施服务（电力、机柜等）；
2. 强大的数据处理能力；
3. 大量云平台应用（私有云、公有云）；
4. 高速带宽（上百 GB 级通信带宽）。

### 1.2 数据中心的分类

**按规模划分（以机架数量为准）：**

| 规模 | 机架数量 |
|---|---|
| 中小型数据中心 | ≤ 3000 个机架 |
| 大型数据中心 | 3000 ~ 10000 个机架 |
| 超大型数据中心 | > 10000 个机架 |

**按等级划分：**

- 国标：根据 GB 50174《数据中心设计规范》分为 **A、B、C** 三个等级（A 级最高，为容错型；B 级为冗余型；C 级为基本型）。
- 国际标准：**TIA-942**（Telecommunications Industry Association，美国电信行业协会第 942 号标准）分为 **T1 / T2 / T3 / T4** 四个等级。

> **补充：TIA-942 各等级可用性与年平均宕机时间对照**
>
> | 等级 | 可用性 | 年平均宕机时间 |
> |---|---|---|
> | T1 | 99.671% | 28.8 小时 |
> | T2 | 99.741% | 22 小时 |
> | T3 | 99.982% | 1.6 小时 |
> | T4 | 99.995% | 0.4 小时 |
>
> 数字越大，机房等级越高，可用性越高，宕机时间越短。
> **可用性（Availability）=（全年总时间 − 宕机时间）÷ 全年总时间**，例如 T4：一年 8760 小时中最多宕机约 0.4 小时。
> 宕机（Down）的原因包括自然灾害（如 2021 年 7 月郑州水灾导致大量机房被淹）和人为原因（如违规吸烟触发气体消防导致机房断电）等。

此外还有一些**非标准**的俗称，例如"腾讯贵安七星级数据中心"，"七星"只是对外宣传说法，并非标准分级。

### 1.3 数据中心的四层结构

| 层次 | 内容 |
|---|---|
| 场地层 | 楼宇建设、市电引入、消防等 |
| 基础设施层 | 变配电、制冷、机柜等（机房正常运行的保障） |
| IT 硬件层 | 服务器、网络设备（交换机、路由器）、存储设备、安全设备（防火墙） |
| IT 软件层 | Linux 操作系统、虚拟化软件（KVM、Docker）、云平台（OpenStack）、大数据等 |

IT 运维工程师主要与 **IT 硬件层和 IT 软件层**打交道；基础设施层则由基础设施运维工程师负责。

### 1.4 数据中心的运营方（运营商）

1. **电信运营商**：中国移动（CM，China Mobile）、中国电信（CT，China Telecom）、中国联通（CU，China Unicom）和中国广电。数据中心领域以前三家为主。
2. **独立第三方**：如万国数据、光环新网、世纪互联、数据港等，有资金实力，承接并运营数据中心。
3. **互联网企业**：如百度、阿里、腾讯（BAT），自建数据中心自用。
4. 其他：银行、政府也有自建数据中心，规模较小。

**它们之间的关系（重要逻辑）：**

- 独立第三方 → 向电信运营商**租赁带宽和场地** → 再运营机房、招揽客户；
- 电信运营商也会请独立第三方代建、代维机房；
- 互联网企业 → 向电信运营商租场地和带宽自用，也会向独立第三方租机房放自己的服务器；
- **带宽的"源头"始终掌握在电信运营商手中**——无论谁建数据中心，带宽必须从电信运营商获取。

### 1.5 IDC 运维岗位的定位（宣导课要点）

- IDC 运维工程师俗称"IDC 卫士"，需要懂服务器硬件、服务器系统（安装、网络配置、远程管理）和网络（网线、光纤、交换机）。
- 按工作内容细分：系统运维、网络运维、数据库运维、桌面运维、IDC 运维等，各有特点。
- 发展阶梯（晋升通道）：**数据中心驻场运维工程师 → 中级系统/网络工程师 → 运维项目经理/机房组长 → 资深运维工程师 → 技术专家（自动化运维、运维开发）→ 系统架构师/CTO（Chief Technology Officer，首席技术官）**。
- IDC 领域岗位分为三块：**基础设施运维、IT 运维（本课程方向）、系统运维**。IT 运维包含 IT 硬件层（服务器、交换机、存储）与 IT 软件层（Linux 系统、网络、安全）。

---

<a id="p1-2"></a>
## 二、数据中心的物理世界：园区、分区与基础设施

### 2.1 数据中心的"五道门"（安全管控体系）

进入数据中心内部要经过五道安全关卡：

1. **园区大门**：需提前办理入室授权（提交姓名、身份证号、电话等信息给运营商），门卫核对信息后放行。
2. **大厅入口闸机**：由驻场 IT 运维工程师接待，刷卡或刷脸进入大楼。
3. **机房门禁**：刷卡或人脸识别进入机房，门禁卡严禁外借。
4. **微模块门禁**：微模块机房两端玻璃门上的门禁，需 IT 运维工程师授权（刷卡/密码为主）。
5. **机柜门**：机柜前后都配锁，需用钥匙打开才能操作设备。

> 所以电影里"轻松插 U 盘进机房服务器"的场景在现实中几乎不可能发生。
>
> **补充：微模块（Micro Module）机房**——新型机房结构。传统机房每列机柜独立、通道开放；微模块由两列机柜组成一个封闭单元，通道两头装玻璃门、顶部封闭，形成"冷通道封闭"。优点：建设周期短、交付快，监控/消防/电力集中化管理，易于运维。新建数据中心大多采用微模块方案。

### 2.2 数据中心的功能分区

| 分区 | 包含房间 | 说明 |
|---|---|---|
| 主机房 | 服务器机房、网络机房、存储机房 | 所有 IT 设备所在地，IT 运维工程师的主战场 |
| 行政办公区 | 监控室、办公室、储藏室、门厅 | 监控室是 IT 运维常驻地点 |
| 辅助区 | 维修室、仪器室、备件库、进线间、资料室等 | |
| 设备间 | 低压配电室、UPS 室、蓄电池室、精密空调间、气体消防（气瓶）间 | 基础设施运维工程师的工作区域 |

**监控室**可监控：视频监控、电力系统监控、IT 设备监控、门禁消防系统监控等，整个数据中心的运行数据都汇总显示在监控大屏上。

**识别数据中心外观的小技巧**：楼宇两侧有大量大型空调外机，或楼旁有大型蓄能罐（用于机房降温）的，大概率是数据中心。

**机房内部结构**：上层黄色走线槽用于布放光纤，中间白色钢架结构布放弱电网线，下层黑色机柜放置 IT 设备。

### 2.3 基础设施五大系统

数据中心基础设施由**五大系统**组成：消防系统、供配电系统、暖通及排水系统、装饰装修系统、弱电系统。

- 暖通及排水系统：冷源系统、新风系统、精密空调系统、排风系统、加湿系统、灾后排烟系统；
- 供配电系统：市电引入、IT 设备配电、UPS（Uninterruptible Power Supply，不间断电源）系统、照明、冷冻系统配电、防雷接地系统；
- 弱电系统：综合布线、安防系统、机柜系统、动环（动力环境）监控系统、拼接屏系统、楼宇自控系统；
- 消防系统：自动报警系统 + 灭火系统；
- 装饰装修系统：墙面地面工程、隔墙柱面工程、门窗工程、防水工程。

#### 2.3.1 电力系统（最重要）

**供电来源有两个：双路市电 + 柴油发电机。**

- 市电：双路 10kV / 35kV 或 110kV 高压电（来自两个不同电站，如一路火电、一路水电），经**变压器**降压为 **380V 低压**，送到机房低压配电柜。
- 供电模式三种：双路直流供电（DC，Direct Current）、双路交流供电（AC，Alternating Current）、交直流混合。

**市电的输送路径（重点，建议背下来）：**

```
双路高压市电 → 变压器（高压→380V低压） → 低压配电柜
    → UPS 系统 → 输出配电柜 → 列头柜（PMM 柜）
    → 各机柜 PDU 插板 → 服务器等 IT 设备加电
```

**关键设备：**

1. **高压/低压配电柜**：完成电压变换与分配。
2. **UPS（Uninterruptible Power Supply，不间断电源）**：可理解为"超大型充电宝"。作用：① 正常时稳压、整流；② 双路市电同时停电时立即接管供电（短时间），为柴油发电机启动赢得时间。
3. **列头柜（课程称 PMM 柜）**：每一列机柜的第一个柜子，负责这一列机柜的电力供应与分配。
4. **PDU（Power Distribution Unit，电源分配单元）**：机房电力分配的最小单元，即机柜内给设备供电的插板。每个机位配备 **A、B 两路 PDU**，分别来自两路不同的市电（互为备份）。
5. **柴油发电机**：双路市电都断时立即启动接管供电。大型/超大型数据中心一般配备 10~20 多台，平时静默待命。

#### 2.3.2 消防系统

- 设备：气瓶间、氮气启动瓶、气体灭火控制器、消防栓、喷淋泵。
- **机房核心区域用气体灭火**（设备不能进水），其他区域可用水灭火（消防栓、喷淋）。
- 组件：报警按钮、烟感、温感、气体紧急灭火器、紧急启停按钮、呼吸器、消防斧等。

#### 2.3.3 制冷系统

**机房环境标准（国家 A 级）：**

- 温度：23℃ ± 2℃（即 21~25℃），大部分机房要求低于 28℃；
- 湿度：45% ~ 65%（一般 20% ~ 80% 视为正常范围）；
- 常年保持恒温恒湿状态。

**三种制冷方式：**

1. **风冷**：空调风或自然风引入数据中心降温（传统方式，耗电大）。
2. **水冷**：引入机房附近水源，在服务器周围循环带走热量，回到冷却塔反复循环。
3. **沉浸式液冷**：将 IT 设备直接浸泡在特定冷却液体中（如阿里仁和数据中心），能耗最低、PUE 值低，但冷却液昂贵，尚未大规模普及。

**制冷设备**：新风机组（机房无窗、门常闭，负责引入新鲜空气、调节湿度）、精密空调、水冷管道、沉浸式液冷设施。

**冷通道与热通道（重点概念）：**

- **冷通道（Cold Aisle）**：空调送风（冷风）进入的通道——地板为镂空通风地板，空调风从地板下吹出。
- **热通道（Hot Aisle）**：服务器排风（热风）排出的通道——服务器后面板（出风口）一侧永远是热通道；地板为实心。
- 布局规律：相邻两列机柜**热通道相对、冷通道相向**，冷热通道间隔排列。
- 现代机房多采用**冷通道封闭**（微模块把冷通道用玻璃封闭起来），防止冷热气流混合，提高制冷效率；也有封闭热通道的设计。

#### 2.3.4 带内管理与带外管理（重要概念）

| 方式 | 英文 | 原理 | 特点 |
|---|---|---|---|
| 带内管理 | In-band Management | 管理数据与用户业务数据**走同一根线缆/链路** | 网络故障时，业务和管理**同时中断** |
| 带外管理 | Out-of-band Management | 管理控制信息与业务数据走**不同的物理通道**，相互独立、互不影响 | 网络故障时仍可通过独立通道管理设备 |

数据中心大部分场景使用**带外管理**（后文 iLO、BMC、Console 口、管理网口都属于带外管理手段）。

#### 2.3.5 监控系统

- **视频监控**：卸货平台、公共通道、冷通道、热通道、仓库全覆盖，机房内 360° 无死角，值班室监控大屏实时切换。
- **环境监控（动环监控）**：监控温度、湿度等，由管理系统（监控管理单元）、数据采集单元、传感器探头构成。异常时（如新增 100 台服务器加电导致温度超过阈值）通过电话、短信、微信、邮件、现场声光报警等方式告警。

#### 2.3.6 门禁系统与防雷

- 门禁子系统：控制器、读卡器、感应卡、电锁、综合管理服务器、系统管理工作站、制卡系统；支持分级管理与联网控制；识别方式有人脸、指纹、密码、刷卡。
- 防雷系统：楼顶大型避雷针，与普通楼宇相同。
- **综合布线系统**：各分区统一设置，多采用**双星型拓扑（Dual-Star Topology）**——每个分支相对独立，单支路故障或改动不影响其他子系统。

---

<a id="p1-3"></a>
## 三、必须掌握的行业名词

| 名词 | 英文全称 | 说明 |
|---|---|---|
| CDN | Content Delivery Network，内容分发网络 | 以中心平台为核心，在全国/全球各机房建设节点，实现**内容分发、负载均衡（Load Balancing）、智能调度、数据同步**。用户流量通过调度机制找到**最近的节点**访问。两大核心技术：内容的存储与分发。两大作用：**降低网络阻塞、提高访问速度和命中率** |
| ISP | Internet Service Provider，互联网服务提供商 | 提供接入服务（帮用户接入 Internet）、导航服务（帮用户找到信息）、信息服务（建立数据服务系统）。国内指中国电信（CT）、中国联通（CU）、中国移动（CM）、中国广电 |
| U | Rack Unit（RU），机架单位 | 机房计量尺寸的单位，**1U = 4.445 cm**。机柜高度常用 42U（约 2 米）；服务器/交换机高度也用 U 表示（1U、2U、4U），常见交换机多为 1U |
| 机架位 | Rack Position | 指定服务器位置的编号规则，例如 `CDN-WH-CM21-2F206-A01-01-1`：武汉 CDN 机房—中国移动第 21 节点—2 楼 206 室—A 列 01 号柜—第 1 台服务器。默认从机柜**最下面**一台开始数（也有机房从上往下数，以现场规范为准） |
| SN | Serial Number，产品序列号 | 产品的唯一标识，用于验证产品合法身份、资产管理。别称：机器码、认证码、注册申请码 |
| TOR | Top of Rack，柜顶交换机 | 每个服务器机柜内部署 1~2 台交换机，服务器直接接入本机柜交换机，实现柜内互联。核心是"交换机部署在服务器机柜内"，可以放在顶部（TOR）、中部（MOR，Middle of Rack）、底部（BOR，Bottom of Rack）。顶部最有利于走线，应用最多 |
| PUE | Power Usage Effectiveness，电源使用效率 | **PUE = 数据中心总能耗 ÷ IT 设备能耗**，值恒 ≥ 1，越接近 1 能效越好。2021 年 10 月起国家要求新建大型、超大型数据中心 PUE 不超过 1.3 |
| CLF | Cooling Load Factor，制冷负载系数 | 制冷设备能耗 ÷ IT 设备能耗 |
| PLF | Power Load Factor，供电负载系数 | 供配电系统能耗 ÷ IT 设备能耗 |
| RER | Renewable Energy Ratio，可再生能源利用率 | 可再生能源（风能、水能、太阳能等）供电量 ÷ 数据中心总能耗 |

> **U 位与机架位的关系（举例）**：42U 机柜中，一台 2U 服务器上下各留约 1U 散热间隔，实际占约 3U 空间；10 台 2U 服务器大约占 30U 以上。放设备前要结合设备高度与散热间隔规划 U 位。

---

<a id="p1-4"></a>
## 四、IT 系统组成与机柜

### 4.1 IT 系统组成

数据中心 IT 系统由三大部分组成：

1. **网络设备**：交换机、路由器、防火墙（Firewall）；
2. **服务器**：缓存服务器、管理服务器、存储服务器等；
3. **综合布线系统**：光纤 + 网线。

此外还有机柜及其相关组件：机柜本体、托盘、支架、PDU、KVM、桥架。

### 4.2 机柜（Cabinet/Rack）

- 机房最基础的设备，所有设备都放在机柜中。
- 材料：冷轧钢板或合金；作用：放置设备、保护设备、屏蔽电磁干扰、方便维护。
- 分类：服务器机柜（最常见）、网络机柜、控制台机柜。
- 标准尺寸：**19 英寸宽、42U 高**（约 2 米）。

### 4.3 机柜配件：托盘与支架

| 配件 | 特点 |
|---|---|
| 托盘（Tray） | 一整块板子装入机架，一块托盘托起一台服务器 |
| L 型支架（L-Bracket） | **2 个为一组**（左右各一）才能托起一台设备，长度根据机柜深度选择 |
| 导轨（Rail/Slider） | 与 L 型支架类似，2 个一组承重，还便于设备抽拉维护 |

三种任选其一，装好后才能把服务器、交换机上架。

### 4.4 PDU（电源分配单元）详解

- PDU = 机柜用电源分配插座，给服务器供电的"插板"，安装方式分横向和竖向。
- **两种常见插孔制式**：
  - **C13 欧标 PDU**：配欧标电源线（电源延长线）；
  - **国标 PDU**：配国标电源线（与空调、洗衣机等大型电器相同）。
- 注意：施工前必须确认机房 PDU 制式，选配对应电源线，否则设备无法加电。

### 4.5 KVM

- **KVM = Keyboard, Video, Mouse（键盘、显示器、鼠标）** 的缩写。机房里"找一套 KVM"就是找键鼠和显示器。
- KVM 一体机：一根线连接 KVM，另一端多个 VGA（Video Graphics Array，视频图形阵列）接口接到各服务器的 VGA 口上，可在多台服务器间切换操作，无需来回插拔。

### 4.6 桥架（综合布线的基础设施）

- 常见类型：槽式、托盘式、**网格式（机房最常用）**、T 式、隔板式、组合式、大跨距桥架。
- 布线规则：**光纤放黄色走线槽；强电或弱电网线放网格式桥架**，且**强电与弱电必须分开布放**（防止电磁干扰）。

---

<a id="p1-5"></a>
## 五、服务器硬件详解

### 5.1 什么是服务器

**服务器（Server）**：具有高计算能力、能够通过网络同时提供给多个用户使用的计算机。可理解为"个人 PC（Personal Computer）的加强版"，在计算能力、稳定性、可靠性、安全性、可扩展性、可管理性方面远超个人 PC。广泛应用于银行、电信、互联网数据中心。

### 5.2 服务器的分类

**按 CPU 架构分：**

| 类型 | 全称/说明 | 特点 | 应用 |
|---|---|---|---|
| x86 服务器 | CISC（Complex Instruction Set Computer，复杂指令集）架构 | 价格便宜、兼容性好；稳定性、安全性相对弱 | 中小型企业、非关键业务 |
| 非 x86 服务器 | RISC（Reduced Instruction Set Computer，精简指令集）架构（如 IBM Power、ARM 等） | 价格昂贵、体系封闭；稳定性好、性能强 | 金融、电信等大型企业核心系统 |

**按外观分：**

| 类型 | 特点 |
|---|---|
| 机架式服务器（Rack Server） | 数据中心最常见；高度按 1U/2U/4U 划分（**2U 最多**）；需通过托盘/支架安装在 19 英寸标准机柜中 |
| 塔式服务器（Tower Server） | 外形类似 PC 台式机主机，扩展性好；多用于企业内部（文件共享等），数据中心少见 |
| 机柜式/整机柜服务器（Rack-Scale Server） | 出厂即整机柜，一柜内置 20~50 台服务器，共用风扇和电源；到场落位、插电即用；大型互联网数据中心常用 |
| 刀片式服务器（Blade Server） | 一个"刀片"即一台服务器，可独立安装操作系统、彼此无关联；支持热插拔（Hot Swap）、管理方便、共用电源风扇 |

### 5.3 服务器外部接口（以 2U 机架式服务器为例）

**前面板：**

- 硬盘位：可放 8 / 12 / 24 块甚至更多硬盘；
- 状态指示灯：系统健康灯、UID 灯等；
- 通风进气口（冷空气从前进，热空气从后排，与冷热通道呼应）；
- 标签粘贴处：标注服务器 IP 地址、SN 号、资产号；
- 光驱模块（现在多数服务器已取消）、电源按钮、USB（Universal Serial Bus，通用串行总线）口、VGA 口。

**后面板：**

- 扩展模块空位：可扩展 GPU（Graphics Processing Unit，图形处理器）卡、网卡等；
- 2 块硬盘（一般用于安装操作系统）；
- 网口 + **UID 灯**：UID（Unit Identification，单元识别）灯用于**定位服务器**——打开后前面板和后面板的 UID 灯同时亮起，防止前后操作的不是同一台服务器造成误操作；
- USB 口、**iLO 口（远程带外管理口）**、VGA 口、电源模块插口；
- 一台典型服务器 = **8 个业务网口 + 1 个管理口（iLO）**；除电口外，很多服务器还提供光模块口（配合光纤使用）。

> **补充：iLO** = integrated Lights-Out，是惠普（HPE）服务器的远程带外管理技术（华为对应 iBMC，戴尔对应 iDRAC，本质上都基于 BMC 芯片，见 5.4 节）。通过它可以在服务器关机/宕机状态下远程开关机、查看硬件状态、挂载 ISO 镜像安装操作系统。

### 5.4 服务器内部组成

服务器与个人 PC 一样包含：**CPU、内存、硬盘、主板、电源、风扇、网卡**，此外还有个人 PC 一般没有的 **RAID 卡**（服务器独有）。

#### 5.4.1 CPU（Central Processing Unit，中央处理器）

- 计算机的运算和控制核心，是"信息处理、程序运行的最终执行单元"。
- **冯·诺依曼（Von Neumann）体系结构**将计算机分为五大部件：**运算器、控制器、存储器、输入设备、输出设备**，CPU 承担其中的**运算器和控制器**两大部件。
- 主要厂商：Intel（英特尔）和 AMD。服务器领域以 Intel 为主：
  - 个人 PC：Intel 酷睿（Core）系列；
  - 服务器：Intel 至强（Xeon）系列，如 E5-2650 v2/v3/v4，以及更高的铂金（Platinum）、黄金（Gold）系列。AMD 在服务器领域占比相对较小（近年 EPYC 霄龙系列增长较快）。

**CPU 核心参数（6 个）：**

1. **主频（时钟频率，Clock Speed）**：单位 MHz（兆赫兹）/ GHz（吉赫兹），表示 CPU 运算处理数据的速度；
2. **核心（Core，内核）**：一个核心同一时刻只能执行一个任务，物理核心越多并行处理能力越强；
3. **缓存（Cache）**：位于 CPU 与内存之间的临时存储器，容量比内存小但交换速度快，用于减少 CPU 访问内存的平均时间，对性能影响很大；
4. **线程（Thread）**：通常一个物理核心对应一个线程；**超线程（HT，Hyper-Threading）技术**可让一个物理核心同时执行 2 个任务（1 核 2 线程），提升并行能力；
5. **内存规格**：服务器支持的内存容量和类型由 CPU 决定（需确认支持的内存类别与最大容量）；
6. **封装规格**：CPU 插槽类型、封装大小、最大配置和最高允许温度等。

**CPU 在主板上的位置**：服务器一般**标配 2 颗 CPU**（CPU1、CPU2），具体位置随主板型号不同。

**CPU 常见故障与处理：**

| 故障现象 | 可能原因 | 处理办法 |
|---|---|---|
| 无法开机 | CPU 硬件损坏、插座/针脚接触不良 | 重新插拔安装 CPU 或更换 |
| 服务器宕机 | CPU 温度过高、硬件不良 | 检查散热、更换 CPU |
| 自动重启 | 温度过高、硬件不良 | 更换 CPU |
| 内存报错 | CPU 内置内存控制器损坏 | 更换 CPU |
| PCIe 报错 | CPU 内置 PCIe 控制器损坏 | 更换 CPU |

排查技巧：可先把 CPU1、CPU2 **对调安装**，观察故障是否跟随 CPU 移动，以判断是 CPU 本身还是插槽问题；同时注意清理 CPU 插座和散热硅脂。

#### 5.4.2 主板（Mainboard/Motherboard）

- 安装在机箱内，是服务器最基本、最重要的组件之一，又称系统板、主机板、母板。
- 服务器主板专为**高稳定性、高性能、高兼容性**设计（服务器 7×24 小时高负载运行，要求严格）。
- 组成 = 集成电路 + 芯片 + 接口。接口方面服务器主板比 PC 提供更多**冗余接口**便于扩展。

**主板上重要芯片（4 类）：**

1. **PCH（Platform Controller Hub，平台控制中心）**：英特尔的集成芯片，同时具备原"南桥"（ICH，I/O Controller Hub，输入输出控制中心——管理 PCIe、SATA、USB、PS/2 等接口）和"北桥"（MCH，Memory Controller Hub，内存控制中心——连接 CPU 与内存）的功能。从 Intel 5 系芯片组起，北桥功能完全并入 CPU，主板上只见 PCH。
2. **BMC（Baseboard Management Controller，基板管理控制器）**：属于 **IPMI（Intelligent Platform Management Interface，智能平台管理接口）**规范的一部分，用于**远程管理服务器**：远程开关机、挂载 ISO 安装操作系统、诊断、配置管理、故障排查。BMC 是**独立系统**，不依赖服务器 CPU、内存、BIOS 和操作系统（可以在关机状态下工作），又能与它们交互。iLO 口连接的正是 BMC。
3. **BIOS（Basic Input/Output System，基本输入输出系统）**：驱动硬件的底层软件程序，程序本体存放在主板 **ROM（Read-Only Memory，只读存储器）**芯片中，不可读写、断电不丢失。
4. **CMOS（Complementary Metal-Oxide-Semiconductor，互补金属氧化物半导体）**：主板上的一块可读写、低耗电的 RAM 存储器，存放 **BIOS 的设置参数**和系统时间日期，由纽扣电池供电。修改 BIOS 设置实际就是修改 CMOS 中的参数。**抠掉 CMOS 电池 = BIOS 复位（恢复出厂设置）**。

**主板重要接口：**

- 2 个 CPU 插槽、8 个内存插槽（数量随主板型号变化）、PCIE 扩展插槽、CMOS 电池接口、SAS/SATA 硬盘接口、硬盘电源接口、风扇接口等。

**PCIE（PCI Express，外围组件互连快速总线）与 Riser 卡：**

- PCIE 是新一代总线接口，用于扩展网卡、GPU 卡、RAID 卡等，数据直接与 CPU 交换。
- 常见插槽长度 4 种：**x1、x4、x8、x16**，向下兼容（如 x16 插槽可插 x1/x4/x8 设备）；x16 速率可达 GB/s 级，主要用于显卡和高性能扩展卡。
- **Riser 卡（转接卡）**：插在主板 PCIE 接口上，本身不含可用组件、唯一功能是转接，提供 2 个或更多插槽供 PCIE 网卡、GPU 卡安装——解决机箱空间限制，让扩展卡"竖着插"。
- PCIE 扩展卡尺寸规范：半高（≤68.9mm）/ 全高（≤111.15mm）；半长（≤167.65mm）/ 全长（≤312mm）。全高全长插槽向下兼容小尺寸卡。

#### 5.4.3 内存（Memory）

- 又称内存储器、主存储器；内存条 = 内存芯片 + 电路板 + 金手指。
- 作用：**暂时存放 CPU 运算数据以及与硬盘等外部存储器交换的数据**，是外存与 CPU 之间的桥梁。所有程序都在内存中运行，操作系统把需要运算的数据从内存调入 CPU，运算完成再传出。内存性能直接影响服务器整体表现。

**服务器内存的 4 种类型：**

| 类型 | 英文全称 | 特点 |
|---|---|---|
| UDIMM | Unbuffered Dual In-line Memory Module，无缓冲双列直插式内存模块 | 地址和控制信号不经缓冲器直达 DRAM 芯片；同频率下延时小 |
| RDIMM | Registered DIMM，寄存式双列直插式内存模块 | 内存上加了寄存器（Register），位于 CPU 与内存颗粒之间；容量和频率更容易做高 |
| LRDIMM | Load-Reduced DIMM，低负载双列直插式内存模块 | 不用复杂寄存器，只用简单缓冲，降低主板电力负载且几乎不影响性能 |
| NVDIMM | Non-Volatile DIMM，非易失性双列直插式内存模块 | 断电（意外断电、系统崩溃、正常关机）也能保留数据 |

**服务器内存技术（4 项）：**

1. **ECC（Error Checking and Correcting，错误检查与纠正）**：发现并纠正错误数据位（电子干扰可能导致某一位存储出错）。局限：只能检测和纠正**单比特（1 bit）错误**，同时 2 bit 以上错误无能为力。
2. **Chipkill**：内存控制器提供的内存保护功能（概念类似带校验的磁盘阵列），写数据时写入多个 DIMM 芯片，可同时检查并修复 **4 个错误数据位**，比 ECC 更强。
3. **内存热插拔（Hot Swap）**：不关机、不断电直接插拔内存。
4. **Register（寄存器/目录寄存器）**：相当于内存的"目录"——收到读写指令先检索目录，命中则直接取用，提高内存工作效率。

**内存标签识别（以一根典型内存条为例）：**

标签上依次包含：品牌 → 容量（4GB/8GB/16GB/32GB 等）→ Rank 数（1R/2R/4R/8R）→ 数据位宽（x8）→ 代数与规格 → 频率 → 类型标识。

- **PC3 = DDR3（Double Data Rate 3）**，**PC4 = DDR4**：DDR4 相比 DDR3 电压更低、能耗更低、支持容量更大、频率更高、兼容性更好，目前服务器主要使用 DDR4（DDR5 正在普及）。
- **L = 低电压版**（Low Voltage），不带 L 为标准版。
- **12800 = 1600 MHz**；10600 = 1333 MHz；8500 = 1066 MHz；6400 = 800 MHz。
- 后缀：**R = RDIMM**；**E = ECC UDIMM**（带 ECC 功能的 UDIMM）；个人 PC 内存中 S = 笔记本规格（SO-DIMM），U = 台式机规格。

#### 5.4.4 硬盘（Hard Disk Drive / Solid State Drive）

**基本分类：**

- **SSD（Solid State Drive，固态硬盘）**：由固态电子存储芯片阵列制成（控制单元 + 存储单元：Flash 芯片、控制芯片、RAM 芯片），读写性能和稳定性超高。
- **HDD（Hard Disk Drive，机械硬盘）**：由盘片、磁头、转轴、控制电机、磁头控制器、数据转换器、接口、缓存组成；磁头沿盘片半径方向运动 + 盘片每分钟几千转高速旋转，定位到指定位置读写数据。

**硬盘接口类型（6 种）：**

| 接口 | 全称 | 说明 |
|---|---|---|
| IDE | Integrated Drive Electronics | 传统古老接口，已基本淘汰 |
| SCSI | Small Computer System Interface，小型计算机系统接口 | 为小型计算机设计的存储接口，可对多设备动态分工、灵活分配多任务 |
| SATA | Serial ATA（Serial Advanced Technology Attachment） | 串行连接，嵌入式时钟信号、纠错能力强、结构简单、支持热插拔 |
| SAS | Serial Attached SCSI，串行连接 SCSI | 新一代 SCSI 技术，串行传输速度更高，缩短连线下改善内部空间；**向下兼容 SATA** |
| PCIe | PCI Express | 数据直接通过总线与 CPU 相连，省去内存中转，速度 2~8 GB/s 级 |
| M.2 | 原名 NGFF（Next Generation Form Factor） | Intel 推出的接口规范，兼容 SATA/PCIe 多种协议，速度快但贵，主要用于固态硬盘 |

> **SATA 与 SAS 的外观区分**：SATA 的数据线接口与电源接口**中间分开**；SAS 的数据接口与电源接口**连在一起**。
> 服务器大多数用 **SATA 或 SAS** 接口；个人 PC 以 SATA 和 M.2 为主。

**硬盘参数：**

1. **容量**：单块 500GB / 1TB / 2TB / 4TB 甚至更大；单台服务器一般支持 12 或 24 块硬盘；
2. **尺寸**：2.5 英寸（小盘，固态硬盘都是 2.5 英寸）/ 3.5 英寸（大盘，以机械硬盘为主，多用于存储服务器）；
3. **缓存（Cache）**：位于硬盘与内存之间的缓冲区，解决机械硬盘写入慢的问题（常见 8MB、32MB）；注意缓存并非全部用于缓存数据，并非越大越好；
4. **转速（RPM，Revolutions Per Minute）**：PC 硬盘 5400/7200 转；服务器硬盘 7200/10000/15000 转——服务器性能要求更高，转速也更高。

**硬盘槽位与系统表示（重点）：**

- 服务器前面板硬盘槽位从 **0 号位**开始编号：0 号槽位 = 第一块硬盘，1 号槽位 = 第二块，以此类推。系统报错显示的"几号槽位"即对应此编号。
- Linux 操作系统中，硬盘以设备文件表示：`/dev/sda`、`/dev/sdb`、`/dev/sdc`……
  - `sd` 表示 SATA/SAS（SCSI 类）接口的块设备；
  - `a` = 第 1 块硬盘，`b` = 第 2 块，`c` = 第 3 块……`/dev/sdo` = 第 15 块硬盘。

#### 5.4.5 网卡（NIC）

- **NIC = Network Interface Card，网络接口卡**，也叫网络适配器（Network Adapter）。没有网卡设备就无法上网。
- **按接口分：**
  - 电口网卡：插网线（RJ45 接口），带宽 10M/100M/1000M（自适应，10M 已基本淘汰），个人 PC 常用；
  - 光口网卡：插光模块、配合光纤使用，带宽 1G/10G/25G/40G/100G。
- **按安装方式分：**
  - 集成网卡：网卡模块集成在主板上，不可插拔；
  - 独立网卡：通过 PCIE 插槽加装，支持插拔。
- 两种维度可交叉组合（集成/独立 × 电口/光口），服务器一般既有集成网卡也有独立网卡。

#### 5.4.6 RAID 卡（服务器独有）

- **RAID = Redundant Array of Independent Disks，独立磁盘冗余阵列**（也称磁盘阵列）。
- RAID 卡：将多个硬盘合理组合成阵列，兼顾**数据存储性能**与**数据安全性**的附加设备，用于高性能服务器；个人 PC 一般没有 RAID 卡，这是 PC 与服务器在组件上的最大差别之一。
- 课程后续会专门讲 RAID 卡的配置与工作原理。

> **补充：常见 RAID 级别速览（帮助理解 RAID 卡作用）**
>
> | 级别 | 原理 | 特点 |
> |---|---|---|
> | RAID 0 | 条带化（Striping），数据分散写入所有盘 | 读写最快，无冗余，坏一块盘数据全毁 |
> | RAID 1 | 镜像（Mirroring），两块盘互为备份 | 安全性高，容量利用率仅 50% |
> | RAID 5 | 分布式奇偶校验，至少 3 块盘 | 允许坏 1 块盘，性能与容量利用率折中 |
> | RAID 6 | 双重校验 | 允许同时坏 2 块盘 |
> | RAID 10 | 先镜像后条带，至少 4 块盘 | 性能与安全兼得，成本高 |

#### 5.4.7 电源（PSU）

- **PSU = Power Supply Unit，电源供应单元**。服务器属于弱电产品，部件工作电压一般在 ±12V 以内的直流电；电源负责把 220V 市电转换为服务器可用的直流电。
- 服务器一般配 **2 个电源（A/B 两路冗余）**：正常时各承担 50% 功率；一个故障时另一个自动承担 100%。

#### 5.4.8 风扇（Fan Module）

- 服务器风扇是**风扇模组**（多个风扇组成，有时一个模组内含 2 个风扇），特点：转速高、风量大、模组化、智能化。
- 电源线 4 针：第 1 针黑色**接地**（GND，Ground）；第 2 针红色 **+12V 正极**；第 3 针黄色 **FG（Frequency Generator，频率发生）信号——风扇测速**；第 4 针 **PWM（Pulse Width Modulation，脉冲宽度调制）——调节转速**。
- 智能化：可根据服务器温度情况自动调速，监控系统中可查看风扇转速等参数。

### 5.5 服务器品牌与资产信息

- 常见品牌：**HPE（惠普）、Dell（戴尔）、华为（Huawei）、浪潮（Inspur）、联想（Lenovo）、曙光（Sugon）** 等。
- 型号：各厂商自定义系列，如华为 RH2288 V2/V3。
- **SN 号**：厂商出厂时分配的**全球唯一**标识码，用于服务器资产管理；
- **资产号**：企业采购后自行编制的**内部**资产管理编号。
- 二者区别：SN 是"厂商给设备的身份证号"，资产号是"东家给设备编的工号"。

---

<a id="p1-6"></a>
## 六、网络设备：交换机与路由器

### 6.1 交换机（Switch）

- 又称交换式集线器，是一种**基于 MAC（Media Access Control，介质访问控制）地址识别**、完成封装转发数据包功能的网络设备，可为接入的任意两个网络节点提供独享的电信号通路。

**理解交换机工作方式（课程比喻）：**

- 集线器（Hub）= 拿大喇叭在小区里喊（全员广播，效率低）；
- 交换机 = 找物业查住户表（按 MAC 地址精准转发）：
  1. 物业查到"冰冰"住哪 → 直接告诉你（MAC 地址表中命中，直接转发）；
  2. 物业不知道 → 打内部电话逐户询问（向除接收端口外的所有端口**广播**），冰冰本人回应后物业记录下来（**MAC 地址学习**：把主机 MAC 与交换机端口的对应关系写入 **MAC 地址表**）；
  3. 问遍全小区都没有 → 说明目标不在本小区（目标不在本广播域，需转发给上层设备）。

**交换机两大功能：** ① 数据转发；② 广播（未知单播帧的泛洪）+ **MAC 地址学习**。

**交换机分类：**

- **按网络拓扑位置分（三层架构）**：
  - **接入交换机（Access Switch）**：直接连接服务器；
  - **汇聚交换机（Aggregation/Distribution Switch）**：连接交换机与交换机；
  - **核心交换机（Core Switch）**：机房与机房之间的骨干网交换机。
- **按工作层次分**：**二层交换机（Layer 2 Switch）**只做数据链路层转发；**三层交换机（Layer 3 Switch）**兼具路由功能（为什么叫二层/三层，见第二部分 OSI 七层模型）。

**交换机接口类型：**

| 接口 | 用途 |
|---|---|
| RJ45 电口 | 插网线（RJ45 = Registered Jack 45） |
| 光口 | 插光纤，需配合光模块使用 |
| Console 口 | 本地登录交换机做配置（现在多为 RJ45 形态），插 Console 线 |
| AUI / BNC | 连接同轴电缆（AUI = Attachment Unit Interface；BNC = Bayonet Neill-Concelman），现已基本绝迹 |
| FDDI 口 | FDDI = Fiber Distributed Data Interface，光纤分布式数据接口，用于骨干网，高端千兆交换机可见 |

**交换机外观（典型配置）：**

- 前面板：48 个 10/100/1000M 自适应以太网电口（1 号槽位）+ 8 个 10GB 光口（2 号槽位）+ 3 个扩展槽位（3/4/5 号位）；
- 后面板：扩展槽位、风扇模块、电源模块、型号 SN 标签、接地端子；
- 3 个重要管理接口：
  - **Console 口**：本地配置管理；
  - **ETH 管理口**：配置 IP 地址后可连接笔记本电脑远程管理；
  - **USB 口**：插 U 盘，一般用于交换机系统升级。

**交换机厂商**：华为（Huawei）、新华三（H3C）、思科（Cisco）、中兴（ZTE）、锐捷（Ruijie）、普联（TP-Link）、网件（Netgear）、腾达（Tenda）等。互联网数据中心以华为、H3C、锐捷居多；部分机房核心网多用思科设备。

### 6.2 路由器（Router）

- 连接 Internet 中各局域网（LAN，Local Area Network）和广域网（WAN，Wide Area Network）的设备，根据信道情况**自动选择和设定最佳路由**，按最佳路径发送信号。
- 工作在网络层（第三层），是网络间的"中转枢纽站"（好比火车站、机场）。

**工作原理：** 路由器为每个数据分组（Packet）选择最佳传输路径（好比导航地图规划路线），依据是路由器中的**路由表（Routing Table）**。数据到达后查路由表决定"从哪里走"。

**路由表的维护方式（两大类）：**

- **静态路由（Static Routing）**：手动配置，好比交警在路边立的路标——简单、可控，但网络变化时不会自动更新；
- **动态路由（Dynamic Routing）**：由路由协议（Routing Protocol）自动计算和更新路由信息，好比导航 App——哪里堵了、路断了会自动重新规划。

**路由器外观（以华为路由器为例）：**

- 后面板：SRU（Switching and Routing Unit，交换路由处理单元）主控板槽位、2 个电源模块槽位、风扇模块槽位；
- 前面板：接地端子、ESD（Electro-Static Discharge，静电释放）插孔、产品型号、4 个 SIC（Smart Interface Card）槽位、2 个 WSIC 槽位、XSSC 槽位；
- 主控面板：系统指示灯、主备指示灯、SD 卡槽、USB、光口指示灯、2 个千兆光口/电口、Mini USB、Console 口、重置按钮。

### 6.3 传输设备（补充认知）

- 主要功能：**延长传输距离，实现长途通信**；另一项重要功能是**复用（Multiplexing，提高线路利用率）**。
- 复用技术：**频分复用 FDM（Frequency Division Multiplexing）、时分复用 TDM（Time Division Multiplexing）、波分复用 WDM（Wavelength Division Multiplexing）、码分复用 CDM（Code Division Multiplexing）**。
- 传输设备多见于运营商机房，企业机房少见，了解即可。

---

<a id="p1-7"></a>
## 七、光纤与光缆

### 7.1 光纤的结构与传输原理

- **光纤（Optical Fiber）**：由二氧化硅（SiO₂）制成，利用**光的全反射原理**传输数据，是长距离、大带宽传输的首选介质。
- 结构由内到外三层：
  - **纤芯（Core）**：光纤中心部分，头发丝粗细，折射率高，传导光线。单模光纤纤芯直径 8~10 μm（常用 9μm）；多模光纤纤芯直径 50μm 或 62.5μm；
  - **包层（Cladding）**：纤芯外层，折射率低于纤芯（利用折射率差把光"锁"在纤芯中传输），直径约 125μm（125~140μm）；
  - **涂覆层（Coating/保护层）**：最外层，保护光纤免受机械损伤。

### 7.2 单模光纤 vs 多模光纤（重点）

| 对比项 | 单模光纤（SMF，Single Mode Fiber） | 多模光纤（MMF，Multi Mode Fiber） |
|---|---|---|
| 护套颜色（行业惯例） | **黄色** | **橙色 / 水绿色（ Aqua ）** |
| 纤芯直径 | 8~10 μm | 50 / 62.5 μm |
| 光源 | 激光二极管（LD，Laser Diode），设备贵 | LED / VCSEL，设备便宜 |
| 传输距离 | 几十千米甚至上百千米 | 一般几百米 |
| 传输模式 | 只传输一种模式的光信号，沿直线传播、无反射，**不存在模间色散** | 一根纤芯中同时传输多种模式的光 |
| 带宽 | 几乎无限带宽 | 相对有限 |
| 综合成本 | 光纤本身便宜，但光电转换设备贵，**综合成本高** | 综合成本低 |

> 注意："单模 = 一根光纤、多模 = 两根光纤"是错误理解。单模/多模指的是**一根纤芯中传输光的模式数量**。机房里看到黄色的一定是单模，其他颜色（橙、水绿）是多模。

### 7.3 光纤成对使用（收发分离）

- 光纤**成对收发**：两根为一组，**一根发送（TX，Transmit）、一根接收（RX，Receive）**——同一根纤芯不能同时收发。
- 宿舍外面的"光纤"通常是一根线里含两根光纤 + 一根铁丝（铁丝用于固定走线）。
- 平常说的"一根光纤"实际上指两根或多根纤芯。

### 7.4 光纤接口类型（重点，实操必备）

| 接口 | 俗称 | 特征 |
|---|---|---|
| LC | 小方头 | **应用最多**（Lucent Connector） |
| SC | 大方头 | Subscriber Connector |
| ST | 圆形卡扣式 | Straight Tip |
| FC | 圆形螺纹式 | Ferrule Connector / Fiber Connector |
| MPO | 多芯推拉式 | Multi-fiber Push-On，高密度并行光连接（40G/100G 常用） |

两端接口类型可能不同（如一端 SC 一端 LC），要看两端设备实际接口选线。

### 7.5 光纤的"线"家族：跳线、尾纤、预端接、AOC

| 名称 | 定义 | 用途 |
|---|---|---|
| **光纤跳线（Jumper/Patch Cord）** | **两端都有接头**的光纤（LC-LC、SC-SC、LC-SC、MPO-MPO 等组合） | 设备间短距离连接，插在光模块上使用 |
| **尾纤（Pigtail）** | **只有一端有接头**，另一端是裸纤断头 | 断头端与光缆熔接；接头端通过法兰连接 |
| **预端接光纤/光缆** | 出厂时两端接头都已做好 | 免去现场熔接，即插即用；**扇形预端接**可一端 1 芯、另一端分出 12~144 芯，大幅减少布线数量（如原需 200 根跳线的场景，几根 12 芯预端接光缆即可完成） |
| **AOC** | **Active Optical Cable，有源光缆**：两端接头就是**光模块**，模块与线一体，直接插设备光口即可，无需另配光模块 | 常见 10G/25G/40G 规格，常用于服务器与交换机连接 |

**补充：DAC（Direct Attach Cable，无源直连铜缆）**——与 AOC 类似的一体化线缆，但线体为铜、无光电转换，适合机柜内短距离（一般 ≤7m）连接，成本低、功耗小。

**法兰（光纤适配器，Optical Fiber Adapter/Connector Adapter）**：光纤连接适配器，用于尾纤/跳线之间的对接，类型与光纤接口一一对应（LC/SC/FC/ST 法兰等），常用在 ODF 架上。

### 7.6 光缆与 ODF

- **光缆（Optical Cable）**：一定数量的光纤按规定方式组成缆芯，外包保护套（有的还有外护层），"光纤的 Plus 版本"。结构：缆芯、加强钢丝、填充物、保护套；按需增加防水层、缓冲层、绝缘金属导线等。
- 分类：**室外光缆**（如海底光缆——"光缆中的 Plus 之 Plus"）与**室内光缆**。IT 运维工程师日常接触的主要是室内光缆（如预端接光缆）。
- **ODF（Optical Distribution Frame，光纤配线架）**：用于光纤通信中局端**主干光缆的成端与分配**，实现光纤线路的连接、分配和调度。室外光缆进入机房后**熔接**到 ODF，纤芯逐对熔接到前面板端口（例如 6 盘、每盘 12 口，即 1 分 72 芯），再通过跳线连接设备。

### 7.7 分光器（Splitter）

- **分光器（Optical Splitter，光分路器）**：实现"一根光纤进、多根光纤出"的无源器件，常用于 PON（Passive Optical Network，无源光网络）等场景。
- 上下行概念：终端设备 → 分光器为**上行数据**（集中，信号会衰减）；分光器 → 终端为**下行数据**（分发，信号衰减）。下行各路分光比可以不同（如 1 分 2：一路 20% 光功率、另一路 80%）。
- 按工艺分两大类：
  - **FBT（Fused Biconical Taper，熔融拉锥型）**：常见 1 分 2；
  - **PLC（Planar Lightwave Circuit，平面波导型）**：可 1 分多（1:2、1:4、1:8……）。
- 按形态分：盒式（用于机房 ODF 架、光纤交接箱）、盘式（只能装在 ODF 架或交接箱内）、机架式、壁挂式等。

### 7.8 光纤的使用与测试

- 光纤是玻璃材质，**最怕弯折（如 90° 折弯）和重压**，玻璃芯易碎，需小心爱护；损坏后影响传输效率。
- **光功率计（Optical Power Meter）**：测量**绝对光功率**或通过一段光纤后的**相对损耗（光衰）**。
- **补充**：光功率常用 **dBm**（分贝毫伏，以 1mW 为基准的对数单位）表示，损耗用 **dB**（分贝）表示；工程中定位断点还常用 **OTDR（Optical Time-Domain Reflectometer，光时域反射仪）**；光纤接续使用**熔接机（Fusion Splicer）**。

---

# 第二部分：网络基础篇（第 16~30 讲）

<a id="p2-1"></a>
## 八、光模块（Optical Transceiver）

### 8.1 光模块的作用与结构

- 光纤只是**传输介质**，自己不能发光——为光纤"发光"的是**光模块**。
- 光模块插在服务器或交换机的光口上，作用是**光电转换**：发送端把电信号转换成光信号，经光纤传送后，接收端再把光信号转换回电信号。
- 组成：光接口、光电子器件（**光发射组件 + 光接收组件**）、功能电路。
- 光模块上有**两个孔**：一个发光口（TX，Transmit），一个收光口（RX，Receive）——收光的孔不发光（"独眼龙"），与光纤成对收发对应。

### 8.2 单模光模块 vs 多模光模块

与光纤一样，光模块分为**单模模块**和**多模模块**，必须配套使用（单模模块配单模光纤、多模模块配多模光纤）：

| 模块类型 | 典型型号 | 拉环/模块颜色 | 传输距离 |
|---|---|---|---|
| 单模（长距离） | SFP-10GB-LR（Long Range，长距离） | 蓝色 | 10 km |
| 单模（长距离） | SFP-10GB-ER（Extended Range，扩展距离） | 红色 | 40 km |
| 单模（长距离） | SFP-10GB-ZR | 白色 | 80 km |
| 多模（短距离） | SFP-10GB-SR（Short Range，短距离） | 黑色 | 300 m |

**快速区分方法（3 种）：**

1. **看波长**：单模模块波长 1310nm 或 1550nm；多模模块波长 850nm；
2. **看拉环颜色**：黑色拉环 = 多模；蓝色 / 黄色 / 绿色拉环 = 单模；
3. **看光**（注意安全，勿直视）：单模模块波长长，肉眼基本看不到发光口的光；多模模块通电后可看到红光。

### 8.3 封装类型与速率（看外形知带宽）

| 封装类型 | 英文全称 | 常见速率 |
|---|---|---|
| SFP | Small Form-factor Pluggable，小型可插拔 | 1.25G / 2.5G / 6G |
| SFP+ | Enhanced Small Form-factor Pluggable，增强型小封装 | 10G |
| XFP | 10-Gigabit Small Form-factor Pluggable | 10G |
| SFP28 | — | 25G |
| QSFP+ | Quad Small Form-factor Pluggable Plus，四通道小封装 | 40G |
| QSFP28 | Quad Small Form-factor Pluggable 28 | 100G |

### 8.4 光模块的波长

- 单模模块：**1310nm（纳米）、1550nm** 两种，可传输几十千米甚至上百千米；
- 多模模块：**850nm**，传输几百米。

---

<a id="p2-2"></a>
## 九、网线及辅材

### 9.1 网线的分类（双绞线）

网线即**双绞线（TP，Twisted Pair）**：内部 8 根线、**两两绞合成 4 组**（绞合是为了抵消电磁干扰/串扰），每组颜色不同。

| 类别 | 标识 | 带宽/速率 | 典型应用 |
|---|---|---|---|
| 五类线 | CAT5（Category 5） | 100M | 数据中心已淘汰，家庭网络基本够用 |
| 超五类线 | CAT5e | 250M | 办公网络（可支撑千兆） |
| 六类线 | CAT6 | 1G | 数据中心业务网 |
| 超六类线 | CAT6A | 10G | 核心网络（该场景现多被光纤替代） |
| 七类线 | CAT7 | 10G 及以上 | 更高速率场景（同样多被光纤替代） |

**屏蔽与非屏蔽：**

- **屏蔽线（STP，Shielded Twisted Pair）**：外层包裹铝箔或铜编织网，可屏蔽外界干扰，性能更好；
- **非屏蔽线（UTP，Unshielded Twisted Pair）**：无屏蔽层，价格便宜；
- 六类线（CAT6）内部还有**十字骨架**，把 4 组线分隔开，进一步降低串扰。

> 网线价格差异（5 元/米 vs 10 元/米）的本质就是**类别（CAT5/5e/6…）与屏蔽工艺**不同。

### 9.2 线序标准：T568A 与 T568B（重点）

制作水晶头（RJ45 接头）时 8 根线必须按固定顺序排列，国际标准有两种（"白 X"指带白色条纹的同色对线）：

| 引脚 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| **T568B（常用）** | 白橙 | 橙 | 白绿 | 蓝 | 白蓝 | 绿 | 白棕 | 棕 |
| **T568A** | 白绿 | 绿 | 白橙 | 蓝 | 白蓝 | 橙 | 白棕 | 棕 |

- 两种标准的区别：**1 号与 3 号线对调、2 号与 6 号线对调**。
- **直通线（平行线）**：两端采用相同标准（都是 568A 或都是 568B）。工程中普遍使用 **568B** 两端做法。
- **交叉线**：一端 568A、另一端 568B。
- **补充**：老规则是同类设备互联用交叉线、异类设备用直通线；现代设备普遍支持 Auto MDI/MDIX（自动翻转），日常基本都用直通线。

### 9.3 测线仪与理线架

- **测线仪（Cable Tester）**：制作水晶头后测试网线是否制作正确——测试时 8 个指示灯依次从 1 到 8（或 8 到 1）循环闪亮即通序正确；某个灯不亮或乱序则该线芯有问题。
- **数据中心中网线的定位**：网线一般用作**带外管理网**；业务网大多使用光纤（也有网线做业务线的场景）。
- **理线架（Cable Manager/理线器）**：安装在机架前端，做水平方向的线缆管理；没有理线架不可能达到整齐美观的布线效果。

---

<a id="p2-3"></a>
## 十、OSI 与 TCP/IP 参考模型

### 10.1 网络协议与标准组织

- **网络协议（Network Protocol）**：网络上所有设备之间**通信规则的集合**，规定通信时信息采用的格式及其意义——相当于计算机之间交流的"语言"，双方必须使用相同协议才能通信。
- 常见协议：TCP、UDP、IP、ARP、HTTP、FTP、SMTP、SSH、Telnet 等。
- 制定全球统一标准的组织：
  - **ISO（International Organization for Standardization，国际标准化组织）**——提出了 OSI 七层模型；
  - **IEEE（Institute of Electrical and Electronics Engineers，电气电子工程师协会）**；
  - 美国国家标准局（ANSI）、电子工业协会（EIA）、国际电信联盟（ITU）、互联网工程任务组（IETF，Internet Engineering Task Force）等。

> **易混淆提醒**：ISO 是"组织"，OSI 是"模型"。OSI（Open System Interconnection，开放式系统互联通信参考模型）由 ISO 提出。

### 10.2 OSI 七层模型（理论模型）

OSI 将通信系统中的数据流划分为 **7 层**，从下到上依次为。每一层为上一层提供服务，自身功能由下一层提供：

| 层号 | 名称（中/英） | 功能 | 数据单位（PDU） | 典型协议 |
|---|---|---|---|---|
| 7 | 应用层 Application Layer | 确定进程间通信性质，为用户提供网络与应用软件的接口 | APDU | DNS、FTP、HTTP、Telnet |
| 6 | 表示层 Presentation Layer | 解决信息语法表示：数据格式转换、加密解密、压缩 | PPDU | JPEG、加密格式 |
| 5 | 会话层 Session Layer | 建立、维护、管理会话（如服务器验证用户登录） | SPDU | — |
| 4 | 传输层 Transport Layer | 分段上层数据，建立**端到端**连接，保障可靠、按序传输 | 数据段（Segment） | TCP、UDP |
| 3 | 网络层 Network Layer | **寻址和路由选择**，跨多条链路把包送达目标网络 | 数据包（Packet） | IP、ICMP、ARP |
| 2 | 数据链路层 Data Link Layer | 相邻节点间**无差错**传输，介质访问、链路管理 | 数据帧（Frame） | 以太网、MAC、802.11 |
| 1 | 物理层 Physical Layer | 提供物理连接，传输**比特流**（传输介质本身不算在内，有人戏称"第 0 层"） | 比特（Bit） | 双绞线、同轴电缆等 |

- **下三层（1~3）为底层**，负责网络数据传输；**上三层（5~7）为高层**，面向主机应用。
- 数据链路层寻址的是 MAC 地址，网络层寻址的是 IP 地址。

### 10.3 TCP/IP 四层模型（事实标准）

- **TCP/IP（Transmission Control Protocol / Internet Protocol，传输控制协议/网际协议）**：不是只有 TCP 和 IP 两个协议，而是由 FTP、SMTP、TCP、UDP、IP 等协议共同构成的**协议族**（协议组），因 TCP 和 IP 最具代表性而得名。它借鉴 OSI 概念建立，是网络互联的**事实标准**。

| OSI 七层 | TCP/IP 四层 |
|---|---|
| 应用层、表示层、会话层 | **应用层** |
| 传输层 | **传输层** |
| 网络层 | **网络层（互联网层）** |
| 数据链路层、物理层 | **网络接口层** |

### 10.4 数据的封装与解封装（重点）

- **PDU（Protocol Data Unit，协议数据单元）**：各层传输数据的统称，一般由本层协议头（Header）、协议尾（Tail）和数据构成。
- **封装（Encapsulation）**：发送数据时，数据沿协议栈**从上到下**逐层传递，**每一层都给数据加上一个本层首部**——好比发货前层层打包。
- **解封装（De-encapsulation）**：接收方**从下到上**逐层剥掉首部取出数据——好比拆快递。
- **对等通信（Peer-to-Peer Communication）**：源端每一层都与目的端的对应层"对话"，使用本层协议通信。

**封装流程（主机 A → 主机 B）：**

```
上三层产生真正的数据（应用数据）
  → 传输层：封装 TCP 或 UDP 头（数据段）
  → 网络层：封装 IP 头（数据包）
  → 数据链路层：封装 MAC 头（数据帧）
  → 物理层：转换为比特流，经网卡 → 网线/光纤发出
```

**中间设备怎么处理数据（重要）：**

- **二层交换机**：只解封装到**第 2 层（数据链路层）**，查看 MAC 地址决定转发——上面的层次对它来说是"黑盒"，所以说**二层交换机基于 MAC 地址转发**；
- **路由器 / 三层交换机**：解封装到**第 3 层（网络层）**，查看源 IP 和目的 IP，查路由表转发——所以**路由器基于 IP 地址转发**；
- 查完再重新封装好交给下一跳设备，逐段传递直到目的地；主机 B 收到后从下往上逐层解封装，拿到真正的数据。

---

<a id="p2-4"></a>
## 十一、二层交换原理

### 11.1 二层交换机与三层交换机

- **二层交换机（Layer 2 Switch）**：工作在 OSI 第 2 层数据链路层，**基于 MAC 地址转发**数据；
- **三层交换机（Layer 3 Switch）**：工作在第 3 层网络层，**基于 IP 地址转发**（兼具部分路由功能）。

### 11.2 MAC 地址表与交换机工作原理

每台交换机内部维护一张**MAC 地址表（MAC Address Table）**，记录"MAC 地址 ↔ 端口 ↔ VLAN"的对应关系，它是交换机**自动学习**得来的：

1. **初始状态**：交换机刚启动时，MAC 地址表为空（无表项）；
2. **学习**：终端加电后互发数据帧，交换机收到后解封装到第 2 层，**提取帧中的源 MAC 地址**，学习到"该端口下连接哪台主机"；
3. **生成/更新 MAC 地址表**：将 MAC 地址、对应端口、所属 VLAN 存入表中；
4. **转发**：将数据帧从除接收端口外的其他端口发送出去（未知单播则泛洪）。

**交换机的三种转发行为：**

| 行为 | 英文 | 含义 |
|---|---|---|
| 泛洪 | Flooding | 把从某端口进来的帧转发给**除接收端口以外的所有端口** |
| 转发 | Forwarding | 把从某端口进来的帧转发给**某一个特定端口**（MAC 表命中时） |
| 丢弃 | Discarding | 直接把帧丢弃 |

**收到不同帧的处理逻辑：**

- 收到**单播帧（Unicast）**：先查 MAC 地址表——无记录则**泛洪**；有记录则从对应端口**转发**（若出端口恰为接收端口则丢弃）；
- 收到**广播帧（Broadcast）**：直接**泛洪**。

### 11.3 MAC 地址（重点）

- **MAC（Media Access Control，介质访问控制）地址**：固化在网卡上的**物理地址**，如同身份证号，**全球唯一**。
- 长度 **48 位（48 bit，即 6 字节）**，用十六进制表示（如 `00-1A-2B-3C-4D-5E`）：
  - **前 24 位**：厂商编码（OUI，Organizationally Unique Identifier，组织唯一标识符）——同一厂商的产品前 24 位相同；
  - **后 24 位**：序列号，厂内唯一。

### 11.4 冲突域与广播域（重点概念）

| 概念 | 英文 | 定义 |
|---|---|---|
| 冲突域 | Collision Domain | **区域内设备竞争同一带宽**的节点集合 |
| 广播域 | Broadcast Domain | **接收相同广播消息**的节点集合（广播帧能够到达的范围） |

**各设备对两个域的隔离能力：**

- **集线器（Hub）**：不能识别 MAC 地址和 IP 地址，收到数据**以广播方式**发给所有端口；同一时刻只能传输一组数据，冲突需重传，**所有端口共享整个带宽**——集线器下所有端口构成**一个冲突域**（整个网络即一个冲突域）；
- **交换机（Switch）**：基于 MAC 地址表定向转发；**每个端口都是独立的物理网段，独享带宽**——**交换机分割冲突域（每个端口一个冲突域）**，但交换机所有端口仍**共同构成一个广播域**；
- **路由器（Router）**：**既隔离冲突域，也隔离广播域**——路由器的**每一个端口就是一个广播域**。

### 11.5 ARP 与 RARP 协议

**问题背景**：IP 地址在第 3 层，MAC 地址在第 2 层，二者不能直接"跨层交流"。发送 IP 数据包时既要封装第 3 层的 32 位 IP 地址，也要封装第 2 层的 48 位 MAC 地址；但发送时往往只知道对方 IP、不知道对方 MAC——这就需要 ARP。

- **ARP（Address Resolution Protocol，地址解析协议）**：根据网络层 IP 包头中的 IP 地址，解析出目标设备的 MAC 地址。
  - 过程：主机 A 只知道主机 B 的 IP → A 发送 **ARP 请求广播**（"谁是 192.168.1.20？请告诉我你的 MAC"）→ 主机 B 收到后**单播回应**自己的 MAC → A 记录下来开始通信。
- **RARP（Reverse Address Resolution Protocol，反向地址转换协议）**：与 ARP 相反，**已知自己的 MAC 地址，向 RARP 服务器请求分配 IP 地址**。
  - 过程：主机发送携带本机 MAC 的 RARP 广播 → RARP 服务器查 RARP 列表，找到该 MAC 对应的 IP 则响应分配；找不到则不回应 → 主机收到响应后用该 IP 通信，一直收不到则初始化失败。
  - 层次归属：在 OSI 七层模型中 RARP 属于**数据链路层**；在 TCP/IP 模型中属于**网络层**。

---

<a id="p2-5"></a>
## 十二、IP 地址、进制转换与子网划分

### 12.1 IP 地址基础

- **MAC 地址用于局域网内的通信寻址；IP 地址是不同局域网之间的通信地址。**
- **IP（Internet Protocol，网际协议）地址**：互联网协议地址，是 IP 协议提供的统一地址格式，为互联网上每个网络、每台主机分配一个**逻辑地址**，以此**屏蔽物理地址（MAC）的差异**——好比"电话号码"，路由器相当于电信局的程控交换机。
- IPv4 地址是 **32 位二进制数**，分割为 4 个 8 位（4 字节），用**点分十进制**表示为 `A.B.C.D`，A/B/C/D 均为 0~255 的十进制整数（如 203.71.212.5）。

**地址结构：网络位 + 主机位**

- 每个 IP 地址由 **网络 ID（网络位）+ 主机 ID（主机位）** 组成：
  - 同一物理网络的所有主机使用**同一个网络 ID**；
  - 网络中每台主机有一个唯一的主机 ID。
  - 例如 `192.168.100.111/24`：`192.168.100` 是网络位，`111` 是主机位——网络位相同的主机在同一网段。
- 两个特殊地址**不能配置给主机**：
  - **网络地址**：主机位二进制**全 0**，代表整个网段（如 192.168.100.0）；
  - **广播地址**：主机位二进制**全 1**，代表向网段内所有主机广播（如 192.168.100.255）。

### 12.2 IP 地址的分类（A/B/C/D/E 类）

Internet 委员会定义了 5 类 IP 地址。其中 A、B、C 三类由 InterNIC 在全球统一分配，D、E 为特殊地址：

| 类别 | 地址范围（第一段） | 网络位/主机位 | 网络数量 | 每网主机数 | 默认掩码 |
|---|---|---|---|---|---|
| A 类 | 1.0.0.1 ~ 127.255.255.254 | 8 位 / 24 位 | 126 个 | 1600 多万 | 255.0.0.0（/8） |
| B 类 | 128.0.0.1 ~ 191.255.255.254 | 16 位 / 16 位 | 16384 个 | 6 万多 | 255.255.0.0（/16） |
| C 类 | 192.0.0.1 ~ 223.255.255.254 | 24 位 / 8 位 | 209 万多个 | 254 | 255.255.255.0（/24） |
| D 类 | 224~239 | 组播（Multicast）地址 | — | — | — |
| E 类 | 240~255 | 科研保留 | — | — | — |

- A 类网络地址最高位必须为 `0`，B 类为 `10`，C 类为 `110`；
- A 类网络少、主机多，适合超大型组织；C 类网络多、主机少，适合小规模局域网。

### 12.3 公有地址与私有地址

- **公有地址（Public Address）**：由 **InterNIC（Internet Network Information Center，互联网信息中心）** 负责管理分配，向其注册申请的组织机构使用，**全网唯一**，可直接访问 Internet。
- **私有地址（Private Address）**：**非注册地址**，专为组织机构**内部局域网**使用，不同企业可重复使用（出网需经 NAT，Network Address Translation，网络地址转换）。

> **补充：私有地址三段（必背）**
>
> | 类别 | 私有地址范围 |
> |---|---|
> | A 类 | 10.0.0.0 ~ 10.255.255.255（10.0.0.0/8） |
> | B 类 | 172.16.0.0 ~ 172.31.255.255（172.16.0.0/12） |
> | C 类 | 192.168.0.0 ~ 192.168.255.255（192.168.0.0/16） |

**特殊 IP 地址（必背）：**

| 地址 | 含义 |
|---|---|
| 127.0.0.1（127.x.x.x 网段） | **回环测试地址（Loopback）**，代表本机 IP |
| 主机位全 0（如 192.168.1.0） | 网络地址（网段本身） |
| 主机位全 1（如 192.168.1.255） | 网段广播地址 |
| 0.0.0.0 | 代表任意网络，一般用作**默认路由** |
| 255.255.255.255 | **全网广播地址**（受限广播） |

### 12.4 IPv4 与 IPv6

- **IPv4**：32 位地址，总量约 **43 亿（2³² ≈ 4.29×10⁹）** 个；因需求激增，**2019 年 11 月 25 日已全部分配完毕**。
- **IPv6（Internet Protocol Version 6）**：地址长度扩展为 **128 位**，采用**十六进制**表示（IPv4 用十进制），共 8 组、每组 4 位十六进制数（共 32 个字符），地址总量极其庞大（约 3.4×10³⁸）。现在很多 APP 开屏页都会标注"支持 IPv6"。

### 12.5 子网掩码（Subnet Mask）

- **作用：区分 IP 地址中的网络位和主机位**。
- 掩码同样是 32 位、分 4 段表示：**对应网络位的部分全为 1，对应主机位的部分全为 0**。
- 两种写法等价：`172.16.10.100/16` = `172.16.10.100 255.255.0.0`。
  - `/16` 表示掩码前 16 位为 1：二进制 `11111111.11111111.00000000.00000000` = 十进制 `255.255.0.0`。
- 三类地址的**默认子网掩码**：A 类 /8（255.0.0.0）、B 类 /16（255.255.0.0）、C 类 /24（255.255.255.0）。

### 12.6 进制转换（网络计算基本功）

**进制的本质**：用有限的符号按规则排列组合表示无限的量。**基数**是每类进制使用的符号个数，**权值**是每一位符号代表的大小（小数点前从右往左，权值从 0 次方开始递增）。

| 进制 | 符号 | 基数 | 规则 | 示例 |
|---|---|---|---|---|
| 十进制 D（Decimal） | 0~9 | 10 | 逢十进一 | 4956 = 4×10³ + 9×10² + 5×10¹ + 6×10⁰ |
| 二进制 B（Binary） | 0、1 | 2 | 逢二进一 | 1011 = 1×2³ + 0×2² + 1×2¹ + 1×2⁰ = 11 |
| 八进制 O（Octal） | 0~7 | 8 | 逢八进一 | 4275₈ = 4×8³ + 2×8² + 7×8¹ + 5×8⁰ |
| 十六进制 H（Hexadecimal） | 0~9、A~F（A=10，B=11，C=12，D=13，E=14，F=15，不区分大小写） | 16 | 逢十六进一 | 81A1₁₆ = 8×16³ + 1×16² + 10×16¹ + 1×16⁰ |

**① 二/八/十六进制 → 十进制：按权展开求和**

例：二进制 `111011` = 1×2⁵ + 1×2⁴ + 1×2³ + 0×2² + 1×2¹ + 1×2⁰ = 32+16+8+0+2+1 = **59**。

**② 8 位二进制的权值（IP 计算核心，必须滚瓜烂熟）：**

| 位置 | 2⁷ | 2⁶ | 2⁵ | 2⁴ | 2³ | 2² | 2¹ | 2⁰ |
|---|---|---|---|---|---|---|---|---|
| 权值 | 128 | 64 | 32 | 16 | 8 | 4 | 2 | 1 |

- 8 个 1（11111111）相加 = **255**——这就是 IP 地址每段最大不超过 255 的原因；
- 例：`11000000` = 128 + 64 = **192**；`10101000` = 128+32+8 = **168**。因此二进制 `11000000.10101000.00000001.00001011` = 点分十进制 `192.168.1.11`。

**③ 十进制 → 二进制：除 2 取余，逆序排列**

例：59 ÷ 2 依次取余（余数从下往上写）= `111011`。转八进制就除 8，转十六进制就除 16（余数 10 记作 A、11 记作 B，以此类推）。

> **小结**：网络计算中以**二进制 ↔ 十进制**转换为主，务必熟练。

### 12.7 子网划分（Subnetting，重点难点）

**为什么要划分子网？**

- 一个网段有 300 台主机：C 类 /24 只有 2⁸ = 256 个地址，**不够用**；
- 干脆分一个 B 类网段：共 65536 个地址，**远超 300**，剩下的 6 万多个地址又不能给别的网段用（否则就成同一网段了）——**严重浪费**；
- 默认分类掩码（有类网络）粒度太粗 → 需要更精细的划分手段。

**两大技术：**

- **VLSM（Variable Length Subnet Mask，可变长子网掩码）**：在分类网络基础上，把**主机位最左边的若干位"借"出来划归网络位**，划分出多个大小可变的子网；
- **CIDR（Classless Inter-Domain Routing，无类别域间路由）**：摆脱 A/B/C 分类限制，按 `/n` 前缀长度表示网络（见 12.8 超网）。

**核心公式（必背）：**

- 子网数量 = 2^N（N = 向主机位借的位数，即掩码向右移动的位数）；
- 每个子网可用主机数 = 2^M − 2（M = 剩余主机位数；减 2 是减去网络地址和广播地址）。

**例题 1（等长子网划分）：将 192.168.0.0/24 划分为 2 个等长子网**

1. 划 2 个子网 → 2¹ = 2 → 借 1 位，新掩码 **/25**（255.255.255.128）；
2. 借来的那一位分别为 0 和 1，得到两个子网：
   - 子网 1：`192.168.0.0/25`（范围 192.168.0.0 ~ 192.168.0.127）；
   - 子网 2：`192.168.0.128/25`（范围 192.168.0.128 ~ 192.168.0.255）；
3. 每个子网可用主机 = 2⁷ − 2 = **126 台**。

**例题 2（课堂练习）：将 192.168.10.0/24 等分成 4 个子网**

- 4 = 2² → 借 2 位，新掩码 **/26**（255.255.255.192 = 128+64）；
- 四个子网：`192.168.10.0/26`、`192.168.10.64/26`、`192.168.10.128/26`、`192.168.10.192/26`；
- 每个子网可用主机 = 2⁶ − 2 = 62 台。

**等长与变长子网划分：**

- **等长子网划分**：把一个网络均分成多个大小相同的子网（如上例）；
- **变长子网划分**：在等长划分的基础上，对其中某个或某几个子网**继续借位细分**——不同子网掩码不同，按需分配地址，减少浪费。

### 12.8 超网（Supernet）与路由汇总

- **超网（Supernetting）/CIDR 汇总**：与子网划分相反，**把多个连续的小网络合并成一个大网络**，用于对外统一通告路由、缩减路由表规模。
- 方法：把各网段 IP 写成二进制，将掩码**向左移动**，**网络位相同部分**的公共前缀即为汇总网络。
- 例：某公司有 `192.168.0.0/24` ~ `192.168.3.0/24` 四个连续网段，第三段 0~3 的二进制前 6 位相同，可汇总通告为 **`192.168.0.0/22`**（255.255.252.0）。

### 12.9 综合练习（课堂原题）

给出 IP 地址 `192.168.5.100`，回答：

1. 属于哪类地址？——第一段 192，落在 192~223 → **C 类**；
2. 默认子网掩码？—— **/24（255.255.255.0）**；
3. 公有还是私有？—— 192.168.x.x 属于 C 类私有地址段 → **私有地址**；
4. 网络地址？——主机位全 0 → **192.168.5.0**；
5. 广播地址？——主机位全 1 → **192.168.5.255**。

---

<a id="p2-6"></a>
## 十三、TCP 与 UDP 通信

### 13.1 传输层概述

传输层（OSI 第 4 层）的主要功能：

1. **分段上层（应用层）数据**；
2. 建立**端到端（主机到主机）**的连接；
3. 保证数据**按序、可靠、正确**地传输。

这些功能由 **TCP** 和 **UDP** 两个协议来保障，不同应用选用不同的协议。

### 13.2 TCP 与 UDP 对比（重点）

| 对比项 | TCP | UDP |
|---|---|---|
| 全称 | Transmission Control Protocol，传输控制协议 | User Datagram Protocol，用户数据报协议 |
| 连接方式 | **面向连接**（收发数据前必须先建立可靠连接） | **无连接**（直接把数据发出去） |
| 可靠性 | 可靠传输（确认、重传、排序） | 简单、不可靠的信息传输服务（可靠性交给应用层负责） |
| 开销 | 需多次交互，网络开销大 | 开销小、效率高 |
| 比喻 | **打电话**：对方接了才能说事，确保对方收到 | **发微信/邮件**：发是发了，对方收没收到不负责 |
| 典型应用 | HTTP、FTP、SSH、SMTP、Telnet | DHCP、NTP、DNS |

> **补充：常见协议端口号（Well-Known Ports，0~1023）**
>
> | 协议 | 端口 | 协议 | 端口 |
> |---|---|---|---|
> | FTP（File Transfer Protocol，文件传输协议） | 20/21 | DNS（Domain Name System，域名系统） | 53 |
> | SSH（Secure Shell，安全外壳协议） | 22 | DHCP（Dynamic Host Configuration Protocol，动态主机配置协议） | 67/68 |
> | Telnet（远程登录协议） | 23 | HTTP（HyperText Transfer Protocol，超文本传输协议） | 80 |
> | SMTP（Simple Mail Transfer Protocol，简单邮件传输协议） | 25 | POP3（Post Office Protocol v3，邮局协议第 3 版） | 110 |
> | NTP（Network Time Protocol，网络时间协议） | 123 | HTTPS（HTTP Secure，安全超文本传输协议） | 443 |
> | SNMP（Simple Network Management Protocol，简单网络管理协议） | 161 | — | — |

### 13.3 TCP 报文头部（TCP Header）结构

| 字段 | 长度 | 作用 |
|---|---|---|
| 源端口号 Source Port | 16 位 | 标识发送方应用进程 |
| 目的端口号 Destination Port | 16 位 | 标识接收方应用进程 |
| 序号 Sequence Number（SEQ） | 32 位 | 对该传输方向上每个字节的编号，用于判断数据**有序性**（如当前序号 1000、发了 1000 字节，则下一个序号为 2000） |
| 确认号 Acknowledgment Number（ACK） | 32 位 | 响应对方报文 = 收到的序号 + 1；三次握手时还要携带自己的序号 |
| 头部长度 Header Length | 4 位 | 以 4 字节为单位指出 TCP 头部长度，最大 15×4 = **60 字节** |
| 保留位 Reserved | 6 位 | 保留 |
| 标志位 Flags | 6 位 | URG/ACK/PSH/RST/SYN/FIN（见下） |
| 窗口大小 Window Size | 16 位 | **流量控制**手段：告诉对端本端 TCP 缓冲区还能容纳多少字节（"你可以说慢点，一分钟 300 字"） |
| 校验和 Checksum | 16 位 | 发送端填充，接收端执行 CRC（Cyclic Redundancy Check，循环冗余校验）算法，检验报文段传输中是否损坏 |
| 紧急指针 Urgent Pointer | 16 位 | 正偏移量，与序号相加 = 最后一个紧急数据的下一字节序号（配合 URG 标志） |

**6 个标志位（Flags）含义：**

| 标志 | 全称 | 含义 |
|---|---|---|
| URG | Urgent | 紧急指针是否有效 |
| ACK | Acknowledgment | 确认号是否有效 |
| PSH | Push | 提示接收端应用程序立即从 TCP 缓冲区读走数据 |
| RST | Reset | 要求对方重新建立连接 |
| SYN | Synchronize | 请求建立一个连接 |
| FIN | Finish | 通知对方本端要关闭连接 |

### 13.4 TCP 三次握手（Three-way Handshake，建立连接）

TCP 的可靠性从"连接建立"就开始体现——正式传输数据前，双方先通过 3 次报文交互确认彼此收发能力：

```
主机 A                          主机 B
   | --- ① SYN（请求建立连接，携带初始序号 x） ---> |
   |                                              |
   | <-- ② SYN + ACK（确认你的请求；携带初始序号 y）-- |
   |                                              |
   | --- ③ ACK（确认收到你的响应）----------------> |
   | ============ 连接建立，开始传输数据 =========== |
```

1. **第一次握手**：主机 A 主动打开端口，向主机 B 发送含**同步序列号标志位（SYN）**的数据段，告诉 B 两件事：① 我要和你通讯；② 你可以用哪个序列号作为起始数据段来回应我；
2. **第二次握手**：主机 B 收到请求后，回送带 **ACK（确认应答）+ SYN（同步序列号）**标志位的数据段，告诉 A：① 我已收到你的请求，可以传输数据了；② 你要用哪个序列号作为起始数据段来回应我；
3. **第三次握手**：主机 A 收到后再发送一个 **ACK 确认应答**，确认收到 B 的数据段，并告知"我要开始传输实际数据了，你做好准备"。三次握手完成，A、B 可以传输数据。

### 13.5 TCP 四次挥手（Four-way Handshake，断开连接）

数据传输完成后，通过 4 次报文交互安全断开连接（TCP 是全双工，两个方向要分别关闭）：

```
主机 A                          主机 B
   | --- ① FIN（A 数据发完，请求断开）------------> |
   | <-- ② ACK（B 确认：A→B 方向关闭）------------ |
   | <-- ③ FIN（B 数据也发完，反方向请求关闭）------ |
   | --- ④ ACK（A 确认：B→A 方向关闭）-----------> |
   | ============ 双向关闭，连接断开 =============== |
```

1. **第一次**：主机 A 完成数据传输后，将 **FIN** 位置 1，提出停止 TCP 连接的请求；
2. **第二次**：主机 B 收到 FIN 后响应，**ACK** 置 1，确认"A→B"这个方向上的连接将关闭；
3. **第三次**：主机 B 数据也传完后，将 **FIN** 位置 1，提出反方向的关闭请求；
4. **第四次**：主机 A 收到后 **ACK** 置 1 确认，双向关闭完成。

**小结**：TCP 采用面向连接的方式（先握手、再传数据、后挥手），使收发双方在正式传输前就有了交互确认，大大提高了数据通信的可靠性；UDP 则牺牲可靠性换取效率。大部分应用使用 TCP 传输。

---

<a id="p2-7"></a>
## 十四、VLAN 虚拟局域网

### 14.1 什么是 VLAN

- **VLAN（Virtual Local Area Network，虚拟局域网）**：一组**逻辑上**的设备和用户，**不受物理位置限制**，可以根据功能、部门及应用等因素组织起来，相互之间的通信就如同在同一个网段中一样。
- 交换机端口的两种 VLAN 属性：**VLAN ID**（端口所属的 VLAN 编号）和 **VLAN Tag**（端口允许通过的标签）。

### 14.2 为什么需要 VLAN

- **广播域（Broadcast Domain）**：广播帧能够传送到的范围，即能够直接通信的范围。严格来说，**广播帧、多播（组播）帧、目标不明确的单播帧**都能在同一广播域中畅行无阻。
- 默认情况下，**一台二层交换机就是一个单一的广播域**——所有端口的主机互发广播（如 ARP 请求），一方面**消耗网络带宽**，另一方面每台收到广播的计算机还要**消耗 CPU** 处理，引发"广播风暴"。
- 典型需求：主机 A 不想收到主机 C 的广播；管理员要求主机 B 不能访问主机 A——把主机分到不同的 VLAN 即可。
- 路由器虽能隔离广播域，但**接口有限**；在二层交换机上用 VLAN 隔离广播域，比加路由器**经济**得多。

### 14.3 VLAN 的四大作用

1. **抑制广播域**：广播只在本 VLAN 内传播，避免广播风暴浪费带宽和 CPU；
2. **创建虚拟工作组**：按部门/功能划分虚拟工作组，不受物理位置限制（如"主机 B 不能访问主机 A"放入不同 VLAN 即可）；
3. **提高局域网安全性**：不同 VLAN 的报文相互隔离，一个 VLAN 内的用户不能直接访问其他 VLAN 的用户；
4. **提高网络健壮性**：故障被限制在本 VLAN 内，不影响其他 VLAN 正常工作。

> **补充**：交换机所有接口默认属于 **VLAN 1**（PVID = 1）。

### 14.4 VLAN 的实现原理（IEEE 802.1Q，重点）

VLAN 隔离广播域的手段是给数据帧**打标签（Tag）**：

- 普通交换机工作在数据链路层，只能对二层封装做标识。**IEEE 802.1Q** 协议规定：在以太网帧的**目的 MAC 地址和源 MAC 地址之后**，插入 **4 字节的 VLAN Tag**，标识该帧所属的 VLAN。

**VLAN Tag 的 4 个字段（共 4 字节）：**

| 字段 | 全称 | 长度 | 说明 |
|---|---|---|---|
| TPID | Tag Protocol Identifier，标签协议标识符 | 16 bit | 判断数据帧是否携带 VLAN Tag，固定取值 **0x8100** |
| PRI | Priority，优先级 | 3 bit | 表示报文的 802.1P 优先级（QoS 用） |
| CFI | Canonical Format Indicator，标准格式指示位 | 1 bit | 标识 MAC 地址在不同传输介质中是否以标准格式封装（0 = 标准封装，1 = 非标准） |
| VLAN ID | 虚拟局域网标识 | 12 bit | 报文所属 VLAN 的编号；取值范围 0~4095，**0 和 4095 为协议保留，实际可用 1~4094** |

- **Tag 帧**：带 VLAN 标签的数据帧；**Untag 帧**：不带标签的普通数据帧。
- 设备处理能力差异：
  - **终端设备**（个人 PC、服务器、集线器）：**只能收发 Untag 帧**；
  - **交换机、路由器、AC（Wireless Access Controller，无线接入控制器）**：既能收发 Tag 帧也能收发 Untag 帧；
  - 交换机**内部**流转的数据帧一律带 Tag，统一处理。
- **PVID（Port Default VLAN ID，端口缺省 VLAN ID）**：数据帧进入端口时：
  - 若**不带 Tag** 且端口配置了 PVID → 给帧打上该端口的 PVID 标签；
  - 若**已带 Tag** → 即使端口配置了 PVID 也不再重复打标签。

### 14.5 三种端口类型与三种链路类型（重点）

| 端口类型 | 接收 Untag 帧 | 接收 Tag 帧 | 发送帧 |
|---|---|---|---|
| **Access（接入端口）** | 打上本端口 PVID，继续处理 | 比较 Tag 与 PVID：一致则处理，**不一致则丢弃** | **剥离 Tag** 后发送（Untag 帧） |
| **Trunk（干道端口）** | 丢弃（课程简化说法；华为设备实际是打上 PVID 后再判断是否允许通过） | 检查该 VLAN 是否在允许列表：允许则处理，**否则丢弃** | Tag 的 VLAN ID 与 PVID 相同 → 剥 Tag 发送；与 PVID 不同但被允许 → **保留 Tag** 发送 |
| **Hybrid（混合端口）** | 打上本端口 PVID，继续处理 | 检查是否允许该 VLAN：允许则处理，否则丢弃 | 按端口上各 VLAN 的 **untagged / tagged 属性**分别决定剥 Tag 或保留 Tag 发送 |

**三种链路类型：**

| 链路 | 名称 | 连接对象 | 传输的帧 |
|---|---|---|---|
| Access 链路 | 接入链路 | 用户主机 ↔ 交换机 | 只能是 **Untag 帧** |
| Trunk 链路 | 干道链路 | 交换机 ↔ 交换机 | 只能是 **Tag 帧** |
| Hybrid 链路 | 混合链路 | 均可（交换机互联或连接用户计算机） | 可属于多个 VLAN，可收发多个 VLAN 的报文 |

> Hybrid 与 Trunk 在多数场景可通用；但当**一个接口需要以 untagged 方式加入多个 VLAN**（如一个接口下接不同 VLAN 的网络）时，**必须使用 Hybrid 接口**。

### 14.6 VLAN 的划分方式

- **静态 VLAN（基于端口的 VLAN）**：手动把交换机端口划入 VLAN（如端口 1、7 加入 VLAN 5，端口 2、10 加入 VLAN 10）。配置简单，但一台台指定很繁琐；网络规模上百台后配置量大，且计算机换端口时必须同步改端口 VLAN 配置——不适合频繁变动的网络。
- **动态 VLAN**：根据端口所连计算机的信息**自动决定**端口所属 VLAN：
  - **基于 MAC 地址的 VLAN**：通过记录端口所连网卡 MAC 地址决定端口归属（MAC 地址 A 被划入 VLAN 10，则它连到哪个端口，哪个端口就属于 VLAN 10）——初始配置复杂，但后续管理方便；
  - **基于子网的 VLAN**：按所连计算机的 **IP 地址**决定；
  - **基于用户的 VLAN**：按**当前登录用户**决定（依据 OSI 不同层的信息划分）。

### 14.7 VLAN 的通信原理（重点）

**① 同一 VLAN、同一交换机内通信（如产品部一楼两台 PC，VLAN 10）：**

1. 主机发送普通 **Untag 帧** → 进入部门交换机；
2. 交换机打上 **VLAN 10 标签**，帧变成 Tag 帧；
3. 交换机根据 **MAC 地址表 + VLAN 标签**把帧送到目的主机所在端口；
4. 该端口（Access 口）发送前**剥掉标签**，还原成 Untag 帧交给主机。

**② 同一 VLAN、跨交换机通信（产品部一楼 → 二楼）：**

- 数据帧经两台交换机互联的 **Trunk 链路**传输（Trunk 链路透传 Tag 帧不做修改）→ 对端交换机查 MAC 表 + VLAN 找到出端口 → 剥 Tag → 送达。

**③ 多 VLAN 跨交换机通信（一楼二楼各有产品部 VLAN 10 + 运营部 VLAN 20）：**

- 各部门数据各自打上对应 VLAN 标签 → 全部经 Trunk 链路传到对端 → 对端交换机按 VLAN 分别找到各自部门的端口，剥标签送达——不同颜色（VLAN）互不干扰。

**④ VLAN 间（跨 VLAN）通信的 3 种方法（重点）：**

| 方法 | 组成 | 网关位置 | 优点 | 缺点 |
|---|---|---|---|---|
| ① 三层交换机 + **VLANIF 接口** | 三层交换机（路由模块）+ 二层交换机 | 在三层交换机上为每个 VLAN 创建虚拟接口 VLANIF（VLAN Interface），如 VLANIF10 配 `192.168.10.254/24`、VLANIF20 配 `192.168.20.254/24`，作为各部门网关，三层路由互转 | 不用额外硬件设备；VLANIF 可创建很多个，管理方便；**现在最常用** | 三层交换机价格较高 |
| ② 路由器**物理接口**（多臂路由） | 路由器 + 二层交换机 | 各 VLAN 的二层交换机分别接路由器不同物理口（如 F0/1、F0/2），接口配各网段 IP 作网关 | 管理配置简单 | 路由器物理接口少，**扩展能力弱**，部门多时接口不够 |
| ③ 路由器**逻辑子接口**（单臂路由，Router-on-a-Stick） | 路由器 + 二层交换机 | 多 VLAN 的二层交换机共用路由器**一个物理接口**（如 F0/2），在其上创建多个逻辑子接口（F0/2.10、F0/2.20），分别配各网段 IP 作网关 | **扩展成本低**，新增部门只需添加子接口 | 配置比较复杂 |

> **VLANIF 接口**：三层交换机上的虚拟逻辑接口（可理解为一个 VLAN 的接口集合），不占用物理端口，是当前数据中心 VLAN 间路由的主流方案。

### 14.8 VLAN 配置命令（以华为交换机为例）

**基础命令：**

```
<Huawei> system-view                       # 进入系统视图
[HW] sysname SW1                           # 交换机重命名
[SW1] vlan 10                              # 创建 VLAN 10（并进入 VLAN 视图）
[SW1] vlan batch 20 30                     # 批量创建 VLAN 20、VLAN 30（多个用空格隔开，可 2 to 5）
[SW1] undo vlan 10                         # 删除 VLAN 10
[SW1] undo vlan batch 2 to 5               # 批量删除 VLAN 2~5
[SW1] display vlan                         # 查看 VLAN 信息
[SW1] display this                         # 查看当前视图配置
[SW1] quit                                 # 退出当前视图
```

**Access 端口配置（连接 PC）：**

```
[SW1] interface GigabitEthernet 0/0/1      # 进入 1 号端口（GigabitEthernet=千兆电口类型）
[SW1-GigabitEthernet0/0/1] port link-type access        # 配置为 Access 端口
[SW1-GigabitEthernet0/0/1] port default vlan 10         # 端口加入 VLAN 10
[SW1-GigabitEthernet0/0/1] quit
```

**Trunk 端口配置（交换机互联）：**

```
[SW1] interface GigabitEthernet 0/0/3
[SW1-GigabitEthernet0/0/3] port link-type trunk          # 配置为 Trunk 端口
[SW1-GigabitEthernet0/0/3] port trunk allow-pass vlan 2 to 10   # 允许 VLAN 2~10 通过
[SW1-GigabitEthernet0/0/3] port trunk allow-pass vlan all       # 允许所有 VLAN 通过
[SW1-GigabitEthernet0/0/3] quit
```

**Hybrid 端口配置：**

```
[SW1] interface GigabitEthernet 0/0/4
[SW1-GigabitEthernet0/0/4] port link-type hybrid         # 配置为 Hybrid 端口
[SW1-GigabitEthernet0/0/4] port hybrid untagged vlan 20  # 以 untagged 方式加入 VLAN 20（该 VLAN 数据发出时剥标签）
[SW1-GigabitEthernet0/0/4] port hybrid untagged vlan all # 以 untagged 方式加入所有 VLAN
[SW1-GigabitEthernet0/0/4] port hybrid tagged vlan 2 to 5 # 以 tagged 方式加入 VLAN 2~5（发出时保留标签）
[SW1-GigabitEthernet0/0/4] quit
```

**批量配置（免逐口敲）：**

```
# 方式一：端口组
[SW1] port-group pg1                                    # 创建端口组 pg1
[SW1-port-group-pg1] group-member GigabitEthernet 0/0/1 to GigabitEthernet 0/0/5   # 1~5 号端口入组
[SW1-port-group-pg1] port link-type access
[SW1-port-group-pg1] port default vlan 10               # 组内端口统一加入 VLAN 10

# 方式二：VLAN 视图下批量加端口
[SW1] vlan 10
[SW1-vlan10] port GigabitEthernet 0/0/1 to GigabitEthernet 0/0/5    # 1~5 号端口全部加入 VLAN 10
```

**验证实验（课程案例）：** PC1、PC3 划入 VLAN 10（192.168.100.10/.20，掩码 255.255.255.0），PC2 划入 VLAN 20（192.168.200.1）：

- PC1 ping PC3 —— **通**（同 VLAN、同网段）；
- PC1 / PC3 ping PC2 —— **不通**（不同 VLAN 二层隔离）。

---

<a id="p2-8"></a>
## 十五、路由基础

### 15.1 什么是路由

- **路由（Routing）**：从源到目的地往往有多条路可走，路由就是**计算数据包如何转发（怎么走最近、最快）的网络进程**。
- 快递类比：武汉给北京寄热干面，可以空运也可以陆运，中间经过机场、高铁站、快递点等**中转站**——每个中转站就是一台**路由器**；快递到达每个中转站都要决定"下一站发哪"——下一站就叫**下一跳（Next Hop）**。
- **路由器的三大能力：**
  1. **学习路径**：只要链路经过它，它就知道去往目的地有哪些下一站；
  2. **选出最佳路由**：去往目的地可能有多条线路，知道怎么走最快最省；
  3. **转发数据包**：把"快递"打包发走。

### 15.2 路由器的工作原理

- 路由器是**三层设备**，根据 **IP 地址**转发数据；端口数量比交换机少很多。
- **IP 地址的网络位确定分组的目标网络**（好比电话号码前几位判断是武汉还是北京）；**主机位 + MAC 地址确定具体接收者**（输完整号码锁定到人）。
- 处理流程：路由器某接口收到数据包 → 解封装到第 3 层，查看**目标网络地址** → 与**路由表（Routing Table）**条目匹配：
  - **匹配成功** → 从路由条目指定的端口把数据转发出去（发给下一跳）；
  - **无匹配条目** → 直接**丢弃**数据包，并向发件方回送"地址不可达"消息（ICMP，Internet Control Message Protocol，互联网控制报文协议）。

### 15.3 路由表及其三种来源（重点）

**路由表（Routing Table）由路由条目构成，条目来源有三种：**

| 来源 | 标识 | 生成方式 | 特点 |
|---|---|---|---|
| **直连路由（Direct Route）** | **C（Connected）** | 路由器接口配置 IP 地址且物理状态 Up 后**自动生成** | 路由器的一个接口就是一个网段；只要接口 Up，直连条目一直存在 |
| **静态路由（Static Route）** | **S（Static）** | 管理员**手动配置**（如"凡去北京的都从 1 号口出"） | 网络变化时**不能自适应**，需人工修改 |
| **动态路由（Dynamic Route）** | 按协议（如 OSPF、RIP） | 路由器运行**路由协议**，从其他路由器自动学习 | 网络拓扑变化时自动更新路由表 |

- **默认路由（Default Route，标识 S*）**：一种特殊的静态路由，目的地址为 **0.0.0.0/0**，固定写在路由表**最后**——当所有条目都不匹配时，数据统一从默认路由指定的下一跳转发出去；若不配默认路由且无匹配，数据包直接丢弃。
- **边缘路由器**：连接交换机（面向局域网）的路由器。
- 查看路由表命令（华为）：`display ip routing-table`。

> **示例（课程案例）**：5 台路由器连接 10~70 共 7 个网段。
> R1 路由表 = 直连 10.1.1.0（F0/0 口）+ 直连 20.1.1.0（F0/1 口）+ 默认路由（下一跳 20.1.1.2，即 R2 的 F0/0）；去 10 网段从 F0/0 出，去 20 网段从 F0/1 出，其余全部交给 R2。
> R2 路由表 = 3 条直连（20/30/60 网段）+ 静态路由去 10 网段（下一跳 20.1.1.1 = R1）+ 静态去 70 网段（下一跳 60.1.1.2 = R5）+ 默认路由（下一跳 30.1.1.2 = R3）。

### 15.4 路由的分类

**按路由表构造方式：直连路由、静态路由、动态路由。**

**静态路由的两种特殊形式：**

- **默认路由**（见 15.3）；
- **浮动路由（Floating Route，路由备份）**：配置两条（或多条）**目的地址相同、下一跳不同**的静态路由，依据管理距离（AD）/优先级形成**主备关系**——优先级高（数值小）的作为主路由生效，另一条作备份；主路由失效时备份路由自动顶上。

静态路由适用于**中小型网络**（校园网、办公网等）。

**动态路由的两种分类维度：**

**① 按作用范围分：**

| 类型 | 全称 | 适用范围 | 代表协议 |
|---|---|---|---|
| IGP | Interior Gateway Protocol，内部网关协议 | **自治系统（AS，Autonomous System）内部**——可理解为一个企业/机构的大网络（如阿里云内部网络） | RIP、OSPF、IS-IS |
| EGP | Exterior Gateway Protocol，外部网关协议 | **自治系统之间** | BGP（Border Gateway Protocol，边界网关协议） |

**② 按路由算法分：**

| 类型 | 工作方式 | 代表协议 |
|---|---|---|
| 距离矢量路由协议（Distance Vector） | 根据**距离 + 方向**通告路由：距离用**跳数**等度量衡量，方向指下一跳/出接口。路由器**定期向所有邻居发送整张路由表**；每台路由器只知"往哪个方向发、距离多远"，**并不知道到达目的网络的完整路径**——像沿途问路的"路标" | RIP、IGRP、EIGRP |
| 链路状态路由协议（Link State） | 路由器把自身链路状态信息**泛洪（Flooding）给区域内所有路由器**，每台路由器都保存一份**相同的网络信息（链路状态数据库）**且绝不改动，各自独立计算最优路径——像人手一张完整的"交通地图" | **OSPF、IS-IS** |

> 注：课程语音原文在此处表述有误（把 OSPF/IS-IS 说成了"距离矢量"），本笔记按标准知识纠正：**RIP 是距离矢量协议，OSPF 和 IS-IS 是链路状态协议**。

### 15.5 路由的度量值与优先级（重点）

**① 度量值（Metric）：同一协议内部比优劣**

- **度量值（Metric）**：基于某一个路由协议计算出来的**路径代价**，路由器根据该值大小选择转发路径（值越小越优）。
- 示例：路由器 A → D 有两条路——上路 A→B→C→D 代价 2+5+2 = **9**；下路 A→E→F→C→D 代价 6+3+1+2 = 12 → 度量值 9 的上路写入路由表。
- **等价路由（Equal-Cost Route）**：去往同一目的地址存在**多条度量值相同、且同一路由协议**的路由（如下一跳分别为 1.1.1.1 / 2.2.2.1 / 3.3.3.1，度量值都是 60）→ 可实现**负载分担（Load Balancing/负载均衡）**，流量分摊到多条链路。

**度量的 6 个维度：**

| 维度 | 含义 |
|---|---|
| 带宽（Bandwidth） | 链路可用的流通容量 |
| 开销（Cost） | 与带宽相关，带宽越大开销越小 |
| 延迟（Delay） | 数据从源到目的所花费的时间 |
| 跳数（Hop Count） | 经过的路由器个数 |
| 负载（Load） | 路由器/链路的繁忙程度 |
| 可靠性（Reliability） | 网络连接失效概率的大小 |

**不同协议的度量方式（常考）：**

- **RIP**：度量值 = **跳数**，最大 **15 跳**（16 跳视为不可达）；
- **OSPF**：度量值 = **开销（Cost）**，带宽越大开销越小越优先；
- **EIGRP**：度量值 = 带宽与延迟共同计算的**复合度量**，带宽越大、延迟越小越优先。

**② 优先级（Preference）/ 管理距离（AD）：不同协议之间比优劣**

- 度量值**只对同一种路由协议有意义**——不同协议的度量值含义不同，互相比较毫无意义；
- 要在**不同协议**去往同一目的地的多条路由中做选择，只能比**路由协议的优先级（Preference）**，又称 **AD（Administrative Distance，管理距离）**：路由来源的**信任等级**，每一种路由协议按可靠性从高到低分配一个等级，**数值越小，优先级越高**；
- 优先级由路由协议规定（各厂商默认值不同），**用户无法修改**。

**华为设备默认优先级（必背）：**

| 路由来源 | 优先级 |
|---|---|
| 直连路由（Direct） | 0 |
| OSPF | 10 |
| IS-IS | 15 |
| 静态路由（Static） | 60 |
| RIP | 100 |
| OSPF ASE（外部路由） | 150 |

（思科设备的各协议默认优先级与华为不同，可自行了解。）

> **路由选择总结（一句话）**：
> **不同协议之间 → 比优先级（Pre），数值小的赢；同一协议内部 → 比度量值（Metric/Cost），开销小的赢。**

---

<a id="p2-9"></a>
## 附：全课程知识框架总结

```
数据中心（IDC）IT 运维知识体系
│
├── 一、数据中心是什么（定义 / 分类分级 / 四层结构 / 运营方）
├── 二、物理设施（五道门 / 功能分区 / 电力·消防·制冷·监控·门禁 / 冷热通道 / 带内带外管理）
├── 三、行业名词（CDN / ISP / U位·机架位 / SN / TOR / PUE）
├── 四、IT 系统组成（机柜 42U / 托盘支架 / PDU / KVM / 桥架）
├── 五、服务器硬件（分类 / 前后面板 / CPU / 主板芯片 / 内存 / 硬盘 / RAID卡 / PSU / 风扇 / 网卡）
├── 六、网络设备（交换机三层架构 / 路由器 / 传输设备）
├── 七、光纤光缆（单模多模 / 接口 LC·SC·ST·FC / 跳线尾纤AOC / ODF / 分光器）
├── 八、光模块（单模多模块 / 封装 SFP→QSFP28 / 波长 850·1310·1550nm）
├── 九、网线（CAT5~CAT7 / 屏蔽 / T568A·B 线序 / 测线仪 / 理线架）
├── 十、网络模型（OSI 七层 / TCP/IP 四层 / 封装解封装 / 各层 PDU）
├── 十一、二层交换（MAC 地址表学习 / 泛洪·转发·丢弃 / 冲突域·广播域 / ARP·RARP）
├── 十二、IP 地址（分类 ABC / 公有私有 / 特殊地址 / IPv6 / 子网掩码 / 进制转换 / VLSM 子网划分 / CIDR 超网）
├── 十三、传输层（TCP 三次握手 / 四次挥手 / 报文头部 / TCP vs UDP / 常见端口号）
├── 十四、VLAN（802.1Q 标签 / Access·Trunk·Hybrid / 静态动态 VLAN / VLANIF 单臂路由多臂路由 / 华为配置命令）
└── 十五、路由（路由表 C·S·S* / 静态动态 / IGP·EGP / 距离矢量·链路状态 / 度量值与优先级）
```

**学习建议**：

1. 第一部分硬件篇以"认识 + 会用"为目标，重点记住 U 位、PDU/UPS/PDU 供电路径、光纤接口与单多模区分、RAID 概念；
2. 第二部分网络篇是后续运维工作的核心技能，OSI 模型、IP 地址与子网划分、VLAN、路由四大块必须做到"能算、能配、能排障"；
3. 建议配合模拟器（如华为 eNSP）动手敲 VLAN、静态路由配置，用真实案例巩固子网划分计算。

---
<!-- ============ 笔记二：预备知识·路由·ACL·NAT·网络架构·交换机·服务器硬件·Linux（第 31~70 讲） ============ -->

# 数据中心（IDC）IT 运维工程师学习笔记

> 覆盖课程：尚诚云 IT 课堂《数据中心（IDC）IT 运维工程师课程》第 31~70 课
> 整理方式：不按课时逐讲罗列，而是**按知识主题重组**，便于初学者系统学习。
>
> **阅读约定**
> 1. 所有英文缩写第一次出现时，均标注**英文全称 + 中文含义**，例如：RIP（Routing Information Protocol，路由信息协议）。
> 2. 标注 **【补充】** 的内容是课程字幕中没有讲、但对初学者理解必不可少的背景知识。
> 3. 标注 **【勘误】** 的内容是课程中表述不准确、此处已修正的地方。
> 4. 配置命令以华为 / 华三（H3C）设备为主（与课程一致），思科（Cisco）差异之处会单独注明。

---

## 目录

- 第〇篇　预备基础知识（零基础必读）
- 第一篇　路由技术：RIP 与 OSPF
- 第二篇　ACL 访问控制列表
- 第三篇　NAT 网络地址转换
- 第四篇　数据中心网络架构
- 第五篇　交换机配置与管理
- 第六篇　服务器硬件：基本操作、BIOS、BMC 带外管理、RAID
- 第七篇　Linux 系统管理
- 附录 A　英文术语速查表
- 附录 B　常用命令速查表
- 附录 C　课程作业自测参考答案

### 知识地图（主题与课时对照）

| 主题 | 对应课时 |
|------|----------|
| 路由协议（RIP / OSPF） | 第 31 课 |
| ACL 访问控制列表 | 第 32~35 课 |
| NAT 网络地址转换 | 第 36~38 课 |
| 数据中心网络架构 | 第 39 课 |
| 交换机配置与管理 | 第 40~51 课 |
| 服务器基本操作 / BIOS / BMC / RAID | 第 52~62 课 |
| Linux 系统管理 | 第 63~70 课 |

---

<a id="p3-1"></a>
# 第〇篇　预备基础知识（零基础必读）

> 本篇为【补充】内容。课程字幕默认你已具备这些概念，但作为网络初学者，建议先读完本篇再学习正文。

## 0.1 网络分层模型

网络通信非常复杂，工程师们把它"拆层"来理解和学习。

**OSI 七层模型**（OSI，Open Systems Interconnection，开放系统互连参考模型）：

| 层号 | 名称（中文 / 英文） | 主要功能 | 典型设备 / 协议 |
|---|---|---|---|
| 7 | 应用层 Application | 为用户程序提供网络服务 | HTTP、FTP、DNS |
| 6 | 表示层 Presentation | 数据格式转换、加密压缩 | — |
| 5 | 会话层 Session | 建立、管理、终止会话 | — |
| 4 | 传输层 Transport | 端到端可靠传输，用**端口号**区分应用 | TCP、UDP |
| 3 | 网络层 Network | 用 **IP 地址**实现跨网段寻址和路由 | 路由器、IP、ICMP |
| 2 | 数据链路层 Data Link | 用 **MAC 地址**在相邻节点间传帧 | 交换机、ARP |
| 1 | 物理层 Physical | 比特流传输（电信号 / 光信号） | 网线、光纤、集线器 |

实际工程中更常用 **TCP/IP 四层模型**（TCP/IP，Transmission Control Protocol / Internet Protocol）：网络接口层、网络层、传输层、应用层。

**初学者必须建立的一个核心认知：**

- **交换机**（Switch）主要工作在**第 2 层（数据链路层）**，根据 MAC 地址（MAC，Media Access Control，媒体访问控制）转发数据，只能在**同一个网段内**转发数据。
- **路由器**（Router）工作在**第 3 层（网络层）**，根据 IP 地址（IP，Internet Protocol，互联网协议）转发数据，负责**跨网段**通信。
- 所谓**二层交换机 / 三层交换机**：二层交换机只做同网段转发；三层交换机（Layer 3 Switch）内置了路由功能，既能二层转发又能跨网段路由，常用于数据中心内部。

## 0.2 IP 地址、子网掩码与网关

1. **IPv4 地址**（IPv4，Internet Protocol version 4）：由 32 位二进制数组成，通常写成点分十进制，如 `192.168.1.1`，分为**网络位 + 主机位**两部分。
2. **子网掩码**（Subnet Mask）：用来标明 IP 地址中哪些位是网络位。例如 `255.255.255.0` 表示前 24 位是网络位，这种写法也常记为 `192.168.1.0/24`（`/24` 即前 24 位为网络位，称为**前缀长度**）。
3. **网关**（Gateway）：本网段通往其他网段的"出口"，通常是路由器或三层交换机上与本网段同网段的一个接口地址。
4. **同一网段**：两台主机 IP 与子网掩码按位"与"运算后结果相同，即处于同一网段，可以直接（经交换机）通信；不同网段必须经过网关（路由）转发。

**私有地址（private address）**——【补充】：IPv4 地址分为公网地址（全球唯一，需向运营商申请）和私有地址（仅内网使用，不能直接上互联网），私有地址由 RFC 1918 规定：

| 类别 | 地址范围 | 可用规模 |
|---|---|---|
| A 类私有 | 10.0.0.0 ~ 10.255.255.255 | 10.0.0.0/8，约 1677 万个 |
| B 类私有 | 172.16.0.0 ~ 172.31.255.255 | 172.16.0.0/12，约 104 万个 |
| C 类私有 | 192.168.0.0 ~ 192.168.255.255 | 192.168.0.0/16，约 6.5 万个 |

> 你家里、公司内网看到的多是 `192.168.x.x` 或 `10.x.x.x`，它们不能直接在互联网上使用——这正是后面 **NAT 技术存在的原因**（见第三篇）。

## 0.3 常用端口号速查表（必须背熟）

传输层用**端口号**（0~65535）区分同一台主机上的不同应用。常见的知名端口：

| 端口 | 协议 | 英文全称 | 说明 |
|---|---|---|---|
| 20/21 | TCP | FTP（File Transfer Protocol，文件传输协议） | 21 控制 / 20 数据 |
| 22 | TCP | SSH（Secure Shell，安全外壳协议） | 加密远程登录 |
| 23 | TCP | Telnet（TELecommunications NETwork，远程登录协议） | 明文远程登录 |
| 25 | TCP | SMTP（Simple Mail Transfer Protocol，简单邮件传输协议） | 发邮件 |
| 53 | UDP/TCP | DNS（Domain Name System，域名系统） | 域名解析 |
| 69 | UDP | TFTP（Trivial File Transfer Protocol，简单文件传输协议） | PXE 安装用（见 7.3） |
| 80 | TCP | HTTP（HyperText Transfer Protocol，超文本传输协议） | 网页 |
| 110 | TCP | POP3（Post Office Protocol version 3，邮局协议第 3 版） | 收邮件 |
| 161/162 | UDP | SNMP（Simple Network Management Protocol，简单网络管理协议） | 网管 |
| 443 | TCP | HTTPS（HTTP Secure，超文本传输安全协议） | 加密网页 |
| 520 | UDP | RIP（Routing Information Protocol，路由信息协议） | 见 1.3 |
| 521 | UDP | RIPng（RIP next generation，RIP 下一代版本） | IPv6 用 |

## 0.4 广播、组播与单播【补充】

- **单播**（Unicast）：一对一发送，最常见。
- **广播**（Broadcast）：发给本网段**所有**设备，目的地址为 `255.255.255.255`（或网段的最后一个地址），例如 RIP V1 发布路由就用广播，广播对网络中所有设备都产生打扰。
- **组播**（Multicast）：发给**加入特定组**的一批设备，目的地址为 `224.0.0.0 ~ 239.255.255.255` 的 D 类地址。例如 RIP V2 用 `224.0.0.9` 发路由更新，只有加入该组的路由器才会处理，比广播更高效。

---

<a id="p3-2"></a>
# 第一篇　路由技术：RIP 与 OSPF

## 1.1 路由与路由表基础

**路由**（Routing）：把数据包从一个网络送到另一个网络的**路径选择**过程。路由器靠**路由表**（Routing Table）决定数据包下一跳发往哪里。

**路由表的三大要素**：目的网段、下一跳地址（或出接口）、度量值。

**路由的来源（类型）**：

| 类型 | 说明 | 特点 |
|---|---|---|
| 直连路由（Direct） | 接口配置 IP 并 UP 后自动生成 | 无需配置，优先级最高 |
| 静态路由（Static） | 手工指定 `ip route-static 目的网段 掩码 下一跳` | 简单可控，但不能自动适应拓扑变化 |
| 动态路由（Dynamic） | 路由器之间运行路由协议**自动学习** | 能自动收敛，适合大网络 |

**路由选路规则**（承接课程第 30 课度量知识）：

1. 先比较**优先级**（Preference，越小越优）：不同协议学到的同一目的网段，比协议优先级。华为设备常见默认值：直连 0、OSPF 10、静态 60、RIP 100、OSPF 外部路由 150、BGP（Border Gateway Protocol，边界网关协议）255。
2. 优先级相同（同一协议），再比较**度量值**（Metric，越小越优）：RIP 用"跳数"，OSPF 用"开销（Cost）"。

**动态路由协议分类**：

- **IGP**（Interior Gateway Protocol，内部网关协议）：在一个**自治系统**（AS，Autonomous System，自治系统，指同一个管理机构下的网络群体）内部使用，如 RIP、OSPF、IS-IS、EIGRP。
- **EGP**（Exterior Gateway Protocol，外部网关协议）：在自治系统**之间**使用，如 BGP。

> 本篇的主角 RIP 和 OSPF 都属于 IGP。

## 1.2 RIP 路由信息协议

### 1.2.1 概念与优缺点

**RIP**（Routing Information Protocol，路由信息协议）：一种**分布式、基于距离矢量**（Distance Vector）的路由协议。每个路由器只知道自己"到每个网络的距离（跳数）和方向（下一跳）"，像是在路口只看路牌问路。

| 优点 | 缺点 |
|---|---|
| 实现简单、开销小 | ① 最大跳数 15（取值 0~16，**16 表示不可达**），限制网络规模 |
| 适合小型网络 | ② 周期性交换**完整路由表**，网络大了开销大 |
| | ③ 收敛慢，拓扑变化后需要较长时间才能全网同步 |

### 1.2.2 RIP 的三个版本

| 对比项 | RIP V1 | RIP V2 | RIPng（RIP next generation） |
|---|---|---|---|
| 面向协议 | IPv4 | IPv4 | IPv6 |
| 有类别 | **有类别**路由协议 | **无类别**路由协议 | 无类别 |
| VLSM / CIDR | 不支持 | 支持 | — |
| 报文发送方式 | **广播** | 广播 + **组播 224.0.0.9** | 组播 |
| 路由验证 | 不支持 | 明文验证 + MD5（Message Digest Algorithm 5，消息摘要算法第 5 版）密文验证 | 支持 |
| UDP 端口号 | 520 | 520 | 521 |
| 现状 | 基本淘汰 | 小网络仍有使用 | IPv6 场景 |

**名词解释：**

- **有类别 / 无类别**（Classful / Classless）：有类别协议按 A/B/C 类默认掩码理解地址，不携带掩码信息；无类别协议在报文中携带掩码，才能支持 VLSM 与 CIDR。
- **VLSM**（Variable Length Subnet Mask，可变长子网掩码）：允许在同一网络中使用不同长度的子网掩码，灵活划分子网。
- **CIDR**（Classless Inter-Domain Routing，无类别域间路由）：打破 A/B/C 类界限，用"前缀长度"表示网段（如 `/24`），并支持**路由汇总**（把多条路由合并成一条更短前缀的路由，精简路由表）。
- RIP V2 支持路由认证的意义：**防止非法设备伪造路由更新报文破坏网络**。

### 1.2.3 RIP 配置要点（华为 / 华三）

思路：进入 RIP 进程 → 指定版本 → `network` 通告**直连网段**（连了几个网段就通告几个）。

```bash
<Huawei> system-view
[Huawei] rip                     # 创建 RIP 进程
[Huawei-rip-1] version 2         # 使用 RIP V2
[Huawei-rip-1] network 192.168.1.0   # 通告直连网段
[Huawei-rip-1] network 172.16.1.0
```

```bash
[Huawei] display rip route       # 查看 RIP 学到的路由表
```

RIP 路由表条目解读：**邻居地址**（从哪个邻居学来）、**网段和掩码**、**Cost**（度量值，即跳数）。

## 1.3 OSPF 开放式最短路径优先

### 1.3.1 概念与特点

**OSPF**（Open Shortest Path First，开放式最短路径优先）：一种**链路状态**（Link State）路由协议。与 RIP"问路"不同，OSPF 让区域内**每台路由器都掌握全网地图**（链路状态数据库），各自用 SPF（Shortest Path First，最短路径优先，即 Dijkstra 算法）计算最短路径。

OSPF 特点（课程归纳）：

1. 支持无类别路由（CIDR），方便路由汇总；
2. 支持**区域划分**，限制通告范围；
3. **无路由自环**：基于最短路径算法生成最短路径树，天然无环；
4. **收敛快**：支持触发更新，拓扑一变化立即泛洪新报文；
5. 用 **IP 组播**收发协议报文（组播地址 224.0.0.5 / 224.0.0.6【补充】），对其他设备影响小；
6. 支持**多条等值路由**（等价负载分担、互为备份）；
7. 支持**协议报文认证**，防止恶意报文攻击。

适用场景：**大中型网络**，被大量用于企业网、校园网，是目前使用最广泛的 IGP 之一。

### 1.3.2 OSPF 工作过程（五个步骤）

1. **了解自身**：每台路由器首先了解自己的直连链路；
2. **发现邻居**：通过 Hello 报文寻找邻居路由器，建立邻居关系；
3. **泛洪 LSA**：将描述本机链路状态的 **LSA**（Link State Advertisement，链路状态通告）泛洪（Flooding，向所有邻居扩散）给邻居；
4. **同步 LSDB**：全网最终形成一致的 **LSDB**（Link State Database，链路状态数据库），即"全网地图"；
5. **SPF 计算**：每台路由器基于 LSDB 独立运行 SPF 算法，生成最短路径树，算出的最优路由加入全局路由表。

### 1.3.3 OSPF 的五种报文

| 报文 | 全称 | 作用 |
|---|---|---|
| Hello | — | 建立并**维护邻居关系**，周期发送 |
| DD / DBD | Database Description，数据库描述 | 传送链路状态的**摘要头部信息**（先"对目录"） |
| LSR | Link State Request，链路状态请求 | 从 DD 中找出自己缺的条目，向邻居请求完整信息 |
| LSU | Link State Update，链路状态更新 | 携带一条或多条完整 LSA 回应 LSR |
| LSAck | Link State Acknowledgment，链路状态确认 | 对收到的 LSU 进行确认（可靠传输） |

> 记忆：Hello 交朋友 → DD 交换"目录" → LSR 点菜 → LSU 上菜 → LSAck 签收。

### 1.3.4 OSPF 区域划分

多区域 OSPF 中：

- **区域 0（Area 0）= 骨干区域（Backbone Area）**，**其他所有区域必须与区域 0 相连**。
- 三种路由器角色：

| 角色 | 英文全称 | 说明 |
|---|---|---|
| 内部路由器 | Internal Router | 所有接口都在同一个区域内，只保存本区域链路状态信息 |
| 区域边界路由器 | ABR（Area Border Router） | 连接骨干区域与其他区域，保存区域间路由信息 |
| 自治系统边界路由器 | ASBR（Autonomous System Boundary Router） | 连接 OSPF 自治系统与外部网络（引入外部路由，如 BGP、静态路由） |

**划分区域的作用**（课程重点）：

1. 缩小 LSA 泛洪范围——拓扑变化只影响本区域；
2. 在区域边界（ABR 上）做**路由汇总**，精简路由表；
3. 提高网络扩展性，便于组建大规模网络。

### 1.3.5 OSPF 配置要点（华为 / 华三）

```bash
<Huawei> system-view
[Huawei] ospf                              # 创建 OSPF 进程
[Huawei-ospf-1] area 0                     # 进入区域 0
[Huawei-ospf-1-area-0.0.0.0] network 192.168.1.0 0.0.0.255   # 通告直连网段（后跟反掩码！）
[Huawei-ospf-1-area-0.0.0.0] network 10.1.1.0 0.0.0.255
```

```bash
[Huawei] display ospf routing              # 查看 OSPF 路由表
```

**注意与 RIP 的区别**：OSPF 的 `network` 命令后面跟的是**反掩码 / 通配符掩码**（wildcard mask）——`0` 表示精确匹配，`255` 表示任意。例如 `/24` 掩码 `255.255.255.0` 的反掩码是 `0.0.0.255`；`/32` 的反掩码是 `0.0.0.0`（全精确匹配）。反掩码详细算法见 2.4.5 节。

## 1.4 RIP 与 OSPF 对比总结【补充】

| 对比项 | RIP | OSPF |
|---|---|---|
| 算法类型 | 距离矢量 | 链路状态 |
| 度量值 | 跳数（最大 15） | 开销 Cost（与带宽相关） |
| 收敛速度 | 慢 | 快（触发更新） |
| 网络规模 | 小型（≤15 跳） | 大中型 |
| 防环能力 | 弱（依赖最大跳数等机制） | 强（SPF 算法天生无环） |
| 资源消耗 | 低 | 较高（CPU / 内存） |
| 区域划分 | 不支持 | 支持（骨干区域 Area 0） |
| 报文承载 | UDP 520 | 直接封装在 IP，协议号 89 |

---

<a id="p3-3"></a>
# 第二篇　ACL 访问控制列表

## 2.1 什么是 ACL

**ACL**（Access Control List，访问控制列表）：**一系列规则的集合**。设备（路由器 / 三层交换机）根据这些规则，对经过接口的数据包进行**过滤**——匹配则按动作放行（permit）或丢弃（deny）。

ACL 是网络世界的"门卫清单"：包到了门口，从上往下逐条核对，命中哪条就按哪条办。

**ACL 的常见用途：**

1. 部署在**边界路由器**上控制网络访问，拒绝未知流量 / 恶意攻击进入内网，提高安全性；
2. 抓取特定数据流转发给数据分析服务器（流量镜像分析）；
3. **登录控制**：限制谁可以 Telnet / SSH / SNMP 登录设备；
4. 供 NAT、IPSec 等技术引用，筛选"哪些流量要被处理"；
5. 降低 P2P（Peer-to-Peer，点对点）下载、网络视频等大流量业务的服务等级，防止其占用过多带宽；
6. 过滤**上送 CPU** 的报文（黑名单 / 白名单 / 自定义流），防止 CPU 占用率过高；
7. **路由过滤**：配合路由协议，过滤发布 / 接收的路由信息（如禁止某网段路由发给邻居）。

## 2.2 ACL 工作原理

1. 一条 ACL 规则 = **一个条件 + 一个处理动作**（permit 允许 / deny 拒绝）；
2. 一个设备可配置多条规则，数据包到达后**从第一条开始、自上而下逐条匹配**；
3. 一旦**命中某条规则立即停止**匹配，按该规则的动作处理；
4. 全部规则都不匹配时，命中系统**隐含的最后一条规则 `deny any`（拒绝所有）**——数据包被丢弃。

> **核心记忆点：ACL 的缺省最终动作是"拒绝"。** 所以部署"以拒绝为主"的 ACL 时，最后必须手工补一条 `permit any`，否则合法流量也会被全部丢掉。

## 2.3 ACL 的分类

### 2.3.1 按匹配颗粒度（思科编号范围）

| 类型 | 匹配条件 | 思科编号 | 华为编号【补充】 |
|---|---|---|---|
| 标准 ACL（Standard） | **仅源 IP 地址**（及分片信息、生效时间段） | 1~99 | 基本 ACL：2000~2999 |
| 扩展 ACL（Extended） | 源 IP、目的 IP、协议类型、源端口、目的端口等 | 100~199 | 高级 ACL：3000~3999 |
| 命名 ACL（Named） | 用字符串名代替编号，含标准 / 扩展两种 | 名称 | 同样存在 |
| 定时 ACL（Time-based） | 在以上基础上叠加"生效时间段"条件 | — | — |

华为另外还有二层 ACL（4000~4999）等【补充】，初学者先掌握前两种。

### 2.3.2 其他划分维度

- **按层次**：二层 ACL（基于 MAC 地址）、三层 ACL（基于 IP / 端口）；
- **按应用位置（方向）**：
  - **入方向**（inbound，`in`）：数据包**进入**设备接口时过滤；
  - **出方向**（outbound，`out`）：数据包从接口**发出**前过滤。
  - 一个接口的**一个方向只能应用一组 ACL**。

## 2.4 ACL 语法详解（以思科为例）

### 2.4.1 标准 ACL

```
access-list <表号> <permit|deny> <源IP地址> <反掩码>
```

示例——编号 20 的规则，允许 10.0.0.0/24 网段通过：

```
access-list 20 permit 10.0.0.0 0.0.0.255
```

### 2.4.2 扩展 ACL

```
access-list <表号> <permit|deny> <协议> <源IP> <反掩码> <目的IP> <反掩码> [操作符 端口号]
```

- **协议**：ip、tcp、udp、icmp（ICMP，Internet Control Message Protocol，互联网控制消息协议，即 ping 所用协议）等三层 / 四层协议；
- **操作符**：`eq`（等于）、`gt`（大于）、`lt`（小于）、`neq`（不等于）、`range`（范围）；
- **关键字**：`any` 代表任意地址（0.0.0.0 255.255.255.255）；`host` 代表某台主机（后跟具体 IP，相当于反掩码 0.0.0.0）。

示例：

```
access-list 101 permit ip 192.168.1.0 0.0.0.255 192.168.2.0 0.0.0.255
! 允许 192.168.1.0/24 访问 192.168.2.0/24 的所有 IP 流量

access-list 103 deny tcp 192.168.1.0 0.0.0.255 host 192.168.2.2 eq 21
access-list 103 permit ip any any
! 拒绝 192.168.1.0/24 网段访问主机 192.168.2.2 的 TCP 21 端口（FTP），其余全部放行

access-list 105 deny icmp 192.168.1.0 0.0.0.255 host 192.168.2.2
access-list 105 permit ip any any
! 拒绝 192.168.1.0/24 ping（ICMP）主机 192.168.2.2，其余放行
```

> 扩展 ACL 可以同时检查多个条件（源地址、目的地址、协议、端口等），**多个条件必须同时满足**才算命中该规则。

### 2.4.3 命名 ACL

用字符串代替编号，先创建命名 ACL，再进入其中配置规则：

```
ip access-list standard <名称>
ip access-list extended <名称>
```

进入后按 `[序号] <permit|deny> <条件>` 编写规则（序号决定匹配顺序）。命名扩展 ACL 条件字段与编号版扩展 ACL 相同。命名 ACL 便于阅读管理，实际工作中使用最多。

### 2.4.4 定时 ACL

**第一步：定义时间范围**

```
time-range <名称>
  periodic <星期> <开始时间> to <结束时间>    # 周期时间，如 weekdays 08:30 to 17:30
  absolute <开始时间> to <结束时间>           # 绝对时间（某日某时 到 某日某时）
```

星期取值：Monday / Tuesday / … / Sunday、daily（每天）、weekdays（**工作日，周一至周五**）、weekend（周末）。

**第二步：在 ACL 中引用时间范围**（在普通语法后加 `time-range <名称>`）：

```
access-list 101 permit ip any any time-range mytime
```

再应用到接口的入 / 出方向。效果：只有在 mytime 定义的时间段内（如周一至周五 8:30~17:30）该规则才生效。

### 2.4.5 通配符掩码（反掩码）——重点难点

**通配符掩码**（Wildcard Mask，又叫反掩码 / 反码）写在 IP 地址之后，**规定 IP 地址的哪些位需要精确匹配、哪些位无所谓**：

- 对应位为 **0**：**需要**精确匹配；
- 对应位为 **1**：**不需要**匹配（任意）。

规则：**通配符掩码 = 子网掩码按位取反**。

| 场景 | 掩码 | 反掩码 | 匹配效果 |
|---|---|---|---|
| 匹配一个 /24 网段 | 255.255.255.0 | 0.0.0.255 | 前 24 位精确匹配 |
| 匹配一台主机 | 255.255.255.255 | 0.0.0.0 | 32 位全部精确匹配（等价 host 关键字） |
| 匹配 /16 网段 | 255.255.0.0 | 0.0.255.255 | 如 `192.168.1.1 0.0.255.255` 命中所有 192.168.x.x |
| 匹配所有地址 | — | 255.255.255.255 | 等价 any 关键字 |

换算方法：把子网掩码每段转成二进制逐位取反即可。例如 `/24`：255.255.255.0 → 二进制取反 → 0.0.0.255。

## 2.5 ACL 的应用原则与场景

### 2.5.1 三大基本原则

1. **默认拒绝原则**：一切未被明确允许的流量最终都会被丢弃（隐含 deny any）；
2. **就近原则（应用位置）**：
   - **标准 ACL** 靠近**目标（目的地址）**配置——因为标准 ACL 只看源地址，放太近源会误伤途经的其他流量；
   - **扩展 ACL** 靠近**源地址**配置——条件足够精细，在源头拦截可以节省沿途链路带宽；
3. **顺序原则**：从上到下逐条匹配、命中即止；因此**更精确（更小范围）的规则要放在更上面**。

### 2.5.2 应用到接口（思科语法）

```
interface <接口>
  ip access-group <ACL编号或名称> <in | out>
```

例如 `ip access-group 1 out` 表示在该接口**出方向**应用编号为 1 的 ACL。

华为设备对应命令为 `traffic-filter inbound | outbound acl <编号>`【补充】。

### 2.5.3 ACL 配置完整流程（课程案例综合）

**案例：只允许特定网段远程登录交换机（华为风格）**

```bash
acl number 2000
 rule 5 permit source 211.138.140.128 0.0.0.15      # 只允许该管理网段
 rule 10 deny source any                              # 拒绝其他所有来源

user-interface vty 0 4
 acl 2000 inbound                                      # 在 VTY 入方向应用
```

**案例：标准 ACL 允许指定网段上网（思科风格）**

```
access-list 1 permit 10.0.0.0 0.255.255.255
access-list 1 permit 192.168.0.0 0.0.0.255
access-list 1 permit 192.168.1.0 0.0.0.255
interface f0/0
 ip access-group 1 out
```

**案例：扩展 ACL 只放行 Web / FTP 流量**

```
access-list 100 permit tcp any any eq 80
access-list 100 permit tcp any any eq 20
access-list 100 permit tcp any any eq 21
access-list 100 permit ip any any
interface f0/0
 ip access-group 100 out
```

**案例：禁止内网访问某有害网站 60.54.145.21 的 80 端口**

```
access-list 100 deny tcp 192.168.0.0 0.0.0.255 host 60.54.145.21 eq 80
access-list 100 permit ip any any
interface f0/0
 ip access-group 100 in
```

### 2.5.4 ACL 查看与排错命令（思科）

```
show access-list                     # 查看全部 ACL 规则及命中次数
show access-list <编号>              # 查看指定 ACL
show ip interface <接口名>           # 查看接口上 ACL 应用情况
```

华为对应：`display acl all`。

**常见错误【补充】**：

1. 忘记补 `permit any`，导致全部流量被拒；
2. 精确规则放在宽泛规则之后，永远匹配不到；
3. 反掩码算错（直接把子网掩码当反掩码用）；
4. 应用方向搞反（in / out）；
5. 一个接口同一方向重复应用多组 ACL（不允许，后配置会覆盖或报错）。

---

<a id="p3-4"></a>
# 第三篇　NAT 网络地址转换

## 3.1 为什么需要 NAT

【补充背景】IPv4 公网地址早在 2011 年就分配殆尽，而内网设备数远超公网地址数。解决思路就是：内网使用**私有地址**（见 0.2 节，免费、可重复），出口处用 NAT 把私有地址"翻译"成少量公网地址上网。

**NAT**（Network Address Translation，网络地址转换）：让**多个私有 IP 地址共用一个（或少量）公有 IP 地址**访问互联网的技术，部署在**边界路由器**（内网与公网的交界处）上。

## 3.2 NAT 工作原理

以内网主机访问公网网站为例：

1. 内网主机发出数据包，**源地址是私有 IP**（如 192.168.1.100）；
2. 数据包到达边界路由器（NAT 设备），NAT 将**源地址转换为公网 IP**（如 61.159.62.130），并在**转换表**中记录这条映射关系；
3. 数据包在公网上传输，公网只看到转换后的公网地址；
4. 回程数据包到达路由器，NAT 查询转换表，把**目的地址还原为原来的私有 IP**；
5. 数据包被送回内网原主机。

### NAT 的四种地址术语（考试常考）

| 术语 | 英文 | 含义 |
|---|---|---|
| 内部本地地址 | Inside Local | 转换前内网主机的**私有 IP** |
| 内部全局地址 | Inside Global | 转换后内网主机使用的**公网 IP** |
| 外部本地地址 | Outside Local | 外部主机呈现给内网的地址（通常是 DNS 视角，初学少见） |
| 外部全局地址 | Outside Global | 外部主机真实的**公网 IP** |

### 转换表条目两种形式

- **简单条目**：记录"内部本地 ↔ 内部全局"的一对一映射；
- **扩展条目**：当多个私有 IP 共用一个公网 IP 时，必须再叠加**端口号**来区分不同主机（即 NAPT，见 3.4.3）。

## 3.3 NAT 的优缺点

| 优点 | 缺点 |
|---|---|
| 节省宝贵的公网 IP 地址 | 增加转发延迟 |
| 多台主机共享带宽上网 | 配置和维护更复杂 |
| 支持内外网地址重叠场景 | 增加设备负载（维护转换表、修改报文） |
| 增强组网灵活性 | 部分应用不兼容（报文数据里携带地址的应用） |
| 隐藏内部网络结构，有一定安全性 | 端到端追踪困难 |

**兼容性说明**：HTTP、TCP、TFTP、FTP 等（配合 ALG）工作正常；而路由表更新、DNS 区域传送、SNMP 等点对点需要真实地址的场景不支持或需特殊处理。
**ALG**（Application Level Gateway，应用层网关）【补充】：像 FTP 这类协议会在数据载荷中携带 IP 地址信息，NAT 需要借助 ALG 深入应用层同步改写这些地址，否则数据连接会失败。

## 3.4 NAT 的三种类型与配置

### 3.4.1 静态 NAT（Static NAT）—— 一对一固定映射

- **规则**：一个私有 IP 永远转换为同一个公网 IP，双向可达；
- **适用场景**：**内网服务器需要被公网主动访问**（如 Web、邮件服务器对外发布）。

配置示例（思科，拓扑：内网 192.168.1.0/24，F1/0 为内网口 192.168.1.1，F0/0 为外网口 61.159.62.130）：

```
interface f1/0
 ip nat inside                       # 声明为 NAT 内部接口
interface f0/0
 ip nat outside                      # 声明为 NAT 外部接口
!
ip nat inside source static 192.168.1.2 61.159.62.131   # 私网IP → 公网IP 固定映射
ip nat inside source static 192.168.1.3 61.159.62.132
!
ip route 0.0.0.0 0.0.0.0 61.159.62.129                  # 默认路由指向运营商网关
```

### 3.4.2 动态 NAT（Dynamic NAT）—— 地址池随机分配

- **规则**：从**公网地址池**（Address Pool）中动态取用公网 IP，先到先得，会话结束归还；
- **适用场景**：运营商分配的合法公网 IP 数量**略少于**内网主机数量（不是所有人都同时上网）。

配置步骤（思科）：

```
! ① 定义 ACL，圈定"哪些内网主机可以做 NAT"
access-list 1 permit 192.168.1.0 0.0.0.255
!
! ② 定义公网地址池
ip nat pool mypool 61.159.62.130 61.159.62.190 netmask 255.255.255.192
!
! ③ 把 ACL 关联到地址池
ip nat inside source list 1 pool mypool
!
! ④ 内外接口声明
interface f1/0
 ip nat inside
interface f0/0
 ip nat outside
!
! ⑤ 默认路由
ip route 0.0.0.0 0.0.0.0 <运营商网关>
```

### 3.4.3 NAPT / PAT —— 端口多路复用（最常用）

- **NAPT**（Network Address Port Translation，网络地址端口转换），又称 **PAT**（Port Address Translation，端口地址转换），思科称 **复用 NAT（NAT Overload）**；
- **规则**：**所有内网主机共用一个公网 IP**，靠**不同端口号**区分不同主机 / 会话；
- **适用场景**：只有一个公网 IP（甚至直接复用路由器外网接口 IP）的家用 / 小型企业出口，是目前最常见的上网方式。

配置（在动态 NAT 基础上加 `overload` 关键字）：

```
ip nat inside source list 1 pool mypool overload
```

或直接**复用路由器外网接口的 IP**，连地址池都不用建：

```
ip nat inside source list 1 interface f0/0 overload
```

### 3.4.4 NAT 通用配置五步法（总结）

1. 配置 NAT 设备各接口的 IP 地址；
2. 用 ACL 定义"哪些内部主机允许做 NAT"；
3. 确定转换后的公网地址（静态单地址 / 动态地址池 / 复用接口 IP）；
4. 配置转换映射关系（static / list+pool / overload）；
5. 在内部接口声明 `ip nat inside`、外部接口声明 `ip nat outside`。

## 3.5 NAT 进阶应用场景

### 3.5.1 端口映射（解决"公网 IP 不够 + 保护服务器"）

多个内网服务器共用一个公网 IP，通过**不同公网端口**区分；同时把知名端口映射成非知名端口，可规避对知名端口的扫描攻击：

```
ip nat inside source static tcp 192.168.1.2 80  61.159.62.131 80     # 网站1：80→80
ip nat inside source static tcp 192.168.1.3 80  61.159.62.131 8080   # 网站2：8080→80
ip nat inside source static tcp 192.168.1.8 80  202.101.100.1 1314   # 非知名端口
```

### 3.5.2 双线接入（多运营商出口）

同一内网服务器通过不同运营商线路对外发布时，一个内部地址映射到**多个内部全局地址**，需加 `extendable` 关键字（允许一个内部地址配置多条 NAT 映射）。

### 3.5.3 负载均衡（TCP 负载分担）

外网访问一个"虚拟公网地址"时，路由器按**轮询（Round Robin）**方式把请求分发到多台内部真实服务器，减轻单台压力：

```
ip nat inside destination list <客户ACL> pool <服务器地址池>
```

## 3.6 NAT 维护与故障排查

### 查看与清除命令（思科）

```
show ip nat translations                 # 查看转换表条目
show ip nat statistics                    # 查看 NAT 统计信息
clear ip nat translation *                # 清除所有动态转换条目
debug ip nat                              # 实时跟踪 NAT 转换过程（排错利器）
```

超时时间调整：`ip nat translation <协议> timeout <秒>`（协议可指定 dns / smtp / tcp / udp，`never` 表示永不超时）。

### 常见故障原因（课程总结）

1. ACL 阻挡了转换后的流量 / ACL 配置不全；
2. NAPT 场景忘配 `overload`；
3. 不对称路由（来回路径不一致）；
4. 动态地址池 IP 范围配错，或与静态映射地址重叠；
5. inside / outside 接口角色配反。

### 使用注意事项

1. 关注 NAT 转换表条目数量上限（厂商有规格限制，用 `show ip nat statistics` 观察）；
2. 特殊协议（FTP 等）需开启 **ALG**；
3. 状态化防火墙 + NAT 部署时：若 NAT 地址池与公网接口**不在同一网段**，必须配置**黑洞路由**（指向 NULL0 的静态路由），否则会出现路由环路【补充：黑洞路由把"去往已转换公网地址"的流量引向空接口，避免环路】。

---

<a id="p3-5"></a>
# 第四篇　数据中心网络架构

## 4.1 常见网络拓扑结构【回顾】

总线型（Bus）、星型（Star）、树型（Tree）、环形（Ring）、网状（Mesh / Full-Mesh 全互联）。

## 4.2 传统三层架构（三层 / 树形架构）

数据中心经典的"接入 — 汇聚 — 核心"三层结构：

| 层次 | 名称 | 主要职责 |
|---|---|---|
| 核心层 | Core Layer | 高速交换主干；连接各汇聚层；数据中心与外部网络的**三层边界** |
| 汇聚层 | Aggregation Layer | 提供基于策略的连接；互联各接入层；本汇聚区域的**二层 / 三层边界** |
| 接入层 | Access Layer | 直接连接所有服务器计算节点 |
| 服务器层 | Server Layer | 对客户端提供数据 / 服务 |

**特点与缺陷**：带宽**逐级收敛**（下层总带宽 > 上层），越靠近树根越容易成为**瓶颈**；流量必须逐层穿过多台设备。目前大多数传统机房仍是三层或胖树结构。

## 4.3 胖树架构（Fat-Tree）

- 灵感来自真实树木：**越接近树根，"枝干"越粗（链路带宽越大）**；
- 从叶子到根，**上下行带宽不收敛**（上行带宽 = 下行带宽），从而消除收敛比瓶颈；
- 任意节点都具备对接入流量的限速 / 转接能力；
- 用**大量低成本、同型号交换机**构建大规模**无阻塞**网络；
- 对任意通信模式，总存在路径让端到端带宽达到**网卡带宽**。

## 4.4 叶脊架构（Spine-Leaf）—— 新一代数据中心主流

### 4.4.1 先认识四类流量

| 流量类型 | 英文 | 含义 |
|---|---|---|
| 南北向流量 | North-South | **客户端 ↔ 服务器**之间的流量（用户访问数据中心） |
| 东西向流量 | East-West | **服务器 ↔ 服务器**之间的流量（数据中心内部 / 之间，如集群同步、虚拟机迁移） |
| 大象流 | Elephant Flow | 持续时间长的**大流量**传输（如虚拟机迁移、备份） |
| 老鼠流 | Mouse Flow | 时间短、数据量小的传输（如网页访问请求） |

> 现代数据中心里东西向流量占比远超南北向，这正是叶脊架构流行的原因。

### 4.4.2 架构特点

- **只有两层**：Spine（脊 / 骨干交换机）层 + Leaf（叶 / 接入交换机）层，**任意 Leaf 与任意 Spine 全互联**，网络扁平；
- 服务器接入 **TOR**（Top of Rack，机柜顶部交换机，即叶交换机），一般位于机柜最顶端；
- Leaf 之下是**独立的二层广播域**，服务器间通信经 Leaf→Spine→Leaf 的多条路径**动态选路**，突破传统物理结构限制；
- 链路**全部采用光纤**；
- 数量规律：**Leaf 的下行端口数决定能接多少服务器；Leaf 的上行端口数决定 Spine 的数量**；
- 优点：带宽利用率高、延迟可预测、扩展性好、安全性可用性高；
- 缺点：全部需要**三层交换机**（成本高）、部署运维复杂。目前主要在核心云机房普及。

## 4.5 三种架构对比总结【补充】

| 维度 | 传统三层 | 胖树 | 叶脊 |
|---|---|---|---|
| 层数 | 三层 | 多级 | 两层 |
| 带宽收敛 | 有（根部瓶颈） | 无收敛 | 接近无收敛 |
| 扩展性 | 一般 | 好 | 好（水平扩展 Leaf / Spine） |
| 设备要求 | 常规 | 低成本同型号交换机 | 三层交换机、光纤 |
| 适用 | 传统企业机房 | 大规模集群 | 现代云数据中心 |

---

<a id="p3-6"></a>
# 第五篇　交换机配置与管理

> 本篇以华为 / 华三（H3C）设备命令为主。华为网络设备操作系统为 **VRP**（Versatile Routing Platform，通用路由平台），配置文件名常见为 `VRPCFG.cfg`。

## 5.1 交换机的四种管理方式总览

| 管理方式 | 说明 | 前提条件 |
|---|---|---|
| **Console 本地管理**（控制台管理） | 用 Console 线直连交换机调试，**最基础** | 无前提，新设备开箱第一步 |
| **Telnet 远程管理** | 远程命令行登录 | 需先通过 Console 配置好管理 IP 并开启服务 |
| **SSH 远程管理** | 加密的远程命令行 | 同上，且需生成密钥 |
| **SNMP 网管平台管理** | SNMP（Simple Network Management Protocol，简单网络管理协议），配合网管软件（如 PRTG）批量图形化管理 | 同上 |
| **Web 浏览器管理** | 图形界面，浏览器输入管理 IP 登录 | 同上 |

**结论**：后四种都依赖"先用 Console 完成初始化配置"，所以 **Console 是一切管理的起点**。

## 5.2 Console 本地登录实操

### 5.2.1 硬件准备

- **Console 口**：交换机后面板上标有 **CON / Console** 的 RJ45（Registered Jack 45）接口；
- **Console 线**：一端 RJ45 水晶头插交换机，另一端为 9 针串口（DB9）；现代笔记本没有串口，需要 **USB 转串口线**（转接线）。

### 5.2.2 登录步骤

1. 用 Console 线连接笔记本与交换机；
2. 在 Windows **设备管理器 → 端口（COM 和 LPT）** 中查看新增串口的 **COM 编号**（如 COM3）；
3. 打开终端软件（SecureCRT / Xshell / PuTTY 任一）；
4. 新建会话，协议选 **Serial（串口）**，端口选上一步的 COM 号；
5. 串口参数（**默认值必须牢记**）：

| 参数 | 默认值 |
|---|---|
| 波特率（Baud Rate） | **9600** |
| 数据位（Data Bits） | **8** |
| 停止位（Stop Bits） | **1** |
| 校验位（Parity） | 无 |
| 流控（Flow Control） | 无 |

6. 点击连接后**按回车**，出现设备提示符即登录成功。

## 5.3 命令行基础：视图、帮助与命令级别

### 5.3.1 视图层级（华为 / 华三）

登录后从用户视图逐级进入：

| 视图 | 进入命令 | 用途 |
|---|---|---|
| 用户视图 | 登录即进入，提示符 `<Huawei>` | 查看基本运行状态和统计信息（`display` 类命令） |
| 系统视图 | `system-view`，提示符 `[Huawei]` | 配置全局参数 |
| 以太网接口视图 | `interface gigabitethernet 0/0/1` | 配置具体端口参数 |
| VLAN 视图 | `vlan 10` | 创建 / 配置 VLAN |
| VLAN 接口（VLANIF）视图 | `interface vlan-interface 10`（华为亦写 `interface vlanif 10`） | 配置 VLAN 三层网关 IP |
| 用户界面视图 | `user-interface vty 0 4` | 配置远程登录用户属性 |
| 路由协议视图 | `rip` / `ospf` | 配置路由协议 |

退出操作：`quit` 返回上一级视图；`return`（或 `Ctrl+Z`）从任意视图直接回用户视图。

### 5.3.2 命令行五大好用特性

1. **帮助命令 `?`**：任意位置输入问号，列出可用命令 / 参数；
2. **命令简写**：`system-view` 可简写 `sys`，`interface gigabitethernet 0/0/1` 可简写 `int g0/0/1`（简写到能唯一识别即可）；
3. **错误提示**：输错命令时用 `^` 符号标出错误位置；
4. **历史命令**：↑ / ↓ 方向键调出刚才输过的命令；
5. **Tab 补全**：输入前几个字母按 Tab 自动补全完整命令。

### 5.3.3 命令级别（华为 0~3 级）

| 级别 | 名称 | 典型命令 |
|---|---|---|
| 0 | 访问级 | 网络诊断工具（`ping`、`tracert`、`telnet`） |
| 1 | 监控级 | 系统维护、业务故障诊断（`display`） |
| 2 | 系统级 | 业务配置命令 |
| 3 | 管理级 | 系统基本运行、文件系统、系统支撑模块（`save`、`reboot`） |

### 5.3.4 最常用基础命令

| 命令 | 功能 |
|---|---|
| `display current-configuration` | 查看当前**运行配置**（存在 DRAM，Dynamic Random Access Memory，动态随机存取内存，断电即失） |
| `display saved-configuration` | 查看**已保存配置**（存在 Flash，闪存） |
| `save` | 保存配置（运行配置 → Flash，重启后生效的就是它） |
| `reset saved-configuration` | 清除保存的配置（恢复出厂，见 5.8） |
| `reboot` | 重启设备 |
| `sysname <新名字>` | 修改主机名（思科为 `hostname`） |
| `undo <命令>` | 删除 / 取消某条配置（华为特色，如 `undo shutdown` 打开端口） |
| `dir` | 查看 Flash 目录文件 |
| `delete <文件>` / `undelete` | 删除 / 恢复文件 |
| `copy` / `move` | 复制 / 移动文件 |

> **运行配置 vs 保存配置**：敲的命令先进"运行配置"（在内存里），不 `save` 就重启会全部丢失。这是新手最常踩的坑。

## 5.4 交换机端口配置

### 5.4.1 端口类型与命名

常见端口类型：Console 串口、快速以太网口（FE，Fast Ethernet，100M）、千兆口（GE，Gigabit Ethernet，1000M）、万兆口（10GE）、40G / 100G 口、环回口（Loopback）、MGMT 管理口等。

接口编号格式 `槽位号/子槽号/端口号`，如 `gigabitethernet 1/0/1`。

### 5.4.2 端口基本操作

```bash
system-view
interface gigabitethernet 0/0/1
  description To-Server-A        # 端口描述（备注用途，好习惯！）
  shutdown                        # 关闭端口
  undo shutdown                   # 开启端口
```

### 5.4.3 给交换机配置管理 IP（二层 vs 三层）

- **二层交换机**：物理端口不能配 IP，必须**创建 VLANIF 三层接口**来配管理 IP：
  ```bash
  vlan 10
  interface vlanif 10
   ip address 192.168.10.1 24      # 管理地址
  ```
- **三层交换机（华为 / 华三）**：同样采用 VLANIF 模式，先把端口划入 VLAN，再在 VLANIF 上配 IP 作网关；
- **思科三层交换机**：可以直接在物理接口下 `no switchport` 后配 IP；
- **MGMT 管理口**：独立的带外管理接口，可直接配 IP，支持 Telnet / SSH / SNMP 管理、AAA 认证、NTP 时间同步、日志外发、TFTP 传输等，与业务口分离更安全。

### 5.4.4 双工与速率

```bash
interface gigabitethernet 0/0/1
  duplex full                     # 全双工（还有 half 半双工、auto 自适应）
  undo duplex                     # 恢复默认
  speed 1000                      # 强制速率（可选 10/100/1000/10000M...）
  undo speed                      # 恢复自适应
```

**排错提示**：两端设备速率 / 双工不匹配是常见端口故障（见 5.10）。

### 5.4.5 端口链路类型（VLAN 相关）

【补充 VLAN 概念】**VLAN**（Virtual Local Area Network，虚拟局域网）：在一台物理交换机上逻辑划分出多个互相隔离的广播域，不同 VLAN 二层不通、需三层互通。端口按链路类型分：

```bash
port link-type access     # access 口：接终端，只属于一个 VLAN
port link-type trunk      # trunk 口：接交换机之间，一条线承载多个 VLAN（带 802.1Q 标签）
port link-type hybrid     # hybrid 口：华为 / 华三特有，可灵活指定 tagged / untagged VLAN
undo port link-type       # 恢复默认
```

## 5.5 VLAN 与路由配置汇总

```bash
vlan 10                                # 创建 VLAN 10
undo vlan 10                           # 删除 VLAN 10
interface gigabitethernet 0/0/1
 port link-type access
 port default vlan 10                  # 将端口划入 VLAN 10
interface vlanif 10
 ip address 192.168.10.1 24            # VLAN 10 的网关
display vlan                           # 查看 VLAN 信息
display interface vlanif 10            # 查看 VLANIF 接口状态
```

静态路由与默认路由：

```bash
ip route-static 10.10.2.0 255.255.255.0 10.10.10.1     # 去往 10.10.2.0/24，下一跳 10.10.10.1
ip route-static 0.0.0.0 0.0.0.0 10.10.10.1             # 默认路由
undo ip route-static 10.10.2.0 255.255.255.0 10.10.10.1
display ip routing-table               # 查看路由表
display version                        # 查看系统版本
```

## 5.6 远程登录：Telnet 与 SSH

### 5.6.1 远程登录前置条件

先通过 Console 完成两件事：① 配置管理 IP 并保证网络可达；② 开启远程登录服务并设置认证。

**VTY**（Virtual Teletype Terminal，虚拟终端）：远程登录进来后占用的虚拟用户界面，`user-interface vty 0 4` 表示支持 5 个（0~4）并发远程会话。

### 5.6.2 Telnet 配置（华为）

```bash
system-view
telnet server enable                          # 开启 Telnet 服务
user-interface vty 0 4
 authentication-mode password                  # 方式一：密码认证
 set authentication password cipher Huawei@123  # cipher 密文存储（simple 为明文）
 user privilege level 3                        # 直接给 3 级权限
```

方式二：用户名 + 密码认证（scheme 模式）：

```bash
telnet server enable
user-interface vty 0 4
 authentication-mode scheme
local-user admin password cipher Huawei@123
local-user admin service-type telnet
local-user admin privilege level 3
```

方式三：`authentication-mode none` 免认证（仅限测试环境，生产严禁）。

> 课程实操要点：VTY 登录后默认级别较低（0/1 级），无法配置设备，需要 `user privilege level 3` 或配置 `super password` 提权。

### 5.6.3 SSH 配置（华为）

**SSH**（Secure Shell，安全外壳协议）：所有内容**加密传输**，是生产环境远程登录的首选。

```bash
system-view
rsa local-key-pair create                    # 生成 RSA 密钥对（老版本命令 rsa）
stelnet server enable                         # 开启 SSH 服务（老版本 ssh server enable）
user-interface vty 0 4
 authentication-mode scheme
 protocol inbound ssh                         # 只允许 SSH 进入
local-user admin password cipher Huawei@123
local-user admin service-type ssh
local-user admin privilege level 3
```

**RSA 与 DSA 的区别**：

- **RSA**（Rivest-Shamir-Adleman，三位发明者姓氏首字母）与 **DSA**（Digital Signature Algorithm，数字签名算法）**都是非对称加密算法**；
- DSA 只能用于**签名**，RSA 可用于**签名、加密、解密**，设备管理一般用 RSA。

> **【勘误】** 课程字幕中称"DSA 是对称加密"，此说法有误：DSA 同样是非对称算法，只是功能上仅限签名，实际配置建议直接选 RSA。

### 5.6.4 Telnet vs SSH 对比

| 对比项 | Telnet | SSH |
|---|---|---|
| 传输方式 | **明文** | **加密** |
| TCP 端口 | 23 | 22 |
| 安全性 | 低（抓包即可看到密码） | 高 |
| 密钥 | 不需要 | 需要密钥对 |
| 生产环境 | 禁用 | 推荐 |

## 5.7 用 ACL 限制远程登录来源（访问管理）

```bash
acl number 2000
 rule permit source 211.138.140.128 0.0.0.15    # 只允许管理网段
user-interface vty 0 4
 acl 2000 inbound                                 # 仅允许 ACL 圈定的地址登录
```

## 5.8 交换机初始化（恢复出厂设置）

### 5.8.1 为什么要初始化

旧设备转用需重新配置、配置文件损坏、**忘记登录密码**等。

### 5.8.2 场景一：知道密码，可正常登录

```bash
reset saved-configuration        # 清空保存的配置（系统会二次确认）
reboot                           # 重启
# 询问是否保存当前配置 → 选 N（否则又把旧配置存回去了！）
# 询问是否重启 → Y
```

重启后恢复默认主机名与空配置（如华三设备主机名回到 `H3C`）。

### 5.8.3 场景二：忘记密码，无法登录

1. 重启设备，开机提示出现 `Press Ctrl+B` 时立即按 **Ctrl+B** 进入 **BootROM 启动菜单**；
2. 选择"**跳过当前配置文件启动**"（Skip Current Configuration，不同版本菜单编号不同，课程示例为选项 7）；
3. 选择重启（选项 0）；
4. 重启后设备以空配置启动，重新登录；
5. 执行 `reset saved-configuration` 清掉旧配置文件，`reboot` 后按需重新配置新密码。

## 5.9 交换机版本升级

### 5.9.1 为什么要升级

新版本功能更多、支持的命令更多、修复已知缺陷（类似手机系统更新）。

### 5.9.2 升级前准备

```bash
display version        # 查看当前软件版本、设备型号、运行时间、DRAM/Flash 大小
```

到厂商官网下载**对应型号**的系统软件：华为系统文件后缀 **`.cc`**，华三 / 锐捷为 **`.bin`**。

### 5.9.3 方式一：U 盘升级

1. 准备 **FAT32 格式** U 盘（其他格式设备可能识别不了）；
2. 将升级文件拷入 U 盘；
3. U 盘插入交换机 USB（Universal Serial Bus，通用串行总线）口；
4. Console 登录设备，把 U 盘里的系统文件复制到 `flash:` 根目录；
5. 执行升级命令（如 `boot-loader` 指定下次启动文件）；
6. `reboot` 重启生效。

> Flash 空间不足时，先删除旧系统文件再拷贝。

### 5.9.4 方式二：网线 + FTP 升级

1. 双线连接：Console 线（登录用）+ 网线（笔记本 ↔ 交换机 MGMT 口，传文件用）；
2. 笔记本上安装 FTP（File Transfer Protocol）服务器软件（如文件传输工具 3C Daemon 等），把升级文件放入共享目录；
3. 交换机管理口与笔记本配**同网段 IP**（课程示例：笔记本 192.168.10.20，交换机 10.1.0.20，改为同段）；
4. 在交换机上 `ftp <笔记本IP>` 登录，`get <文件名>` 下载到 flash；
5. 指定启动文件并重启。

## 5.10 交换机常见维护命令大全

### 5.10.1 硬件信息类

| 查看内容 | 命令 |
|---|---|
| 设备 / 部件信息、SN、MAC、出厂日期 | `display device` |
| 设备制造信息 | `display device manufacture-info` |
| 软件版本 | `display version` |
| 电源状态 | `display power` |
| 整机功耗 | `display power system` |
| 温度 | `display temperature` |
| 风扇状态 | `display fan` |
| CPU 占用率 | `display cpu-usage` |
| 内存占用 | `display memory` |
| 接口状态 | `display interface` / `display interface brief` |
| 接口 IP 简况 | `display ip interface brief` |
| 日志缓冲区 | `display logbuffer` |
| 系统时间 | `display clock` |

**实操示例解读**：`display power` 显示"电源 1 交流正常、电源 2 直流 Absent（掉线 / 未加电）"；`display cpu-usage` 显示 5 秒 / 1 分钟 / 5 分钟负载（如 23% / 22% / 27%）；`display memory` 显示总内存 27M、已用 11M、使用率 41%。

**版本信息解读**：型号 `S5100-48-EI` 中 **EI = Enhanced（增强版）**、SI = Standard（标准 / 简化版）；【补充】另有 HI 等。

## 5.11 数据中心网络故障排查

### 5.11.1 两大基础命令

```
ping 1.1.2.20              # 测试端到端连通性
tracert 1.1.2.20           # 跟踪每一跳路径，定位断点在哪一跳
tracert -h <最大跳数> <IP>  # 限制跟踪跳数
```

ping 结果解读：有回包时间 = 通；`Request timed out`（请求超时）= 目标不存在 / 关机 / 路由不可达 / 中途被丢；`Destination Host Unreachable`（目标主机不可达）= 路径上无路由。

### 5.11.2 分层排查思路（先物理后配置）

**物理层常见故障：**

| 故障点 | 具体表现 |
|---|---|
| 线缆 | 网线 / 光纤没插紧、线缆老化损坏、水晶头氧化、光纤弯折导致**光衰过大** |
| 端口 | 接口损坏、端口被 `shutdown`、两端**速率 / 双工不匹配** |
| 光模块 | 光模块损坏、速率不匹配（如 10G 口插 40G 模块） |
| 环路 / 攻击 | 网络环路导致广播风暴、遭受攻击 |

**排查流程（课程总结）：**

1. `ping` 测连通性 → `tracert` 定位断点跳数；
2. 检查物理层：线缆、光模块、端口指示灯与状态（`display interface`）；
3. 检查配置：端口是否被关闭、速率双工是否匹配、有无环路；
4. **替换法**：换线、换模块、换端口逐一验证，快速隔离坏件。

---

<a id="p3-7"></a>
# 第六篇　服务器硬件：基本操作、BIOS、BMC 带外管理、RAID

## 6.1 服务器基本操作（机房现场必会）

### 6.1.1 供电与加断电

- 服务器一般配 **双电源模块（PSU，Power Supply Unit，电源供应单元）**；
- 机柜配电由 **PDU**（Power Distribution Unit，电源分配单元）提供，通常分 **A 路 / B 路**两路市电；
- **正确接法**：两个电源分别接 PDU 的 A 路和 B 路（冗余供电，任一路断电服务器仍能运行）；错误接法是两个电源都插同一路。
- **断电操作**：在机柜内优先拔**服务器那一端**的电源线接头，不要直接拔 PDU 端（避免误碰其他设备）。

### 6.1.2 上架前测电

测电 = 上架前验证服务器能否正常加电。步骤：

1. 到指定测电区（普通市电，非机房专用供电）；
2. 两个电源都插上，加电约 5 秒无异常；
3. 断开 A 路电源，让 B 路单独运行 5 秒；
4. 插回 A 路，拔掉 B 路，让 A 路单独运行 5 秒；
5. 均正常则测电通过（数量多时可抽样测电，视机房要求）。

### 6.1.3 面板按钮与指示灯

- **UID 灯**（Unit Identification，单元定位灯）：蓝色，前后面板都有，用于在众多机柜中**定位某台服务器的物理位置**，按任意一端另一端同步亮；
- **电源 / 开关机键**：琥珀色 = 待机，绿色 = 运行；
  - 开机：轻按一下；
  - 关机：**长按 3~6 秒**；
  - 有的机型加电即自动开机，有的需要手动按（约等 10 秒后观察指示灯颜色判断）。

### 6.1.4 关机与重启方式汇总

| 类型 | 方式 | 适用 |
|---|---|---|
| 软关机 / 重启 | 操作系统命令（Windows 菜单关机；Linux `shutdown` / `reboot`） | 正常情况首选 |
| 软重启 | 组合键 `Ctrl + Alt + Delete` | 系统级重启 |
| 硬关机 / 硬重启 | 长按开机键 | 系统卡死时 |
| 最后手段 | 拔双路电源 | 仅特殊情况，硬件状态会被重置 |

### 6.1.5 开机自检与快捷键提示

服务器开机后先跑 **POST**（Power-On Self-Test，加电自检），随后屏幕会短暂显示各功能入口按键（**提示稍纵即逝，要快速按**）：

| 按键 | 功能 |
|---|---|
| `Del` / `F1` / `F2` / `F9` / `F10` | 进入 BIOS（不同厂商不同） |
| `F11` | 启动项选择菜单（Boot Menu，U 盘安装系统用） |
| `F12` | PXE 网络启动 |
| `Ctrl+C` / `Ctrl+H` / `Ctrl+R` / `Ctrl+M` / `Ctrl+S` | 进入 RAID 卡配置界面（不同 RAID 卡不同） |

### 6.1.6 服务器标签识别

机身标签包含：厂商、型号（如华为 RH2288HV2）、配置、出厂日期、**SN**（Serial Number，序列号）、资产编号等；可用扫码枪扫描二维码做资产统计。

## 6.2 BIOS

### 6.2.1 什么是 BIOS

**BIOS**（Basic Input/Output System，基本输入输出系统）：固化在主板 **ROM**（Read-Only Memory，只读存储器）芯片上的一组程序，保存着计算机最重要的**基本输入输出程序、开机自检程序和系统自启动程序**，为计算机提供最底层的硬件设置与控制。

相关芯片分工：

| 芯片 | 作用 |
|---|---|
| ROM 芯片 | 存放 BIOS **程序本身**（只读，断电不丢） |
| CMOS 芯片 | 存放 BIOS 的**设置参数**（如启动顺序、时间），由纽扣电池供电【补充：CMOS = Complementary Metal-Oxide-Semiconductor，互补金属氧化物半导体】 |

> 个人电脑开机时的进度条画面，就是 BIOS 在做 POST 自检。

### 6.2.2 BIOS vs UEFI【重点对比】

**UEFI**（Unified Extensible Firmware Interface，统一可扩展固件接口）是 BIOS 的替代者：

| 对比项 | BIOS（传统） | UEFI |
|---|---|---|
| 界面 | 文本界面，键盘操作 | 图形界面，支持鼠标 |
| 启动速度 | 慢 | 快 |
| 安全性 | 弱 | 有安全启动机制，抵御预引导攻击 |
| 大容量硬盘 | 启动盘分区限制约 2TB | 支持超过 2TB（配合 GPT 分区表【补充】） |
| 驱动 | 传统 16 位模式 | 支持现代 64 位设备驱动 |
| 现状 | 服务器领域仍大量在用 | 正逐步替代 BIOS |

常见 BIOS 厂商：Award、AMI（American Megatrends Inc.）、Phoenix；进入 BIOS 界面后顶部显示厂商名。

### 6.2.3 各厂商进入 BIOS 按键

| 厂商 | 按键 |
|---|---|
| 华为 | `Del` |
| 浪潮 | `Del` |
| 华三 | `Del` 或 `F9` |
| 惠普（HP） | `F10` |
| IBM | `F1` |
| 戴尔（Dell） | `F2` |

### 6.2.4 BIOS 界面五大页面（以华为 RH2288HV 为例）

| 页面 | 功能 | 主要内容 |
|---|---|---|
| Main（主页面） | 查看基本信息 | BIOS 版本、出厂日期、CPU 型号 / 主频 / 数量、内存容量 / 频率、系统时间日期 → 可据此判断 CPU / 内存好坏 |
| Advanced（高级） | 系统参数与功能控制 | CPU 超频、内存技术、远程管理（IPMI / BMC 配置）、硬盘模式、USB、串口、PXE 等 |
| Security（安全） | 安全控制 | TPM（Trusted Platform Module，可信平台模块）、开机密码、管理员密码、用户权限 |
| Boot（启动项） | 启动控制 | 启动方式、启动顺序（硬盘 / 光驱 / U 盘 / 网卡）、快速启动 |
| Exit（退出） | 保存退出 | 保存并退出、不保存退出、恢复默认配置 |

**通用操作按键**：↑↓ 选择、←→ 切换页面、Enter 进入、F1 帮助、F9 恢复默认、F6 修改值、F10 保存退出（有的机型为 F4）、ESC 退出。

### 6.2.5 高级页面常用设置（实操要点）

- **CPU 配置**：查看 CPU1 / CPU2 信息与一 / 二 / 三级缓存；电源策略（节能 / 性能 / 自定义）；**HT**（Hyper-Threading，超线程）开关——让一个物理核心模拟两个逻辑核心提升性能；
- **虚拟化技术**（Virtualization Technology）：安装虚拟机 / 虚拟化平台（如 VMware）前必须设为 **Enable**；
- **NUMA**（Non-Uniform Memory Access，非统一内存访问）：多 CPU 架构下的内存访问优化；
- **硬盘模式**：AHCI（Advanced Host Controller Interface，高级主机控制器接口，默认）/ IDE（Integrated Drive Electronics，传统兼容模式）/ RAID；
- **IPMI / BMC 配置**：设置带外管理 IP（见 6.3）；
- **PXE 配置**：网卡启动相关；
- **内存信息**：查看各 CPU 对应内存条与插槽（示例：16GB×2、共 12 插槽 / 24 通道）。

## 6.3 BMC 带外管理

### 6.3.1 核心概念

- **BMC**（Baseboard Management Controller，基板管理控制器）：焊在服务器主板上的**独立管理芯片**，是服务器"带外管理"的大脑。
- **IPMI**（Intelligent Platform Management Interface，智能平台管理接口）：BMC 遵循的标准接口规范，提供本地 / 远程诊断、配置管理、硬件监控与故障排除能力。
- **带外管理**（Out-of-Band Management）：**不依赖服务器操作系统和业务网**，通过独立的**管理网络**访问服务器的管理方式。与之相对的是**带内管理**（In-Band，走业务网 / OS，系统宕机就失联）。
- **BMC 的独立性**：不依赖服务器 CPU、内存、BIOS、操作系统——**服务器关机（只要通电）也能远程控制**，这是服务器区别于普通 PC 的重要能力。

**带外管理能做什么**：远程开关机 / 重启、远程装系统（挂载虚拟光驱）、查看硬件状态与传感器（温度 / 风扇 / 电压）、查看日志、远程 KVM（Keyboard-Video-Mouse，键盘显示器鼠标，即远程控制台，把服务器画面拉到自己电脑上操作）。

### 6.3.2 管理网口与各厂商叫法

带外管理口是独立于业务网口的 RJ45 网口（不与业务口挤在一起）：

| 厂商 | 带外管理技术名称 |
|---|---|
| 华为 | BMC / iBMC（intelligent Baseboard Management Controller） |
| 惠普 | iLO（Integrated Lights-Out） |
| 戴尔 | iDRAC（Integrated Dell Remote Access Controller）【补充】 |
| IBM / 联想 | IMM（Integrated Management Module） |
| 华三 | HDM（H3C Device Management） |
| 通用叫法 | IPMI 口、MGMT（Management）口 |

### 6.3.3 端口（网口）模式

| 模式 | 说明 |
|---|---|
| 专用模式（Dedicated） | 独立管理口单独插网线，仅做管理（推荐） |
| 共享模式（Shared） | 借用某个业务网口同时做管理（管理口不插线时的选择） |
| 自适应模式 | 自动识别切换 |

### 6.3.4 配置带外管理地址的四种方法

1. **BIOS 中配置**（首次使用，接显示器键盘进 BIOS，见 6.2.5）；
2. **出厂默认地址**（部分服务器自带）；
3. **Web 页面里修改**；
4. **系统内命令**：`ipmitool lan set 1 ipaddr <IP>`（ipmitool 为 IPMI 命令行工具）。

配置要素：IP 地址、子网掩码、网关；管理机与之网络可达（能 ping 通）即可，地址由管理员统一分配（私网 / 公网均可）。

### 6.3.5 各厂商 BIOS 中配置带外地址路径（课程案例）

| 厂商 | 进入方式 | 路径 |
|---|---|---|
| 华为 | 开机 `Del` | Server Management（服务器管理）→ BMC 网络配置 → 手动配置 IP / 掩码 / 网关 → `F4` 保存 |
| IBM | 开机 `F1` | System Settings → IMM → 网络配置 → 选端口模式 + 静态 IP → 保存 |
| 惠普 | 开机按提示，`F8` 进 iLO | Network 选项 → 配置地址 → `F10` 保存 |
| 华三 | 开机 `F9` 或 `Del` | 系统配置 → BMC 配置 → 网络配置 → 静态 IP → 保存 |

### 6.3.6 实操流程（华为 RH2288HV 示例）

实验环境：A01 机柜 1 号，管理 IP 192.168.3.101 / 24，网关 192.168.3.1。

1. 用 **KVM**（Keyboard, Video, Mouse，机房共享键盘鼠标显示器切换器）连接服务器：插好 VGA（Video Graphics Array，视频接口）与 USB 线，用 KVM 面板按键切换到目标服务器；
2. 开机按 `Del` 进 BIOS → Advanced（高级）→ **IPMI / BMC Configuration**；
3. 确认 IPMI 已启用；
4. 设置 BMC 登录用户名 / 密码（供浏览器 Web 登录用）；
5. 选择端口模式（**Dedicated 专用模式**：管理口单独插网线；Shared 共享模式：与业务口共用）；
6. IPv4 地址模式由 DHCP 改为 **Static（静态）**，填写 IP / 掩码 / 网关（IPv6 可不配）；
7. `F10` 保存退出。

**验证**：笔记本与带外口直连 / 同网段，浏览器输入 `https://<带外IP>`，出现登录页即成功；开机画面上也会显示带外管理地址。

### 6.3.7 带 Web 管理界面功能总览

登录后常见模块：总体概况（设备 / CPU / 内存 / 硬盘 / 电源 / 风扇健康状态）、操作控制（电源开关机 / 重启 / 强制下电 / 安全下电）、远程控制台（KVM）、固件升级、网络配置、用户管理、事件日志（含开机自检代码、屏幕截图）、实时监控（温度 / 转速 / 功耗）、通电开机策略（加电后"保持关机 / 自动开机"）、服务开关（SSH / IPMI / Web 端口）等。

**远程控制台实操要点（课程环境）**：

- 推荐环境：Firefox 浏览器 + Java 运行环境（版本不宜过高，过高可能弹不出虚拟控制台）；
- 点击"远程控制"→ 选择独占（专用）或共享模式 → Java 提示信任风险时接受；
- 弹不出界面时：Java 控制面板 → 安全 → 调低安全级别，并把带外管理地址加入例外站点列表，重启浏览器再试；
- 进入虚拟控制台后，**鼠标必须点进控制台画面内**才能操作服务器；按特定组合键（如 `Ctrl+Alt+Delete`）可软重启，用另一组合把鼠标释放回本地；
- **服务器关机 / 强制下电后，只要还通着电，带外管理界面仍然可用**——可远程重新开机。

## 6.4 RAID 独立冗余磁盘阵列

### 6.4.1 什么是 RAID

**RAID**（Redundant Array of Independent Disks，独立冗余磁盘阵列）：把多块独立硬盘组合成一个容量巨大的磁盘组，同时实现两大目标：

1. **数据组织**（提升读写性能）；
2. **数据保护**（提升可靠性）。

**实现方式**：**硬 RAID**（独立 RAID 卡，主流方式）与**软 RAID**（操作系统软件实现）。常见 RAID 卡：LSI SAS 2308 / 9361 系列、Adaptec、Broadcom（几家厂商已并购整合）。

### 6.4.2 RAID 四大基本原理

| 原理 | 说明 |
|---|---|
| **条带化**（Striping） | 把一份数据切分成小块分别存到不同硬盘，多盘**并行读写**，减少机械寻道时间，提升性能 |
| **镜像**（Mirroring） | 同一份数据完整写入多块硬盘，互为备份 |
| **校验**（Parity） | 用**异或运算 XOR**（Exclusive OR）算出校验值存盘，坏盘后用剩余数据 + 校验值反推还原 |
| **热备盘**（Hot Spare） | 平时空闲的"备胎盘"，RAID 组内硬盘故障时**自动顶替**（逻辑顶替，非物理换盘） |

**XOR 运算速记**：相同为 0，相异为 1（0^0=0，1^1=0，0^1=1，1^0=1）。

恢复示例【补充】：数据盘 D1=1、D2=0、D3=1，校验 P = 1^0^1 = 0。若 D2 损坏，则 D2 = D1^D3^P = 1^1^0 = 0，数据被完整还原。

**相关术语**：

- **分块（Chunk）**：把每块硬盘划分出的等大、地址连续的存储单元，是条带的组成元素；
- **条带（Stripe）**：不同硬盘上**相同位置**的分块合起来构成一个条带；写入每块盘的条带数据块大小称为**条带深度**；
- **重构（Rebuild）**：坏盘被顶替后，RAID 用 XOR 重新算回数据写入新盘；换上新盘后数据迁移过去，热备盘回到备胎状态。数据量大时重构时间较长；
- **热插拔**（Hot Swap）【补充】：不断电直接拔插硬盘。

### 6.4.3 RAID 组的五种状态

**创建 → 正常工作 → 降级 → 重构 →（失效）**：

| 状态 | 含义 |
|---|---|
| 创建 | 硬盘加入组成 RAID 组 |
| 正常工作 | 一切正常 |
| 降级（Degraded） | 组内某块盘故障 / 掉线，性能与冗余下降 |
| 重构（Rebuild） | 热备盘顶替 / 更换新盘后正在恢复数据 |
| 失效（Failed） | 坏盘未及时更换、又坏第二块（超出冗余能力），整个 RAID 组数据不可用 |

### 6.4.4 常见 RAID 级别对比（重点必背）

| 级别 | 最少盘数 | 容量利用率 | 性能 | 允许坏盘数 | 特点与适用 |
|---|---|---|---|---|---|
| **RAID 0**（条带卷） | 2 | **100%** | 读写都快 | **0** | 无冗余，坏一块全丢；图形工作站等重速度轻安全场景 |
| **RAID 1**（镜像卷） | 2 | **50%** | 写较慢，读可单盘 | 1 | 两盘互为备份；数据库等重要数据 |
| **RAID 3** | 3 | (N-1)/N | 读好写差 | 1 | 专用校验盘（最后一块盘专存校验） |
| **RAID 5**（分布式校验） | 3 | (N-1)/N | 读高写一般 | 1 | 校验值分布各盘，综合均衡，最常用级别之一 |
| **RAID 6** | 4 | (N-2)/N | 读高写一般 | **2** | 双校验 P+Q，更安全但成本高 |
| **RAID 10**（RAID 1+0） | 4 | 50% | 较高 | 每组镜像可坏 1 块（先镜像后条带） | 速度 + 安全兼顾，银行金融领域常用 |
| RAID 01（RAID 0+1） | 4 | 50% | 较低 | 1 | 先条带后镜像，可靠性不如 RAID 10 |
| **RAID 50**（RAID 5+0） | 6 | 视分组而定 | 读高写低 | 每组可坏 1 块 | 先做 RAID 5 再条带，兼顾容量与安全 |

**记忆技巧**：数字命名"先做前面的、再做后面的"——RAID 10 = 先 RAID 1（镜像）再 RAID 0（条带）；RAID 50 = 先 RAID 5 再 RAID 0。

**补充说明**：

- RAID 0 是唯一**没有数据保护能力**的级别；
- 做 RAID 的硬盘应使用**同型号、同容量、同转速**的盘，避免短板效应；
- 同一台服务器多块盘可划分多个相同或不同级别的 RAID 组；
- 热备盘分**全局热备**（服务所有 RAID 组）与**局部热备**（只服务指定组）。

### 6.4.5 RAID 配置实操（LSI SAS 2308 卡）

**进入方式**：开机看屏幕提示，不同 RAID 卡快捷键不同：

| 快捷键 | 对应 |
|---|---|
| `Ctrl+C` | LSI SAS 2308 配置界面 |
| `Ctrl+H` | WebBIOS 模式 |
| `Ctrl+M` | Adaptec 卡 |
| `Ctrl+R` | 部分戴尔 PERC 卡【补充】 |
| `Ctrl+S` | Software RAID（软 RAID）模式 |

**操作流程（通过 BMC 远程控制台完成）**：

1. 带外管理进入远程虚拟控制台（独占模式），对服务器开机；
2. 鼠标点进虚拟控制台画面；
3. 开机提示 `Press Ctrl-C to enter Configuration Utility` 时按 **Ctrl+C** 进入；
4. 选择目标 RAID 控制器（界面显示 LSI SAS2308）；
5. 进入 RAID 属性 / 创建阵列（Create Array）；
6. 光标移到 Disk 列，**按空格键选中**要加入的硬盘（"N" 变 "Y"）；
7. 选择 RAID 级别（如 RAID 0）；
8. 按 **C** 创建并保存（Save）；
9. 退出配置界面并重启，系统安装时即可看到整块的逻辑盘。

**界面通用按键**：ESC 返回 / F1 帮助 / `+/-` 调整顺序 / Insert-Delete 管理启动列表。

> 提示：如果卡在 DHCP / PXE 引导界面，多半是服务器没有可启动系统，`Ctrl+Alt+Delete` 软重启再进 RAID 或 BIOS 设置启动项。

---

<a id="p3-8"></a>
# 第七篇　Linux 系统管理

## 7.1 Linux 系统概述

### 7.1.1 起源与发展

- 1991 年 10 月 5 日由 **Linus Torvalds（林纳斯·托瓦兹）** 发布内核；吉祥物是**企鹅**；
- Linux 是**类 Unix** 操作系统，遵循 **POSIX**（Portable Operating System Interface，可移植操作系统接口）标准，免费使用、自由传播。

### 7.1.2 常见发行版

Red Hat、CentOS【补充】、openSUSE、SUSE（企业版 SLES，SUSE Linux Enterprise Server）、Ubuntu、Debian、红旗等。
【补充说明】openSUSE 是社区免费发行版，SUSE Linux Enterprise 是付费企业版；课程以 openSUSE 为教学环境，机房也有 SLES、Ubuntu 等多种系统。

### 7.1.3 Linux 的核心特点

1. **一切皆文件**：CPU、内存、硬盘、U 盘等硬件设备都以文件形式存在（在 `/dev` 目录下）；
2. 内核完全免费；
3. 兼容 POSIX 标准（一个符合标准的程序可方便地移植到其他系统编译运行）；
4. **多用户多任务**：多个用户同时登录执行不同任务；
5. 既有图形界面也有命令行，**运维工程师主要用命令行**；
6. 支持多平台（PC、服务器、手机、嵌入式等）；
7. **严格区分大小写**（命令和文件名都是，新手高频错误点！）。

### 7.1.4 两种安装方式

| 场景 | 方式 |
|---|---|
| 少量服务器 | U 盘安装 |
| 批量（几十~上百台） | PXE 网络批量无人值守安装 |

## 7.2 制作 Linux 安装 U 盘

**工具**：软碟通（UltraISO，ISO = ISO 9660 光盘镜像格式）；**U 盘**：建议 8GB 以上（Linux DVD 镜像常超过 4GB）。

**镜像下载渠道**：发行版官网（如 www.opensuse.org）、网易 / 搜狐等开源镜像站。镜像版本区别：

| 版本 | 说明 |
|---|---|
| DVD | 标准服务器安装版（约 4GB+） |
| Live DVD / 桌面体验版（GNOME / KDE） | 自带图形桌面可直接体验（GNOME = GNU Network Object Model Environment；KDE = K Desktop Environment，两大桌面环境【补充】） |
| Minimal | 最小化安装，无图形界面 |
| 网络安装版 | 主体从网络下载 |

**制作步骤**：

1. 插入 U 盘，**以管理员身份**运行软碟通（否则可能无写入权限）；
2. 文件 → 打开 → 选择 `.iso` 镜像；
3. 启动 → **写入硬盘映像**；
4. 确认**选中自己的 U 盘**（选错会毁掉别的盘的数据！）；写入方式等保持默认；
5. 点击"写入"，确认警告（**会格式化 U 盘，先备份资料**）；
6. 等待写入完成（日志进度走完即成功），关闭软件拔出 U 盘。

## 7.3 U 盘安装 Linux 全流程

### 7.3.1 安装步骤

1. 开机按 **F11**（各机型不同，看屏幕提示）进入启动项菜单，选择 U 盘启动；
2. 出现系统安装菜单：
   - **Install openSUSE**：正常安装（最常用）；
   - Test Media and Install：先校验镜像完整性再装（较耗时）；
   - Troubleshooting：排错模式；
3. 按向导配置：
   - 语言（如中文）、时区（Asia/Shanghai）、键盘；
   - 安装源：本地介质；
   - 软件选择：**默认最小化安装无图形界面**；需要桌面则选带 GNOME / KDE 的方案；
   - 安装位置：选择 RAID 逻辑盘（如课程示例 134G 的 RAID 组）；**选手动分区**；
4. 手动分区（标准分区方案）：
   - **swap 交换分区**：建议为**物理内存的 1.5~2 倍**（课程示例 12G）；
   - **根分区 `/`**：课程示例 100G；
   - 文件系统默认 **ext4**（fourth extended filesystem，第 4 代扩展文件系统）；
5. Kdump（Kernel Crash Dump，内核崩溃转储机制，默认即可）；
6. 设置 **root 密码**（root = 超级管理员账号）；
7. 开始安装 → 完成后重启 → 接受许可协议 → 初始化配置（语言 / 键盘 / 隐私）→ 创建普通用户 → 进入系统。

### 7.3.2 常见安装故障处理（课程案例）

**故障**：安装界面卡住反复弹出"警告：进程超时"。
**原因**：安装程序通过 U 盘标签找不到安装介质。
**解决**：在启动菜单选中 Install 项后按 **Tab** 编辑启动参数，把 `hd:` 后的内容改为明确的设备路径，如 `hd:/dev/sda4`（U 盘实际是第几块盘第几个分区就写什么，如 sdb4、sdc4）。

### 7.3.3 如何找到 U 盘的设备名

Linux"一切皆文件"，磁盘设备都在 `/dev` 目录：

| 设备名 | 含义 |
|---|---|
| `/dev/sda` | 第一块 SCSI/SATA/USB 磁盘 |
| `/dev/sdb`、`/dev/sdc` | 第二、第三块盘，依此类推 |
| `/dev/sda4` | 第一块盘的第 4 个**分区** |

判断技巧：U 盘通常只有一个分区（如 sda4），而未分区的整盘（如 sdb）不会是 U 盘的安装分区。可在系统中 `cd /dev && ls` 查看。

## 7.4 PXE 网络批量安装

**PXE**（Preboot eXecution Environment，预启动执行环境）：网卡芯片自带的**网络启动**能力，配合服务器实现批量、无人值守自动化装机。

### 7.4.1 架构角色

- **母机（安装服务器）**：先用 U 盘装好 Linux，再搭建 **DHCP**（Dynamic Host Configuration Protocol，动态主机配置协议）、**TFTP**（Trivial File Transfer Protocol，简单文件传输协议）、**HTTP**（HyperText Transfer Protocol，超文本传输协议）及 kickstart 服务；
- **客户端（小机）**：待装系统的服务器，开机按 **F12** 选择 PXE 引导。

### 7.4.2 PXE 工作流程（完整链路）

1. 客户端网卡以 PXE 方式启动，**广播 DHCP 请求**索要 IP；
2. DHCP 服务器校验合法性，**分配 IP** 并同时告知**启动文件 `pxelinux.0` 的位置**；
3. 客户端向 **TFTP** 服务器请求下载 `pxelinux.0`（TFTP 先发文件大小试探，客户端同意后正式传输）；
4. 客户端执行 `pxelinux.0`（一段引导程序）；
5. 客户端再向 TFTP 请求**配置文件**（位于 `pxelinux.cfg` 目录）；
6. 依据配置文件向 TFTP 请求 **Linux 内核**（vmlinuz 及初始化镜像）；
7. 内核启动后，通过 **HTTP/NFS** 下载完整安装镜像；
8. 读取 **kickstart 自动应答脚本**（预设分区、软件包、IP、主机名等），全自动完成安装并自动重启进入登录界面。

### 7.4.3 批量装机的异常处理

- 卡在某步不动 → 拍照记录，反馈需求方或二线工程师；
- 母机自建环境排错顺序：**防火墙是否放行**、**SELinux**（Security-Enhanced Linux，安全增强型 Linux，安全模块，常需关闭 / 放行）、DHCP / TFTP / HTTP 服务是否启动、配置文件是否写错；
- 重新安装：小机重启再按 F12 重新进入 PXE 流程即可。

## 7.5 服务器硬件信息查看（Linux 命令）

| 查看目标 | 命令 | 说明 |
|---|---|---|
| CPU 型号 / 数量 / 核心 / 主频 / 架构 | `lscpu` | 最常用 |
| CPU 详细信息 | `cat /proc/cpuinfo` | 每 CPU 一段（CPU0、CPU1…） |
| 服务器厂商 / 型号 / SN / 主板 | `dmidecode`（需 root） | 读固件中的硬件信息，可配合 `grep` 过滤 |
| BIOS 信息 | `dmidecode -t bios` | 制造商 / 版本 / 发布日期 |
| 内存硬件（插槽数 / 最大支持容量） | `dmidecode -t memory` | 空闲插槽也能看出 |
| 内存使用情况 | `free -m`（MB）/ `free -h`（人类可读单位） | total / used / free / swap |
| 磁盘与分区结构 | `lsblk` | 树状列出硬盘与分区（做 RAID 后多块盘显示为一个逻辑盘，如 sda） |
| 磁盘与分区详情（含文件系统） | `lsblk -f` | 挂载点、文件系统类型 |
| 分区容量与使用率 | `df -h` | 已挂载分区的容量 / 已用 / 可用 / 挂载点 |

**课程示例**：32G 内存的服务器，`free -m` 显示 total 约 31G、used 约 900M；`lsblk` 显示 `sda`（RAID 逻辑盘）下有 `sda1`（swap 12G）与 `sda2`（根分区 100G）；`df -h` 显示 sda2 挂载在 `/`，已用 12G。

## 7.6 Linux 文件管理基础

### 7.6.1 目录结构（FHS）

Linux **没有 C 盘 D 盘的概念**，一切从**根目录 `/`** 开始，整棵目录树遵循 **FHS**（Filesystem Hierarchy Standard，文件系统层次结构标准）：

| 目录 | 用途 | 重要程度 |
|---|---|---|
| `/` | 根目录，一切目录的起点 | ★★★ |
| `/boot` | 内核与引导配置文件 | ★★★ |
| `/dev` | **设备文件**（一切皆文件：硬盘 sda、CPU 信息等） | ★★★ |
| `/etc` | **各种服务的配置文件**（改配置基本都在这） | ★★★ |
| `/home` | 普通用户的家目录（`/home/tom`） | ★★★ |
| `/root` | 超级管理员 root 的家目录 | ★★★ |
| `/proc` | 虚拟文件系统，内核与进程信息（如 `/proc/cpuinfo`） | ★★ |
| `/bin` | 基本用户命令（`ls`、`cp`…）【补充】 | ★★ |
| `/sbin` | 系统管理命令（root 用）【补充】 | ★★ |
| `/var` | 经常变化的数据：日志（`/var/log/messages`）、邮件 | ★★★ |
| `/tmp` | 临时文件 | ★★ |
| `/usr` | 应用程序与资源（`/usr/bin`、`/usr/local`）【补充】 | ★★ |
| `/mnt`、`/media` | 挂载点（U 盘、光盘等临时挂载） | ★ |

### 7.6.2 文件命名规则

1. 除 `/` 以外所有字符均合法（但空格、`*`、`?` 等特殊字符**不建议**用）；
2. 避免 `.` 开头（那代表**隐藏文件**）；
3. **严格区分大小写**（`A.txt` 与 `a.txt` 是两个文件）；
4. 后缀名**没有实际功能意义**（Linux 靠文件属性识别类型），但可自定义便于识别（`.txt`、`.conf`）；
5. 同一目录下文件与目录**不能重名**（目录也是文件）；
6. 三个特殊符号：`.` 当前目录、`..` 上一级目录、`~` 当前用户家目录。

### 7.6.3 文件类型与颜色

`ls` 时终端用颜色区分类型：**黑色=普通文件，蓝色=目录，绿色=可执行文件，红色=压缩包，黄色=设备文件，浅蓝色=链接文件**。

【补充】用 `ls -l` 第一列字符判断类型：`-` 普通文件、`d` 目录、`l` 链接、`b/c` 块 / 字符设备。

### 7.6.4 绝对路径与相对路径

- **绝对路径**：从 `/` 开始写全，如 `/etc/passwd`，任何位置都有效；
- **相对路径**：从当前目录出发，如 `./hubei/wuhan` 或 `../etc`。

对比：`mkdir -p ./hubei/wuhan`（在当前目录下创建）vs `mkdir -p /hubei/wuhan`（在根目录下创建）。

## 7.7 文件与目录常用命令详解

### 7.7.1 目录操作命令

| 命令 | 功能 | 关键参数 / 示例 |
|---|---|---|
| `pwd` | 显示当前所在目录绝对路径 | — |
| `ls` | 列出目录内容 | `-a` 含隐藏文件（`.` 开头）；`-l` 详细信息（简写 `ll`） |
| `cd` | 切换目录 | `cd /etc`；`cd ..` 上一级；`cd ~` 回家目录；`cd -` 回上一次目录【补充】 |
| `mkdir` | 创建目录 | `-p` 递归创建多级：`mkdir -p hubei/wuhan/test` |
| `rmdir` | 删除**空**目录 | 目录非空会报错 |
| `rm` | 删除文件 / 目录 | `-r` 递归、`-f` 强制；**`rm -rf 目录`** 可删非空目录（危险，不可恢复！） |

### 7.7.2 文件操作命令

| 命令 | 功能 | 示例 |
|---|---|---|
| `touch` | 创建空文件（已存在则更新时间属性） | `touch 1.txt` |
| `cp` | 复制（可顺带改名） | `cp 3.ppt 301.ppt`；`cp /etc/passwd .`；`cp /var/log/messages mess111` |
| `mv` | 移动 / 重命名 | `mv 3.ppt 3.pptx`；`mv a.txt /tmp/` |
| `rm -f` | 强制删除不提示 | `rm -f 2.txt` |
| `scp` | 跨主机远程复制【补充】 | `scp file user@192.168.1.10:/tmp` |

### 7.7.3 文件查看命令

| 命令 | 功能 | 特点 |
|---|---|---|
| `cat` | 一次性输出全部内容 | 适合小文件 |
| `more` | 分页查看 | 空格翻页、回车逐行，**只能向下** |
| `less` | 分页查看 | 可上下翻页、搜索（`/关键字`），按 **q** 退出 |
| `head` | 看开头若干行 | 默认前 10 行；`head -3 /etc/passwd` 看前 3 行 |
| `tail` | 看末尾若干行 | 默认后 10 行；`tail -4` 后 4 行；【补充】`tail -f /var/log/messages` **实时滚动查看日志**（运维高频命令） |

### 7.7.4 课堂实操流程串联

```bash
pwd                                  # 确认当前位置
cd /tmp && ls                        # 切到临时目录并查看
ls -a                                # 看隐藏文件
ll                                   # 详细列表
mkdir hubei && mkdir hubei/wuhan     # 逐级创建
mkdir -p hubei/wuhan/sz              # 递归一次创建
rm -rf hubei                         # 删除非空目录
touch 1.txt                          # 建文件
cp /etc/passwd .                     # 复制配置文件到当前目录
mv 1.txt 1.txt.bak                   # 重命名
cat /etc/passwd | more               # 分页查看
head -3 /etc/passwd                  # 前 3 行
tail -4 /etc/passwd                  # 后 4 行
```

【补充】`/etc/passwd` 存放用户账户信息（用户名:密码占位:UID:GID:描述:家目录:Shell），是理解用户体系的关键文件。

## 7.8 vim 编辑器

### 7.8.1 vim 是什么

**vim**（Vi IMproved，vi 增强版）是 Linux 最常用的文本编辑器；老系统可能只有 **vi**。打开文件：

```bash
vim 文件名        # 如 vim /etc/passwd
```

### 7.8.2 三种模式（核心中的核心）

| 模式 | 作用 | 进入方式 |
|---|---|---|
| **命令模式**（普通模式） | 移动光标、复制、粘贴、删除、撤销 | `vim` 打开文件后**默认**就是它；任何模式按 **Esc** 回到它 |
| **插入模式**（编辑模式） | 正常输入文字 | 命令模式下按 `i / a / o / I / A / O` |
| **末行模式**（底行模式） | 保存、退出、查找替换、设置 | 命令模式下按 **`:`**（冒号） |

**模式切换图**：

```
        i/a/o/I/A/O              :  （冒号）
命令模式 ──────────→ 插入模式    命令模式 ──────────→ 末行模式
   ↑ ←── Esc ──────┘                ↑ ←── Esc ──────┘
```

### 7.8.3 进入插入模式的六个按键

| 按键 | 效果 |
|---|---|
| `i` | 在**光标前**插入 |
| `a` | 在**光标后**插入 |
| `o` | 在**下一行**插入新行 |
| `I` | 在**行首**插入 |
| `A` | 在**行尾**插入 |
| `O` | 在**上一行**插入新行 |

### 7.8.4 命令模式常用操作

| 按键 | 功能 |
|---|---|
| `yy` | 复制当前行（`nyy` 复制 n 行，如 `10yy`） |
| `p`（小写） | 在光标处**粘贴**（课程写作 P） |
| `dd` | 删除（剪切）当前行（`ndd` 删 n 行，如 `10dd`） |
| `u` | 撤销上一步 |
| `gg` | 跳到**第一行**行首 |
| `G` | 跳到**最后一行**行首（`nG` 跳到第 n 行，如 `10G`）【勘误：课程字幕"2G 跳最后一行"应为 Shift+G】 |
| `ZZ` | 保存并退出 |
| `x` | 删除光标处单个字符【补充】 |

### 7.8.5 末行模式常用命令

| 命令 | 功能 |
|---|---|
| `:w` | 保存 |
| `:q` | 退出 |
| `:q!` | **强制退出不保存** |
| `:wq` | 保存并退出 |
| `:wq!` | 强制保存并退出 |
| `:set number`（`:set nu`） | 显示行号（`:set nonu` 取消） |
| `/关键字` | 查找（n 跳下一个）【补充】 |

### 7.8.6 实操流程串联

```bash
vim test.txt      # 打开 → 默认命令模式
i                 # 进入插入模式开始打字
Esc               # 回到命令模式
yy → p           # 复制一行、粘贴
10dd             # 删 10 行
u                 # 撤销
gg / G            # 跳首行 / 末行
:set nu           # 显示行号
:wq               # 保存退出（或命令模式直接 ZZ）
```

---

<a id="p3-9"></a>
# 附录 A　英文术语速查表

| 缩写 | 英文全称 | 中文 |
|---|---|---|
| IDC | Internet Data Center | 互联网数据中心 |
| OSI | Open Systems Interconnection | 开放系统互连（参考模型） |
| TCP | Transmission Control Protocol | 传输控制协议 |
| UDP | User Datagram Protocol | 用户数据报协议 |
| IP | Internet Protocol | 互联网协议 |
| MAC | Media Access Control | 媒体访问控制（地址） |
| RIP | Routing Information Protocol | 路由信息协议 |
| RIPng | RIP next generation | RIP 下一代（IPv6 版） |
| OSPF | Open Shortest Path First | 开放式最短路径优先 |
| SPF | Shortest Path First | 最短路径优先（Dijkstra 算法） |
| LSA | Link State Advertisement | 链路状态通告 |
| LSDB | Link State Database | 链路状态数据库 |
| ABR | Area Border Router | 区域边界路由器 |
| ASBR | Autonomous System Boundary Router | 自治系统边界路由器 |
| AS | Autonomous System | 自治系统 |
| IGP | Interior Gateway Protocol | 内部网关协议 |
| EGP | Exterior Gateway Protocol | 外部网关协议 |
| BGP | Border Gateway Protocol | 边界网关协议 |
| VLSM | Variable Length Subnet Mask | 可变长子网掩码 |
| CIDR | Classless Inter-Domain Routing | 无类别域间路由 |
| MD5 | Message Digest Algorithm 5 | 消息摘要算法第 5 版 |
| ACL | Access Control List | 访问控制列表 |
| NAT | Network Address Translation | 网络地址转换 |
| NAPT | Network Address Port Translation | 网络地址端口转换 |
| PAT | Port Address Translation | 端口地址转换 |
| ALG | Application Level Gateway | 应用层网关 |
| VLAN | Virtual Local Area Network | 虚拟局域网 |
| TOR | Top of Rack | 机柜顶部（交换机） |
| ICMP | Internet Control Message Protocol | 互联网控制消息协议 |
| ARP | Address Resolution Protocol | 地址解析协议 |
| FTP | File Transfer Protocol | 文件传输协议 |
| TFTP | Trivial File Transfer Protocol | 简单文件传输协议 |
| HTTP | HyperText Transfer Protocol | 超文本传输协议 |
| HTTPS | HyperText Transfer Protocol Secure | 超文本传输安全协议 |
| DNS | Domain Name System | 域名系统 |
| SMTP | Simple Mail Transfer Protocol | 简单邮件传输协议 |
| POP3 | Post Office Protocol version 3 | 邮局协议第 3 版 |
| SNMP | Simple Network Management Protocol | 简单网络管理协议 |
| P2P | Peer-to-Peer | 点对点 |
| Telnet | TELecommunications NETwork | 远程登录协议 |
| SSH | Secure Shell | 安全外壳协议 |
| RSA | Rivest-Shamir-Adleman | 非对称加密算法 |
| DSA | Digital Signature Algorithm | 数字签名算法 |
| VTY | Virtual Teletype Terminal | 虚拟终端 |
| NTP | Network Time Protocol | 网络时间协议 |
| SNTP | Simple Network Time Protocol | 简单网络时间协议 |
| AAA | Authentication, Authorization, Accounting | 认证、授权、计费 |
| VRP | Versatile Routing Platform | 华为通用路由平台 |
| DRAM | Dynamic Random Access Memory | 动态随机存取内存 |
| POST | Power-On Self-Test | 加电自检 |
| BIOS | Basic Input/Output System | 基本输入输出系统 |
| UEFI | Unified Extensible Firmware Interface | 统一可扩展固件接口 |
| ROM | Read-Only Memory | 只读存储器 |
| CMOS | Complementary Metal-Oxide-Semiconductor | 互补金属氧化物半导体 |
| TPM | Trusted Platform Module | 可信平台模块 |
| HT | Hyper-Threading | 超线程 |
| NUMA | Non-Uniform Memory Access | 非统一内存访问 |
| AHCI | Advanced Host Controller Interface | 高级主机控制器接口 |
| IDE | Integrated Drive Electronics | 电子集成驱动器（传统硬盘接口） |
| RAID | Redundant Array of Independent Disks | 独立冗余磁盘阵列 |
| XOR | Exclusive OR | 异或运算 |
| SAS | Serial Attached SCSI | 串行连接 SCSI |
| SCSI | Small Computer System Interface | 小型计算机系统接口 |
| SATA | Serial Advanced Technology Attachment | 串行高级技术附件 |
| SSD | Solid State Drive | 固态硬盘【补充】 |
| BMC | Baseboard Management Controller | 基板管理控制器 |
| iBMC | intelligent Baseboard Management Controller | 华为智能基板管理控制器 |
| IPMI | Intelligent Platform Management Interface | 智能平台管理接口 |
| KVM | Keyboard, Video, Mouse | 键盘、显示器、鼠标（切换器 / 远程控制台） |
| iLO | Integrated Lights-Out | 惠普带外管理技术 |
| iDRAC | Integrated Dell Remote Access Controller | 戴尔带外管理技术 |
| IMM | Integrated Management Module | IBM 带外管理模块 |
| HDM | H3C Device Management | 华三带外管理 |
| PSU | Power Supply Unit | 电源供应单元 |
| PDU | Power Distribution Unit | 电源分配单元 |
| UID | Unit Identification | 单元定位（灯） |
| SN | Serial Number | 序列号 |
| VGA | Video Graphics Array | 视频图形阵列（接口） |
| USB | Universal Serial Bus | 通用串行总线 |
| RJ45 | Registered Jack 45 | 标准网线接口 |
| FAT32 | File Allocation Table 32 | 32 位文件分配表（文件系统） |
| POSIX | Portable Operating System Interface | 可移植操作系统接口 |
| ISO | ISO 9660 光盘镜像格式 | 镜像文件后缀 |
| FHS | Filesystem Hierarchy Standard | 文件系统层次结构标准 |
| GNOME | GNU Network Object Model Environment | 桌面环境 |
| KDE | K Desktop Environment | 桌面环境 |
| Kdump | Kernel Crash Dump | 内核崩溃转储 |
| PXE | Preboot eXecution Environment | 预启动执行环境 |
| DHCP | Dynamic Host Configuration Protocol | 动态主机配置协议 |
| NFS | Network File System | 网络文件系统 |
| SELinux | Security-Enhanced Linux | 安全增强型 Linux |
| ext4 | fourth extended filesystem | 第 4 代扩展文件系统 |
| LSI | LSI Logic（公司名） | RAID 卡厂商 |
| MGMT | Management | 管理（口） |
| FE / GE | Fast / Gigabit Ethernet | 快速 / 千兆以太网 |
| CPU | Central Processing Unit | 中央处理器 |

<a id="p3-10"></a>
# 附录 B　常用命令速查表

### 网络设备（华为 / 华三）

```bash
system-view                                 # 进系统视图
display current-configuration               # 查看运行配置
save                                        # 保存配置
reboot                                      # 重启
reset saved-configuration                   # 清空配置（恢复出厂）
sysname <名字>                              # 改主机名
display version                             # 版本
display interface brief                     # 接口概览
display ip routing-table                    # 路由表
display vlan                                 # VLAN
display cpu-usage / display memory          # CPU / 内存
display device / display power / display fan / display temperature   # 硬件
ping <IP> / tracert <IP>                    # 连通性测试 / 路径跟踪
```

### Linux

```bash
# 硬件
lscpu                       # CPU
cat /proc/cpuinfo           # CPU 详情
dmidecode -t bios|memory|system    # BIOS / 内存 / 整机
free -h                     # 内存
lsblk -f                    # 磁盘分区
df -h                       # 分区使用率

# 文件目录
pwd / ls -a / ll / cd ..    # 位置与切换
mkdir -p a/b/c              # 递归建目录
rm -rf <目录>               # 强删
touch / cp / mv             # 建 / 复制 / 移动改名
cat / more / less / head / tail -f   # 查看（-f 实时日志）

# 编辑与系统
vim <文件>                  # 编辑器（i 插入、Esc、:wq 保存退出）
shutdown -h now / reboot    # 关机 / 重启【补充】
```

<a id="p3-11"></a>
# 附录 C　课程作业自测参考答案

**第 31 课作业：**

1. **路由器的作用**：连接不同网络（局域网 / 广域网）；在网络间转发数据包（第三层转发）；隔离广播域；根据路由表选择最优路径转发。
2. **路由表中的路由类型**：直连路由、静态路由、动态路由（按协议分 RIP / OSPF 等学习到的路由）。
3. **属于 IGP 的路由协议**：RIP、OSPF（以及 IS-IS、EIGRP 等）；BGP 属于 EGP。
4. **华为路由器 OSPF 的优先级**：内部路由 **10**（外部路由为 150；对比：直连 0、静态 60、RIP 100）。

**第 59 课作业（带外管理）：**

1. 带外管理口插了网线 → 选择**专用（独占 / Dedicated）模式**；
2. 带外管理地址由管理员分配，私网 / 公网地址均可，关键保证管理机可达；
3. 带外管理口标识：BMC、iLO、iDRAC、IMM、HDM、IPMI、MGMT；
4. 带外管理功能：远程开关机 / 重启、远程虚拟控制台（KVM）、远程挂载安装系统、硬件状态与传感器监控、日志查看、固件升级、用户管理等。

**第 62 课作业（RAID）：**

1. 开机按 `Ctrl+H` 进入的是 **WebBIOS 模式**的 RAID 配置界面；
2. **RAID 0** 不具备数据保护功能（唯一无冗余的级别）；
3. RAID 组中一块硬盘坏了 → 处于**降级（Degraded）状态**；
4. RAID 5 至少需要 **3** 块硬盘。

**第 66 课作业（Linux / PXE）：**

1. Linux 系统特点：一切皆文件、免费开源、兼容 POSIX、多用户多任务、命令严格区分大小写；
2. swap 交换分区建议为物理内存的 **1.5~2 倍**；
3. 属于 Linux 操作系统的：openSUSE、SUSE、Ubuntu、Red Hat、CentOS、Debian 等；
4. 服务器 PXE 安装开机一般按 **F12**。

---

## 学习建议（给初学者）

1. **网络部分**按"OSI 分层 → IP 地址 → 交换 / 路由 → 安全（ACL / NAT）→ 架构"的顺序理解，每学一个协议先问自己：它解决什么问题？工作在哪一层？
2. **设备配置**一定要动手敲，用 eNSP（华为网络模拟器）或真机练习，重点掌握：视图切换、display 家族、Telnet/SSH、VLAN、ACL、NAT 六大场景。
3. **服务器部分**先把"开机按键 → BIOS → RAID → 带外管理 → 装系统"这条物理链路走通，理解一台服务器从上架到交付的完整流程。
4. **Linux 部分**坚持用命令行：文件管理命令每天练，vim 三模式切换练到形成肌肉记忆——这是运维工程师吃饭的本事。

> 本笔记基于课程字幕整理，【补充】与【勘误】内容为编者按公开的通用技术资料补正，具体设备命令以各厂商官方手册为准。

---
<!-- ============ 笔记三：Linux 系统管理·综合布线·管理制度·运维流程 ============ -->

# 数据中心（IDC）IT 运维工程师学习笔记

> **笔记来源**：数据中心（IDC，Internet Data Center）IT 运维工程师课程第 71–86 讲（语音转写字幕整理）。
> **覆盖内容**：Linux 服务器系统管理、数据中心综合布线管理、数据中心管理规范。
> **组织方式**：不按讲次罗列，而是按知识体系重新归纳，并为初学者补齐前置基础知识。
> **阅读约定**：所有关键术语在首次出现时给出完整英文全称；"补充知识"部分为课程之外拓展内容，帮助零基础读者理解。

---

## 目录

- 第 0 章 预备知识（写给零基础的你）
- 第 1 章 文件归档与压缩
- 第 2 章 用户与用户组管理
- 第 3 章 文件权限管理
- 第 4 章 磁盘与文件系统管理
- 第 5 章 网络配置与管理
- 第 6 章 防火墙与 SELinux
- 第 7 章 运行级别与 root 密码重置
- 第 8 章 SSH 远程登录管理
- 第 9 章 综合布线基础
- 第 10 章 线缆布放流程与规范
- 第 11 章 标签粘贴规范
- 第 12 章 综合布线项目验收标准
- 第 13 章 数据中心管理规定
- 第 14 章 数据中心日常运维流程
- 第 15 章 服务器操作流程
- 第 16 章 网络设备操作流程
- 附录 A 术语中英文对照总表
- 附录 B 常用命令速查表
- 附录 C 课程自测题与参考答案
- 结语：从初级到进阶

---

<a id="p4-0"></a>
# 第 0 章 预备知识（写给零基础的你）

课程字幕默认读者已有一定基础，本章把后面会用到、但课程没细讲的前置知识集中补齐。

## 0.1 认识数据中心与 IT 运维

- **数据中心（IDC，Internet Data Center）**：集中放置服务器、网络设备、存储设备，并提供供电、制冷、安防等配套环境的场所。互联网公司的网站、App、游戏等业务，最终都跑在数据中心的服务器上。
- **IT 运维（IT Operations & Maintenance，O&M）**：保障数据中心里的服务器、网络设备、布线系统持续正常运行的岗位。日常核心工作是：**处理工单、日常巡检、定期盘点**。
- **运维工程师的两大工作界面**：
  1. **带内管理**：通过网络（如 SSH 远程登录）登录设备操作系统进行管理。
  2. **带外管理（OOB，Out-of-Band Management）**：服务器主板上有一块独立的管理芯片（如戴尔的 iDRAC，Integrated Dell Remote Access Controller；HPE 的 iLO，Integrated Lights-Out；通用标准 IPMI，Intelligent Platform Management Interface），即使服务器操作系统坏了，也能通过浏览器远程看到服务器画面并开关机。

## 0.2 网络基础概念速成

服务器上网和人的通信类似，需要几个关键参数，配置 IP 时都会用到：

| 概念 | 英文全称 | 通俗解释 |
|---|---|---|
| IP 地址 | IP（Internet Protocol）Address | 设备在网络中的"门牌号"，如 `192.168.10.11`。目前主流是 IPv4（IP version 4），用 32 位二进制表示，写成 4 段十进制（每段 0–255） |
| 子网掩码 | Subnet Mask | 用来区分"哪部分是小区名、哪部分是门牌号"。例如 `255.255.255.0`（等价写法 `/24`）表示前 24 位是网络位、后 8 位是主机位 |
| 网关 | Gateway | 本网段对外的"大门"，通常是出口路由器/三层交换机的地址，如 `192.168.1.1`。跨网段通信必须经过网关 |
| DNS | Domain Name System，域名系统 | 把域名（www.baidu.com）翻译成 IP 地址的"电话簿"。常用公共 DNS：`114.114.114.114`、`8.8.8.8` |
| MAC 地址 | Media Access Control Address | 网卡的全球唯一物理编号（出厂烧录），48 位十六进制，如 `00:0C:29:XX:XX:XX` |
| DHCP | Dynamic Host Configuration Protocol，动态主机配置协议 | 服务器开机后自动从 DHCP 服务器获取 IP、掩码、网关、DNS，无需手工填写 |
| 端口 | Port | 区分同一台服务器上的不同服务，取值 0–65535。例如 SSH 用 22、网页 HTTP（HyperText Transfer Protocol）用 80 |

**补充知识：私有 IP 地址段**

以下三段地址专用于内网，不会出现在公网上（这也是为什么机房里到处都是 192.168.x.x）：

- `10.0.0.0 – 10.255.255.255`（10.0.0.0/8）
- `172.16.0.0 – 172.31.255.255`（172.16.0.0/12）
- `192.168.0.0 – 192.168.255.255`（192.168.0.0/16）

**补充知识：TCP/IP 与 OSI 分层模型**

| TCP/IP 模型 | 对应 OSI 模型 | 典型协议/设备 |
|---|---|---|
| 应用层 | 应用层/表示层/会话层 | HTTP、DNS、SSH、DHCP |
| 传输层 | 传输层 | TCP（Transmission Control Protocol，可靠传输）、UDP（User Datagram Protocol，快速但不保证送达） |
| 网络层 | 网络层 | IP、ICMP（Internet Control Message Protocol，ping 命令用的协议）；路由器工作于此层 |
| 网络接口层 | 数据链路层/物理层 | 以太网帧；交换机工作于数据链路层 |

## 0.3 Linux 入门必知

- **Linux**：开源操作系统内核，由 Linus Torvalds 于 1991 年发布。数据中心服务器绝大多数运行 Linux。
- **常见发行版**：RHEL（Red Hat Enterprise Linux，红帽企业版）、CentOS（Community ENTERprise Operating System，社区企业版，与 RHEL 兼容且免费，本课程演示环境）、Ubuntu、Debian、openEuler 等。课程中的 "red5/6/7" 即 CentOS/RHEL 5、6、7。
- **终端与 Shell**：终端（Terminal）是命令行交互窗口；Shell 是解释你输入命令的程序，Linux 默认 Shell 是 **bash（Bourne-Again SHell）**。`/bin/bash` 表示可登录用户，`/sbin/nologin` 表示禁止登录（常用于程序账户）。
- **一切皆文件**：Linux 把硬盘、网卡、键盘等设备都抽象成文件放在 `/dev`（devices）目录下，这是理解磁盘管理的基础。
- **/etc（etcetera）**：存放系统配置文件的目录，课程中的用户配置、网卡配置、挂载配置都在这里。

**补充知识：Linux 主要目录结构（FHS，Filesystem Hierarchy Standard，文件系统层次标准）**

| 目录 | 用途 |
|---|---|
| `/` | 根目录，一切起点 |
| `/etc` | 系统配置文件（passwd、fstab、ssh 等） |
| `/dev` | 设备文件（sda 硬盘等） |
| `/home` | 普通用户家目录（每用户一个文件夹） |
| `/root` | 超级管理员 root 的家目录 |
| `/var` | 经常变化的数据（日志 /var/log、邮件 /var/spool/mail） |
| `/tmp` | 临时文件 |
| `/bin`、`/sbin` | 基本命令与系统管理命令 |
| `/proc` | 内核与进程的虚拟信息（内存中的映像） |

**补充知识：vim 编辑器三步走（课程多次用到但未讲解）**

1. 敲 `vim 文件名` 打开文件，此时是**普通模式**（只能看，方向键移动光标）；
2. 按 `i` 进入**插入模式**（左下角出现 INSERT），此时才能修改内容；
3. 改完按 `Esc` 回到普通模式，再输入 `:wq` 回车（保存并退出）。常用：`:q!`（不保存强制退出）、`dd`（删除一行）、`yy` + `p`（复制粘贴一行）。

**补充知识：课程中零散出现的基础命令**

| 命令 | 全称/含义 | 作用 |
|---|---|---|
| `ls` / `ll` | list；ll 是 `ls -l` 的别名 | 列出文件；ll 显示详细信息 |
| `cd` | change directory | 切换目录 |
| `cp` | copy | 复制 |
| `mv` | move | 移动/改名 |
| `rm -rf` | remove，r 递归、f 强制 | 删除；`rm -rf *` 清空当前目录（危险） |
| `mkdir` | make directory | 创建目录 |
| `cat` | concatenate | 查看文件内容 |
| `clear` / `Ctrl+L` | 清屏 | 清屏 |
| `man` / `--help` | manual | 查看命令帮助手册 |

## 0.4 常见端口号速记

| 端口 | 服务 | 全称 |
|---|---|---|
| 22 | SSH | Secure Shell |
| 23 | Telnet | 远程登录（明文传输，已被 SSH 取代） |
| 80 | HTTP | HyperText Transfer Protocol |
| 443 | HTTPS | HTTP Secure（over TLS/SSL） |
| 53 | DNS | Domain Name System |
| 21 | FTP | File Transfer Protocol |

---

<a id="p4-1"></a>
# 第 1 章 文件归档与压缩

## 1.1 为什么要归档和压缩

运维中经常需要备份日志文件（如 `/var/log/messages`）、迁移数据。单个文件可以用**压缩**减小体积，多个文件合并成一个文件叫**归档（打包）**，两者通常配合使用：先归档成一个大文件，再压缩。

## 1.2 Linux 四种常用压缩格式

| 格式 | 压缩工具 | 压缩命令 | 解压命令 | 默认保留原文件 | 能否压缩目录 | 特点 |
|---|---|---|---|---|---|---|
| `.gz` | gzip（GNU Zip） | `gzip 文件` | `gunzip 文件` 或 `gzip -d 文件` | 否（压缩后原文件消失） | 否 | 速度最快 |
| `.bz2` | bzip2 | `bzip2 文件` | `bunzip2 文件` | 否 | 否 | 压缩率高于 gzip，速度较慢 |
| `.xz` | xz | `xz 文件` | `xz -d 文件` | 否 | 否 | 压缩率最高、体积最小，速度最慢 |
| `.zip` | zip | `zip 压缩包.zip 文件` | `unzip 压缩包.zip` | 是 | 是 | 跨平台（Windows 通用） |

**要点**：gzip/bzip2/xz 只能压缩单个文件、不能直接压缩目录，所以必须先配合归档工具 tar；zip 可以压缩目录且保留原文件，但压缩率一般。

## 1.3 tar 归档命令

**tar（Tape Archive，磁带归档）** 将多个文件合并为一个归档文件（俗称 tar 包），并可直接调用上面三种压缩工具。

| 选项 | 英文含义 | 作用 |
|---|---|---|
| `-c` | create | 创建归档 |
| `-x` | extract（提取） | 解开归档 |
| `-f` | file | 指定归档文件名（**必须放在选项最后**） |
| `-v` | verbose（详细） | 在终端显示过程 |
| `-t` | list（列目录） | 查看归档内容（不解压） |
| `-z` | 调用 gzip | 生成/解开 `.tar.gz` |
| `-j` | 调用 bzip2 | 生成/解开 `.tar.bz2` |
| `-J` | 调用 xz | 生成/解开 `.tar.xz` |

**常用组合（以备份 2021 年 12 月 30 日两个文件为例）**：

```bash
# 压缩归档
tar -zcf 20211230.tar.gz messages passwd    # gzip 方式
tar -jcf 20211230.tar.bz2 messages passwd   # bzip2 方式
tar -Jcf 20211230.tar.xz  messages passwd    # xz 方式（体积最小）

# 解压缩
tar -zxf 20211230.tar.gz                     # 解开 gz 包
tar -jxf 20211230.tar.bz2                    # 解开 bz2 包
tar -Jxf 20211230.tar.xz                     # 解开 xz 包
```

**记忆口诀**：压缩看"c、f"，解压看"x、f"；z/j/J 对应三种压缩格式（小 z → gzip，小 j → bzip2，大 J → xz）。

**实操演示流程回顾**：进入 `/tmp` 目录 → `rm -rf *` 清空 → `cp /etc/passwd .` 和 `cp /var/log/messages .` 复制两个测试文件 → 用 `ll`（list long）查看大小 → 分别用三种方式压缩对比体积（xz 最小）→ 再逐个解压验证。

**补充知识**

- `-C` 可以指定解压路径：`tar -zxf a.tar.gz -C /root`。
- 想在不解开包的情况下确认内容：`tar -tf a.tar.gz`。
- gzip 只保留一个原文件时，可加 `-k`（keep）选项保留源文件：`gzip -k 文件`。
- Windows 下对应关系：`.tar.gz` ≈ `.zip`/`.rar`；Windows 10/11 资源管理器已原生支持打开 tar.gz。

---

<a id="p4-2"></a>
# 第 2 章 用户与用户组管理

## 2.1 Linux 的多用户机制

Linux 是**多用户、多任务的分时操作系统**：多人可以同时登录使用同一台服务器。任何想使用系统资源的人，必须先向系统管理员申请账号，再以该账号身份进入系统。

**核心概念**：Linux 内部其实**不认识用户名，只认 ID 号**。

- **UID（User Identifier，用户标识号）**
- **GID（Group Identifier，组标识号）**

每个用户创建时会默认属于一个同名主组。

## 2.2 用户的三种类型

| 用户类型 | 说明 | CentOS 6 的 UID | CentOS 7 的 UID |
|---|---|---|---|
| 超级用户 root | 至高权限，进入任何用户都不需要密码 | 0 | 0 |
| 程序（系统）用户 | 给系统进程/服务运行用，不允许登录 | 1–499 | 1–999 |
| 普通用户 | 日常人员使用，权限受限 | 500–60000 | 1000–60000 |

> 注：CentOS 7 中系统用户与普通用户的准确分界是 999/1000，课程取约数 1000/1001。

## 2.3 用户管理命令

| 命令 | 全称 | 作用 | 常用选项 |
|---|---|---|---|
| `useradd` | add user | 创建新用户 | `-c` 指定描述信息；`-d` 指定家目录；`-g` 指定主组；`-G` 指定附加组；`-s` 指定登录 Shell；`-u` 指定 UID |
| `userdel` | delete user | 删除用户 | `-r` 连同家目录和邮件文件一起删除（"人走了，家也拆了"） |
| `usermod` | modify user | 修改用户属性 | 选项与 useradd 相同 |
| `passwd` | password | 设置/修改密码 | 后跟用户名；不跟则默认改当前用户（root 下即改 root） |
| `su` | switch user（习惯说法） | 切换用户 | `su 用户名` 只切身份；`su - 用户名` 连登录环境一起切换 |
| `id` | identity | 查看用户 UID/GID 信息，也可判断用户是否存在 | — |
| `whoami` | who am I | 查看当前登录的用户名 | — |

**演示要点**：

```bash
id kai            # 查询用户是否存在；不存在则报 "no such user"
useradd kai        # 创建用户 kai，默认 UID 从 1001 开始
useradd -c "boss" -u 1003 -g kai dianwei   # 指定描述、UID、主组创建用户
id dianwei         # 显示 UID=1003, GID=1001
usermod -u 1314 kai   # 修改 kai 的 UID
userdel -r dianwei    # 彻底删除用户
passwd kai         # 给 kai 设置密码（输入不回显；提示"无效的密码"可忽略，继续输入）
su kai             # 切到 kai；root 切普通用户不需要密码
su - root           # 普通用户切回 root 需要 root 密码
```

**为什么 root 切换到任何用户都不需要密码？** 因为 root 是超级管理员——密码本来就是它发的。

## 2.4 用户组管理命令

| 命令 | 全称 | 作用 |
|---|---|---|
| `groupadd` | add group | 创建用户组（`-g` 指定 GID） |
| `groupdel` | delete group | 删除组 |
| `groupmod` | modify group | 修改组属性（如 `-g 1011` 改 GID） |
| `gpasswd` | group password | 管理组成员：`-A` 指定组管理员，`-a` 添加用户到组，`-d` 从组中删除用户 |

```bash
groupadd -g 1010 game      # 创建 game 组，GID 1010
gpasswd -a dianwei game    # 把 dianwei 加入 game 组
gpasswd -d dianwei game    # 把 dianwei 移出 game 组
```

## 2.5 用户与组相关的配置文件（重点）

"一切皆文件"——所有用户管理命令最终都是在修改这几个文件：

### /etc/passwd（用户配置文件，7 个字段，冒号分隔）

```
kai:x:1001:1001::/home/kai:/bin/bash
│  │  │    │   │   │         └ 登录 Shell：/bin/bash 可登录；/sbin/nologin 禁止登录
│  │  │    │   │   └ 家目录
│  │  │    │   └ 描述信息（useradd -c 设置）
│  │  │    └ GID（主组 ID）
│  │  └ UID
│  └ 密码占位符：x 表示真实密码在 /etc/shadow 中
└ 用户名
```

### /etc/shadow（影子密码文件，9 个字段）

存放**加密后的用户密码**，仅超级管理员可读写。字段依次为：

```
登录名 : 加密口令 : 最后一次修改时间 : 最小时间间隔 : 最大时间间隔 : 警告时间 : 不活跃时间 : 失效时间 : 标志
```

含义速记：多久才能改一次密码（最小间隔）、密码多久必须换（最大间隔）、过期前几天开始警告、过期后宽限几天、账号哪天作废。

### /etc/group（组配置文件，4 个字段）

```
组名 : 密码 : GID : 组内用户列表
```

另有 /etc/gshadow（组影子文件：组名、密码、组管理员、组成员）。

### 创建用户时系统做了什么

每创建一个新用户：

1. `/etc/passwd`、`/etc/shadow`、`/etc/group`、`/etc/gshadow` 末尾各追加一行；
2. `/home/` 下新建与用户同名的**家目录**；
3. `/var/spool/mail/` 下新建该用户的**系统邮件文件**。

`userdel -r` 删除的正是第 2、3 项。

**补充知识：sudo**

日常运维不建议直接用 root 登录。`sudo（superuser do）`允许管理员在 `/etc/sudoers` 中授权普通用户以 root 权限执行个别命令，执行时输入自己的密码。这是"最小权限原则"的体现，也是生产环境的标准做法。

---

<a id="p4-3"></a>
# 第 3 章 文件权限管理

## 3.1 如何查看权限：ls -l 输出逐段解读

```bash
ls -ld /etc
drwxr-xr-x. 136 root root 12288 Sep 30 10:00 /etc
```

| 段 | 示例 | 含义 |
|---|---|---|
| 第 1 段（10 字符） | `drwxr-xr-x` | 文件类型 + 权限。首字符：`d` 目录（directory）、`-` 普通文件、`l` 链接文件（link）、`b`/`c` 块/字符设备 |
| 第 2 段 | 136 | 被硬链接（hard link）的次数 |
| 第 3 段 | root | 所有者（owner） |
| 第 4 段 | root | 所属组（group） |
| 第 5 段 | 12288 | 大小（字节，Byte） |
| 第 6 段 | Sep 30 10:00 | 最后修改时间 |
| 第 7 段 | /etc | 文件名 |

**9 个权限字符每 3 个一组**：

| 组 | 代表 | 字母 |
|---|---|---|
| 第 1 组 | 所有者（u，user/owner） | rwx |
| 第 2 组 | 所属组（g，group）内成员 | r-x |
| 第 3 组 | 其他用户（o，other） | r-x |
| 全部 | a（all）= u+g+o | — |

## 3.2 三种基本权限：r、w、x

| 权限 | 英文 | 字母 | 数字 | 对**文件**的意义 | 对**目录**的意义 |
|---|---|---|---|---|---|
| 读 | Read | r | 4 | 可查看文件内容 | 可查看目录内的内容（ls） |
| 写 | Write | w | 2 | 可修改文件内容 | 可在目录内新建/删除/改名条目 |
| 执行 | eXecute | x | 1 | 可执行该文件（脚本、程序） | 可 cd 进入该目录 |

- 三种权限全有：字母 `rwx`，数字 `4+2+1=7`。
- **默认权限**：文件 `644`（rw-r--r--），目录 `755`（rwxr-xr-x）。为什么是三位数？分别对应所有者/所属组/其他用户的权限。
- 三位数字速读示例：`755` = 所有者 7（rwx）、组 5（r-x）、其他 5（r-x）；`600` = 所有者 6（rw-）、其他人无任何权限。

## 3.3 权限的管理命令

| 命令 | 全称 | 作用 | 示例 |
|---|---|---|---|
| `chown` | Change Owner | 更改所有者（也可同时改所属组） | `chown kai 1.txt`；`chown kai:kai 1.txt`（同时改组）；`chown :root 1.txt`（只改组） |
| `chgrp` | Change Group | 只更改所属组 | `chgrp kai 1.txt` |
| `chmod` | Change Mode | 更改权限（字母法/数字法） | 见下 |

**chmod 字母法**（对象 u/g/o/a，+ 加 - 减 = 赋值）：

```bash
chmod u-x 1.txt      # 去掉所有者的执行权限
chmod a+x 1.txt      # 所有人都加执行权限
chmod g+w 1.txt      # 给所属组加写权限
```

**chmod 数字法**：

```bash
chmod 600 1.txt      # 所有者可读写，其他人无权限
chmod 755 aaa        # 目录常用
chmod 000 aaa        # 收走所有权限（root 仍可进入，普通用户则不行）
```

> 思考题答案（课程原题）：`a+w` = 给所有用户（all）加上写（write）权限。

## 3.4 演示流程回顾

1. 在 `/tmp` 下 `mkdir aaa` 并创建测试文件，`ll` 观察：文件默认 `644`、目录默认 `755`。
2. root 环境创建的文件，所有者和所属组都是 root。
3. `chown kai:kai 1.txt` 一次改两个；`chown root 1.txt` 只改所有者；`chown :root 1.txt` 或 `chgrp kai aaa` 只改组。
4. `chmod a+x` 后文件名变绿色（可执行文件的显示色）。

**补充知识：默认权限的由来——umask**

文件/目录的默认权限由 **umask（user file-creation mask，用户文件创建掩码）** 决定：

- 文件：`666 - umask(022) = 644`
- 目录：`777 - umask(022) = 755`

**补充知识：三种特殊权限**

| 特殊权限 | 全称 | 数字 | 常见例子 |
|---|---|---|---|
| SUID | Set User ID | 4（占所有者位） | `/usr/bin/passwd`（普通用户执行时临时获得 root 权限才能改密码） |
| SGID | Set Group ID | 2（占组位） | 共享目录：新建文件自动继承目录属组 |
| Sticky Bit | 粘滞位 | 1（占其他位） | `/tmp`：各用户只能删除自己的文件 |

---

<a id="p4-4"></a>
# 第 4 章 磁盘与文件系统管理

## 4.1 硬盘在 Linux 中的表示

**先回顾硬件**：服务器硬盘按槽位编号，**0 号槽位是第一块硬盘**，1 号槽位是第二块，以此类推（系统报错显示槽位号时，0 即第一块盘）。

**设备文件**在 `/dev`（devices，设备目录）下：

| 设备文件 | 含义 |
|---|---|
| `/dev/sda` | 第一块硬盘（a = 第一块） |
| `/dev/sdb` | 第二块硬盘（b = 第二块） |
| `/dev/sdo` | 第 15 块硬盘 |

`sd` 表示接口类型为 SATA（Serial Advanced Technology Attachment，串行 ATA）或 SAS（Serial Attached SCSI，串行连接 SCSI）。

**补充知识**：sd 全称 SCSI Disk（SCSI，Small Computer System Interface）；云主机 virtio 盘叫 `/dev/vda`；NVMe（Non-Volatile Memory Express）固态盘叫 `/dev/nvme0n1`，生产环境越来越常见。

## 4.2 分区的表示方法

| 分区名 | 含义 |
|---|---|
| `sda1` | 第一块硬盘的第 1 个**主分区**（primary partition） |
| `sda2`、`sda3` | 第 2、3 个主分区（一块硬盘最多 4 个主分区） |
| `sda4` | 扩展分区（extended partition），占用第 4 个主分区名额 |
| `sda5`、`sda6`… | 扩展分区上划分的**逻辑分区**（logical partition），编号从 5 开始 |

**补充知识：MBR 与 GPT**

上述"最多 4 个主分区"的限制来自 **MBR（Master Boot Record，主引导记录）** 分区表，单盘最大支持 2TB。大于 2TB 的硬盘需使用 **GPT（GUID Partition Table，全局唯一标识分区表）**，主分区数量几乎不受限。MBR 分区用 fdisk，GPT 分区用 parted/gdisk。

## 4.3 文件系统类型

Windows 的 C 盘 D 盘用 NTFS（New Technology File System），Linux 则有以下选择：

| 文件系统 | 全称/来历 | 关键参数 | 特点与现状 |
|---|---|---|---|
| ext2 | Extended File System 2 | — | Linux 早期商业级文件系统，无日志，已淘汰 |
| ext3 | Extended File System 3 | 文件系统 16TB、单文件 2TB | ext2 + 日志（journal），RHEL/CentOS 5 默认 |
| ext4 | Extended File System 4 | 文件系统 1EB、单文件 16TB、子目录无限 | 兼容 ext3；快速 fsck（File System Check）、日志校验、在线碎片整理；RHEL/CentOS 6 默认 |
| XFS | X File System（源自 SGI IRIX） | 单文件最大 8EB−1 字节 | 高性能日志文件系统：崩溃后可迅速恢复、快照、高 I/O 吞吐、在线扩容；RHEL/CentOS 7 及以上默认 |
| swap | 交换空间 | — | 不是用来存数据的"特殊文件系统"：物理内存不足时把不活跃数据换到磁盘上，相当于 Windows 的虚拟内存 |

**补充知识：什么是日志文件系统（Journaling File System）**
写数据前先把"打算做什么"记到日志区，断电崩溃后可依据日志快速恢复一致性，避免全盘扫描。这就是 ext3/ext4/XFS 比 ext2 健壮的原因。

## 4.4 磁盘操作四步曲：分区 → 刷新 → 格式化 → 挂载

**核心概念——挂载（mount）**：Linux"一切皆文件"，分区必须挂载到某个已存在的目录下才能访问。访问该目录 = 访问该分区。

### 第一步：fdisk 分区

```bash
fdisk -l                 # 查看当前所有硬盘（如 sda 68.7G、sdb 10G）
fdisk /dev/sdb           # 对第二块硬盘分区，进入交互界面
```

fdisk 交互界面常用按键：

| 按键 | 作用 |
|---|---|
| `m` | 帮助 |
| `n` | 新建分区（p 选主分区，e 选扩展分区；逻辑分区自动从 5 开始编号） |
| `p` | 打印当前分区表 |
| `d` | 删除分区 |
| `w` | 保存并退出（**不保存等于白干**） |
| `q` | 不保存退出 |

分区大小可用 `+10G`、`+500M` 直接指定（起始扇区默认 2048 即可）。典型方案：3 个主分区（如 5G + 500M + 100M）+ 1 个扩展分区（吃掉剩余空间）+ 若干逻辑分区。

### 第二步：刷新分区表

```bash
partprobe /dev/sdb
```

**partprobe**（partition probe，分区探测）通知内核重新读取分区表，避免重启才生效。

### 第三步：格式化（创建文件系统）

```bash
mkfs.xfs  /dev/sdb1     # 格式化为 XFS
mkfs.ext4 /dev/sdb2     # 格式化为 ext4
```

**mkfs** = Make File System。连按两下 Tab 可列出系统支持的类型（ext2/ext3/ext4/xfs…）。

### 第四步：挂载

```bash
mkdir /dianwei /kai          # 创建挂载点目录（挂载点必须已存在）
mount /dev/sdb1 /dianwei    # 把分区挂到目录
df -Th                       # 查看挂载情况与容量
```

**df**（Disk Free，磁盘剩余空间）：`-T` 显示文件系统类型，`-h`（human-readable，人性化单位）按 GB/MB 显示。

**临时 vs 永久挂载**：

- `mount` 命令挂载是**临时**的，重启失效。
- 永久挂载需写入 **/etc/fstab**（File System Table，文件系统表）：

```
/dev/sdb1   /dianwei   xfs     defaults    0 0
/dev/sdb2   /kai       ext4    defaults    0 0
```

6 个字段：设备 | 挂载点 | 文件系统类型 | 挂载选项（defaults 默认） | dump 备份标志 | fsck 开机检查顺序。**此文件写错会导致开机失败，务必仔细核对。**

**卸载**：

```bash
umount /kai        # 卸载；若正位于该目录内会失败，先 cd 出去
mount -a           # 重新读取 /etc/fstab 全部挂载（开机时系统就是执行它）
```

## 4.5 磁盘信息查看命令

| 命令 | 全称 | 作用 |
|---|---|---|
| `df -Th` | Disk Free | 查看已挂载分区容量使用情况 |
| `fdisk -l` | fixed disk | 查看硬盘与分区详细信息 |
| `lsblk` | list block devices | 树状列出块设备（补充） |
| `blkid` | block device id | 查看分区 UUID（补充） |

**补充知识：LVM 与 UUID 写法**

- 生产环境推荐在 fstab 中用 `UUID=xxx` 代替 `/dev/sdb1`，避免插拔硬盘后盘符漂移导致无法开机。
- **LVM（Logical Volume Manager，逻辑卷管理）**：把多块物理盘/分区组成卷组再划分逻辑卷，支持在线扩容，是 CentOS/RHEL 默认安装布局，中级阶段应掌握。

> 课程反复强调：**数据无价，谨慎操作**。分区、格式化都会清空数据，操作前务必确认目标盘。

---

<a id="p4-5"></a>
# 第 5 章 网络配置与管理

## 5.1 服务器网卡基础

服务器网卡一般有**电口（RJ45，接网线）**和**光口（接光纤）**两种，带宽可能不同，但与个人电脑一样，必须配置 **IP 地址、子网掩码、网关、DNS** 四要素才能联网。

- `eth0` 是第一块网卡，`eth1` 第二块，以此类推。
- `lo` 是**回环网卡（loopback）**，地址固定 `127.0.0.1`，代表"本机自己"，用于本机内部测试。

**补充知识**：CentOS 7 开始网卡可能叫 `ens33`、`eno16777736` 等（一致性命名规则，Predictable Network Interface Names），含义与 eth0 一样，配置方法完全相同。

## 5.2 永久配置：编辑网卡配置文件

配置文件目录：**/etc/sysconfig/network-scripts/**，文件名为 `ifcfg-eth0`、`ifcfg-eth1` 或 `ifcfg-ens33` 等。

**DHCP 方式（自动获取）**：

```
TYPE=Ethernet
BOOTPROTO=dhcp
NAME=eth0
UUID=xxxx-xxxx-xxxx            # 网卡全局唯一标识
ONBOOT=yes                     # 关键：开机是否启用网卡，必须为 yes
```

**静态方式（手工指定，服务器标准做法）**：

```
TYPE=Ethernet
BOOTPROTO=static               # 由 dhcp 改为 static
NAME=eth1
ONBOOT=yes
IPADDR=192.168.10.11           # IP 地址
NETMASK=255.255.255.0          # 子网掩码（须大写）
GATEWAY=192.168.1.1           # 网关
DNS1=114.114.114.114           # 第一个 DNS
DNS2=8.8.8.8                   # 第二个 DNS（可选）
```

> 参数名必须大写（IPADDR、NETMASK、GATEWAY）。IPv6 相关行不需要可直接删除。

**改完必须重启网卡服务**（见 5.5）。

## 5.3 临时配置：ifconfig 命令

```bash
ifconfig                              # 查看所有网卡 IP（含 MAC 地址）
ifconfig eth0                         # 只看第一块网卡
ifconfig eth1 192.168.10.10 netmask 255.255.255.0   # 临时设置 IP，重启失效
ip a                                  # ip addr show，同样可查看（更现代）
```

**ifconfig** = Interface Configuration（接口配置），属于 net-tools 老工具包。

## 5.4 DNS 与域名解析相关文件

| 文件 | 作用 |
|---|---|
| `/etc/resolv.conf` | DNS 服务器配置文件。网卡配置文件里写的 DNS1/DNS2 会同步显示在这里；也可直接编辑此文件修改 DNS |
| `/etc/hosts` | 本地域名解析表（主机名与 IP 的对应关系）。**服务器解析域名时优先查找此文件**，利用它可以把常用远程主机写成主机名直接登录 |

```bash
cat /etc/resolv.conf    # 查看当前 DNS
```

## 5.5 网卡服务管理

| 命令 | 作用 |
|---|---|
| `ifconfig eth0 down` / `up` | 关闭 / 激活单块网卡 |
| `systemctl restart network` | 重启所有网卡服务（CentOS 7） |
| `systemctl stop network` / `start network` | 停止 / 启动所有网卡 |
| `systemctl enable network` | 设置开机自启 |
| `service network restart` | CentOS 5/6 的写法，7 中仍兼容 |

**systemctl** = system control，是 CentOS 7 的服务管理总命令；`service` 是 5/6 时代（SysV init）的服务管理命令。

**补充知识**：CentOS 7 里真正的网络服务通常是 NetworkManager，可用 `nmcli`（NetworkManager Command Line Interface）管理；`ip` 命令来自 iproute2 工具包，正在全面取代 ifconfig/netstat 等老命令。

## 5.6 网络测试命令

**ping（Packet Internet Groper，因特网包探测器）**——检测连通性：

```bash
ping 192.168.3.1       # ping 网关
ping www.baidu.com     # ping 域名（顺带验证 DNS）
# Linux 下 ping 默认不自动停止，按 Ctrl + C 结束
```

**netstat（Network Statistics，网络统计）**——检查服务端口：

```bash
netstat -nultp
# n 数字形式显示地址  u UDP  t TCP  l listen（监听状态）  p 显示进程
```

服务器对外提供服务必须开放对应端口；`netstat -nultp` 可显示所有处于监听（LISTEN）状态的 TCP/UDP 连接，用来确认服务是否真的起来了。

**补充知识**：

- 新系统可用 `ss -nultp`（Socket Statistics）替代 netstat，功能相同更快。
- 排查路径问题可用 `traceroute`（路由跟踪）。
- 网络不通的标准排查顺序：`ip a`（本机 IP 对不对）→ `ping 网关`（本网段通不通）→ `ping 8.8.8.8`（外网通不通）→ `ping 域名`（DNS 是否正常）。

---

<a id="p4-6"></a>
# 第 6 章 防火墙与 SELinux

## 6.1 防火墙是什么

企业内网比公网安全，公网环境恶劣、服务器常被攻击，因此需要在内外网之间设置**保护屏障——防火墙（Firewall）**，如同家门口的防盗门。防火墙分软件/硬件两种，本质都是**依据策略（规则）对穿越防火墙的流量进行过滤**：

- 策略可基于**源地址、目的地址、端口号、协议、应用**等信息定制；
- 流量与某条规则匹配 → 执行对应处理；不匹配 → 丢弃；
- 从而保证只有合法流量在内网与公网之间流动。

> 例子：之所以访问不了 Google、Facebook 等国外网站，就是因为存在国家级的"长城防火墙"（GFW，Great Firewall）。

## 6.2 Linux 防火墙工具体系

| 系统 | 默认防火墙管理工具 |
|---|---|
| CentOS/RHEL 5、6 | iptables |
| CentOS/RHEL 7、8 | firewalld |

**关键认知**：iptables 和 firewalld **都不是真正的防火墙**，只是"防火墙策略的管理工具/服务"。真正的过滤动作由内核的 **netfilter**（网络过滤器）框架完成：

```
用户空间：iptables 服务 / firewalld 服务（配置规则）
        ↓ 把规则交给
内核空间：netfilter（对报文进行实际过滤处理）
```

**补充知识**：除 iptables/firewalld 外还有 nftables、ebtables 等工具。同一台服务器只需配置其中一种，其余关闭。

## 6.3 iptables 工作原理：五条链（链 chain = 报文必经的"关卡"）

当客户端访问服务器时，报文从网卡进入内核，按目标是否为本机走不同路径：

| 链 | 英文含义 | 时机 |
|---|---|---|
| PREROUTING | 路由前 | 报文刚进入网卡 |
| INPUT | 入站 | 判断**目标是本机**，进入本机服务前 |
| OUTPUT | 出站 | 本机服务发出响应 |
| FORWARD | 转发 | 判断**目标不是本机**，需转发给其他主机 |
| POSTROUTING | 路由后 | 报文离开前 |

两条典型路径：

1. **访问本机服务**：网卡 → PREROUTING →（目标是本机）→ INPUT → 内核 TCP/IP 协议栈 → 用户空间服务 → OUTPUT → POSTROUTING → 返回客户端。
2. **本机做转发**：网卡 → PREROUTING →（目标不是本机）→ FORWARD → POSTROUTING → 转给其他主机。

每条链上挂着**一串规则**，报文经过时要逐条匹配；规则由"匹配条件 + 动作"构成：

- **匹配条件**：源地址、目的地址、传输协议（TCP/UDP）、服务类型（端口）等；
- **动作（三种）**：
  - **放行 ACCEPT**：正常通过；
  - **拒绝 REJECT**：拒绝并回复"我不接受"（对方能感知被拒）；
  - **丢弃 DROP**：直接扔掉，不回应（对方表现为超时）。

**补充知识：四表**

规则按功能分属于四张"表（table）"：filter 表（过滤，默认）、nat 表（地址转换）、mangle 表（改包）、raw 表（跳过跟踪）。具体配置命令属于进阶内容，本课程只讲原理。

## 6.4 SELinux

**SELinux = Security-Enhanced Linux（安全增强型 Linux）**：由美国国家安全局（NSA，National Security Agency）主导开发的安全框架，对系统上的每一个进程、文件、用户访问都做强制控制，利用安全策略强制执行"策略允许的访问"。

三种运行模式：

| 模式 | 行为 | 使用建议 |
|---|---|---|
| Enforcing（强制） | 严格按策略执行，违规就报错 | 最安全但容易"动不动报错"，实际很少用 |
| Permissive（宽容/警告） | 违规只记录警告、不拦截 | **运维常用**，兼顾安全与业务可用 |
| Disabled（关闭） | 完全关闭 SELinux | — |

**修改方式**：

1. 永久：编辑 `/etc/selinux/config`，把 `SELINUX=` 后改为对应模式，保存退出。
2. 临时切换（补充）：`setenforce 0`（切宽容）/ `setenforce 1`（切强制）。
3. 查看当前模式（补充）：`getenforce`。

> Linux 安全是一大专题，本章只需掌握"防火墙的原理与工具关系 + SELinux 三种模式"即可。

---

<a id="p4-7"></a>
# 第 7 章 运行级别与 root 密码重置

## 7.1 Linux 的 7 种运行级别（runlevel）

数据中心常有旧服务器，长时间无人登录后忘记了 root 密码。**重置 root 密码需要进入"单用户模式"，而单用户模式与运行级别直接相关。**

| 级别 | 名称 | 说明 |
|---|---|---|
| 0 | 关机 | `init 0` 即关机 |
| 1 | 单用户模式 | **无网络连接、不运行守护进程、不允许非超级管理员登录**——重置密码就在这里 |
| 2 | 多用户模式 | 无网络连接、不运行守护进程 |
| 3 | 完整多用户（字符界面） | 服务器最小化安装后的默认级别 |
| 4 | 保留 | 无意义 |
| 5 | 多用户图形界面 | 安装了桌面环境的系统进入此级别 |
| 6 | 重启 | `init 6` 即重启 |

切换命令：`init 3`（图形界面切到字符界面）、`init 0`（关机）、`init 6`（重启）。

**补充知识：systemd target 对照**

CentOS 7 改用 systemd（system daemon）管理，运行级别对应 target（目标单元）：

| SysV 运行级别 | systemd target |
|---|---|
| 0 | poweroff.target |
| 1 | rescue.target |
| 2 / 3 / 4 | multi-user.target |
| 5 | graphical.target |
| 6 | reboot.target |

## 7.2 CentOS 7 重置 root 密码完整步骤

**GRUB = GRand Unified Bootloader（统一引导加载程序）**，是开机后加载操作系统前的选择菜单。

1. 开机出现 GRUB 内核选择界面，**倒计时结束前按任意键**停留在该界面；
2. 按 `e` 键进入启动参数编辑模式；
3. 找到以 `linux16` 开头的那一行；
4. 删掉行中的 `rhgb quiet`（rhgb = Red Hat Graphical Boot 图形开机画面；quiet 屏蔽启动信息），改为 `rd.break`；
5. 按 `Ctrl + X` 启动，进入单用户（紧急）模式；
6. 重新以读写方式挂载根：
   ```bash
   mount -o remount,rw /sysroot
   ```
7. 切换到真正的系统环境（**chroot** = change root，切换根目录）：
   ```bash
   chroot /sysroot
   ```
8. 重置密码：
   ```bash
   passwd root        # 输入两次新密码，如 123123（演示用）
   ```
9. 若系统原本开启了 SELinux，必须创建隐藏文件触发重新打标签，否则新密码可能无法登录：
   ```bash
   touch /.autorelabel    # 文件名千万不能写错
   ```
10. 输入两次 `exit` 退出并重启，之后用新密码登录。

**补充知识**

- CentOS 6 进入单用户的方式不同（在 GRUB 界面按 `a`，在行尾加 `1` 或 `single` 回车），思路一致。
- `rd.break` 停在的其实是一个内存中的紧急 Shell，根位于 `/sysroot` 且默认只读，所以必须 remount,rw。
- 现实影响：既然任何人都能借此改 root 密码，机房物理安全和 GRUB 密码保护才如此重要（见第 13 章物理安全规定）。

---

<a id="p4-8"></a>
# 第 8 章 SSH 远程登录管理

## 8.1 为什么需要 SSH

服务器放在机房，日常管理方式有两种：

1. **带外管理**：浏览器打开服务器远程管理地址（iDRAC/iLO），拉取服务器画面——操作繁琐；
2. **SSH 远程登录**（主流方式）：直接连进服务器命令行。

**SSH = Secure Shell（安全外壳协议）**：

- 默认使用 **TCP 22 端口**；
- 全程加密（对比 Telnet 明文传输，SSH 更安全，Telnet 已被淘汰）；
- 通过服务器的 **IP 地址 + 用户名/密码（或密钥）** 登录。

**Linux 服务器端默认已开启 SSH 服务**（守护进程名为 sshd，SSH daemon），前提是：服务器配好 IP、防火墙放行 22 端口。

## 8.2 常用客户端工具

| 平台 | 工具 | 说明 |
|---|---|---|
| Windows | **Xshell** | 国内常用，配套 Xftp 传文件 |
| Windows | **SecureCRT** | 经典商业软件，支持密钥生成 |
| Windows | **PuTTY** | 免费轻量（补充） |
| macOS / Linux | 自带终端 | 直接敲 `ssh root@192.168.3.108` |

## 8.3 方式一：密码认证登录

```bash
ssh root@192.168.3.108
# 提示输入服务器 root 密码，输入后进入远程终端
# 此时执行 ifconfig 看到的就是服务器自己的 IP
```

操作要点：

- 登录前先用 `ping` 确认客户端与服务器网络通（客户端在 cmd 中 `ping 192.168.3.108`）；
- Xshell/CRT 中可**新建会话**保存主机 IP（端口 22、协议 SSH），勾选记住用户名密码，下次双击直连；
- Mac 终端首次连接会问 `Are you sure you want to continue connecting?`，输入 `yes`。

## 8.4 方式二：密钥免密登录（重点）

**原理**：客户端生成一对密钥——**公钥（public key）+ 私钥（private key）**。公钥放到服务器上，私钥留在客户端。登录时服务器用公钥发起数学挑战，只有持有私钥的客户端能正确应答，全程不需要密码。

**文件约定**：

| 文件 | 位置 | 权限 | 说明 |
|---|---|---|---|
| `id_rsa` | 客户端 `~/.ssh/` | 600 | 私钥，绝不能外泄 |
| `id_rsa.pub` | 客户端 `~/.ssh/` | — | 公钥 |
| `authorized_keys` | **服务器**目标用户家目录 `~/.ssh/` | **600** | 认证密钥文件，存放允许登录的公钥 |

> 密钥文件权限不是 600 会登录失败，配置完务必检查。

### Linux/Mac 客户端三步搞定

```bash
# 1. 生成密钥（一路回车全默认即可）
ssh-keygen

# 2. 一条命令上传公钥并自动配置（推荐）
ssh-copy-id -i ~/.ssh/id_rsa.pub root@192.168.3.108
# 该命令会自动把公钥追加到服务器 ~/.ssh/authorized_keys

# 3. 直接登录，不再要密码
ssh root@192.168.3.108
```

### Windows 客户端（SecureCRT 生成密钥）

1. 工具栏 → 创建公钥（Create Public Key）；
2. 算法选 RSA（Rivest–Shamir–Adleman 非对称加密算法）或 DSA（Digital Signature Algorithm），口令可留空；
3. 长度 1024 或 2048 位：越长越安全但越慢，默认即可；
4. 密钥保存在本机 `~/.ssh/` 下（Identity 私钥 + .pub 公钥）；
5. 用 Xftp 之类的工具把 `id_rsa.pub` 上传到服务器，再把内容追加进服务器的 `~/.ssh/authorized_keys`（`cat id_rsa.pub >> ~/.ssh/authorized_keys`）；
6. 新建连接 → 属性 → 认证方式选"公钥（PublicKey）"，选择**私钥**文件登录（注意：客户端登录用的是私钥，公钥在服务器端）。

**补充知识：SSH 服务端配置与传输命令**

- 服务端配置文件：`/etc/ssh/sshd_config`（可改端口 `Port`、是否允许 root 登录 `PermitRootLogin`、是否允许密码登录 `PasswordAuthentication` 等，改完 `systemctl restart sshd`）。
- 传文件：`scp`（Secure Copy，如 `scp a.txt root@192.168.1.1:/tmp/`）或 `sftp`（SSH File Transfer Protocol）；Windows 图形化工具用 Xftp。
- 首次连接自动记录的指纹存在客户端 `~/.ssh/known_hosts`，服务器重装后会提示指纹变化。
- 安全建议：生产环境尽量用密钥登录并关闭密码认证，可极大降低被暴力破解的风险。

---

<a id="p4-9"></a>
# 第 9 章 综合布线基础

## 9.1 什么是综合布线

**综合布线（PDS，Premises Distribution System，也称通用布线系统 Generic Cabling System）**：按照**标准化、统一、简单、结构化**的方式编制和布置建筑物/数据中心内的通信线路，是数据中心基础设施的子系统之一。

覆盖范围包括：**网络系统、电话系统、监控系统、电源系统、照明系统**等（本课程聚焦**网络综合布线**）。

**总体要求**（五个特性）：

- **实用性**、**可扩展性**、**经济性**、**灵活性**、**可靠性**

**质量总要求**（四句话，记住）：**横平竖直、整齐划一、强电弱电分离、线缆测试全部通过。**

**收线（收敛）原则**：核心层设备向汇聚层设备收敛，汇聚层设备向接入层设备收敛；余线（多余线缆）盘在机柜中，**桥架上不允许出现多余线缆**。

**补充知识：三层网络架构**

课程提到的核心层/汇聚层/接入层是数据中心经典架构：接入层（Access Layer，连接服务器）、汇聚层（Aggregation/Convergence Layer，汇聚接入流量）、核心层（Core Layer，高速转发骨干）。流量自下而上汇聚，布线也按此收敛。

## 9.2 两大传输介质

### 双绞线（网线）

**双绞线（Twisted Pair）**：8 根铜线两两绞合，成对抵消电磁干扰。

| 类别 | 全称 | 带宽 | 典型速率 | 现状 |
|---|---|---|---|---|
| Cat5 | Category 5 | 100MHz | 100Mbps | 已淘汰 |
| Cat5e（超五类） | Category 5 enhanced | 100MHz | 1000Mbps | 百兆/千兆短距，课程中用于管理网 |
| Cat6（六类） | Category 6 | 250MHz | 1000Mbps（万兆可达 55m） | 课程中内网标准 |
| Cat6a | Category 6 augmented | 500MHz | 10Gbps | 万兆主流 |
| Cat7 | Category 7 | 600MHz | 10Gbps | 屏蔽系统 |

- **UTP（Unshielded Twisted Pair，非屏蔽双绞线）**：机房最常用；
- STP（Shielded Twisted Pair，屏蔽双绞线）：抗干扰强、布放要求高。

**补充知识：T568B 线序与直通线/交叉线**

网线两端水晶头（RJ45，Registered Jack 45）压线有两种标准线序：T568A 与 T568B（T568B 从左到右：白橙、橙、白绿、蓝、白蓝、绿、白棕、棕）。

- **直通线（straight-through cable）**：两端同为 T568B。测线仪两端指示灯按 1→8 顺序依次同步亮。普通业务线都是直通线。
- **交叉线（crossover cable）**：一端 T568A、一端 T568B。测线仪 B 端灯序为 8→1 反向。早年用于同类设备互联，现代网卡支持自动翻转（Auto MDI-X），已很少用。

**补充知识：强电弱电分离**
动力电（220V/380V，强电）与网络信号线（弱电）必须分开走线，避免电磁干扰和安全事故——这就是质量总要求中"强电弱电分离"的含义。

### 光纤（Fiber Optic）

用光信号传输，损耗低、距离远、不受电磁干扰。

| 类型 | 全称 | 常用波长 | 外皮颜色 | 适用距离 |
|---|---|---|---|---|
| 多模光纤 | MMF（Multi-Mode Fiber） | 850nm（纳米） | 橙色/湖蓝 | 短距离（数百米，楼内/机房间） |
| 单模光纤 | SMF（Single-Mode Fiber） | 1310nm / 1550nm | 黄色 | 长距离（数十公里） |

相关部件：

- **尾纤（pigtail）**：一端带接头、一端用于熔接的短光纤；
- **跳线（patch cord / jumper）**：两端带接头的成品短纤；
- **光模块（光收发一体模块，transceiver）**：常见封装 SFP（Small Form-factor Pluggable）、QSFP（Quad SFP）；
- **ODF（Optical Distribution Frame，光纤配线架）**：光缆成端、跳接的配线设备；
- 连接头类型：LC、SC、ST、FC（数据中心最常见 LC）；
- 损耗单位 **dB（decibel，分贝）**，越小越好。

---

<a id="p4-10"></a>
# 第 10 章 线缆布放流程与规范

## 10.1 综合布线总流程（七步）

```
布线规划表 → 清点耗材 → 线材规划 → 写号 → 布线（先网线后光纤）→ 插接端口 → 引线绑扎 + 粘贴标签
```

**第一步：布线规划表**。由客户或网络架构工程师给出，明确标明：哪个设备的哪个端口连到对端哪个设备的哪个端口、用什么类型线缆。各家公司的布线表样式不同（有的按"H3C 交换机 G0/X 口 → 某服务器某口"标注，有的按"机位 → 接入交换机端口号"标注），核心是看**线缆两端各连接到什么位置**。

**第二步：清点耗材**。确认网线、光纤、模块等耗材的**数量和型号与工单一致**，能满足本次布线需求。

**第三步：线材规划**。为了经济性，把合适长度的线用在合适的地方：

| 布线场景 | 建议网线长度 |
|---|---|
| 本机柜内 | 3 米以内 |
| 相邻机柜 | 3–5 米 |
| 跨机柜 | 5–7 米 |
| 跨多个机柜 | 7–20 米不等 |

做法：按类别和长度把线分类摆放，统计每种长度共需多少根；根据布线图统计每个机柜需要多少根光纤/网线，再按长度给每个机柜配线，并检查是否够长。

**第四步：写号**。给线缆**两端做同样的标记**，防止布线后不知道另一端是哪根线。要点：

- 从长到短、按顺序、两头依次书写；
- 不要用白板笔（会被抹掉），要用永久性记号笔；
- 字迹规整清晰，**特别注意 6 和 9 要区分开**。

**第五步：布线**。既有网线又有光纤时，**先布网线，再布光纤**。

**第六步：插接端口 + 引线绑扎；第七步：粘贴标签**（见第 11 章）。

## 10.2 网线布放规范

**顺序规范**：

- 机柜内服务器、交换机布线顺序：**从下往上**；
- 连接交换机的端口顺序：**从小到大**；
- **机位编号顺序与交换机端口编号顺序保持一致**：如 A01-1 服务器接交换机 1-0-5 端口，则 A01-2 接 1-0-6，依次往后；
- **每个机柜布放 1 根备用网线**，长度要能满足整个机柜所有服务器连接使用；布放光纤的机柜**另备 1 根光纤（备纤）**，长度满足交换机所有端口使用；
- 同一机柜同时有光纤和网线时**必须分离**：如网线放机柜左侧，光纤放右侧。

**工艺规范（数字要记牢）**：

| 项目 | 标准 |
|---|---|
| 绑扎（扎带）间距 | 不大于 **10cm**，且间距均匀一致 |
| 与走线夹固定间距 | 不大于 **2m** |
| 弯曲半径 | 大于线缆直径的 **8 倍** |
| 线缆预留长度 | 约 **60cm**（以机柜宽度为准） |
| 走线形态 | 平直，不得出现"牛角"状弯折、打圈 |
| 机柜内走线 | 自然垂直向下，内侧分线到各机位，垂直无缠绕、无长短不一 |
| 扎带选择 | 网线用**白色尼龙扎带**；剪扎带要平剪（防止划手），扎带头朝同一方向、不朝外 |

**不合格示例**（课程列举）：三种线缆交叉混合、扎带间距不一致、扎带朝向不一致。

## 10.3 光纤布放规范

光纤比铜缆脆弱，规范更严格：

- 捆扎用**柔性扎带（黑色魔术贴/理线贴，hook & loop）**，不用尼龙扎带勒伤光纤；
- 线缆自然平直，不得"牛角"弯折打圈，不受外力挤压损伤；
- **没有走线槽时必须穿 PVC 管（Polyvinyl Chloride，聚氯乙烯）保护**，PVC 管与光纤接口处做**防割处理**；
- 光纤与桥架连接处增加**软管**保护；
- 未接光口的**尾纤必须套上光纤帽**防尘防损；
- 桥架下线**不允许 90° 直角弯折**，要用斜面或圆弧过渡；
- 多余长度光纤在桥架上盘整时需**四点固定**，防止牵引时局部弯折、半径过小。

**实操补充（实训基地演示要点）**：

- 一个机柜通常有一台光纤交换机 + 一台电口交换机，管理本柜及邻近机柜设备；
- 光纤走黄色 PVC 管，网线可直接走线槽；本机柜布线网线可柜内收线，**光纤必须先上桥架再下来**；邻柜/跨机柜布线所有线缆都要"上桥架—下来—再上去—再下来"；
- 交换机端理线：网线先穿**理线架（理线器）**再插入端口，插之前**看清端口编号**（有的交换机 1 号口在上一排、有的在下一排，即"上单下双/下单上双"），余线往下收不留多余，所有线保持相同弧度；
- 捆扎位置要与设备位置对应：管理哪台设备就从对应高度引出，不从最上/最下面引出。

> 提示：若到非自有机房（运营商机房）布线，**务必提前与运营商沟通其布线要求**并严格执行，否则可能被要求返工。

---

<a id="p4-11"></a>
# 第 11 章 标签粘贴规范

## 11.1 标签的作用与制作规范

标签是后期网络运维时**快速定位**线缆的唯一依据。所有线缆——**网线、电源线、光纤——都必须在两端粘贴标签**。

- 形状：常用的为**旗形标签**（贴在线缆上呈小旗状）；
- 颜色：多种多样，一般用颜色**区分线缆功能**（如按所连接服务器类型区分网线）；不少公司只要求颜色统一即可；
- **电源线一般采用白色标签**。

## 11.2 标签命名规则（重点）

- 两端标签**命名必须一致**，且**包含两端信息**；
- 标签信息依据**资源表**中各端口信息制作。

**命名示例解读**：

```
CDNWH-CM21-2F206-A列1柜1设备
```

| 片段 | 含义 |
|---|---|
| CDNWH | 武汉 CDN（Content Delivery Network，内容分发网络）机房 |
| CM21 | 中国移动（CM，China Mobile）21 号机列 |
| 2F | 2 楼（Floor） |
| 206 | 206 房间 |
| A列1柜1设备 | A 列第一个机柜的第一台设备 |

**课程示例**：某黄色标签两行的含义为——"襄阳 CDN 5 排 12 号机柜交换机的 1-0-3 端口，连接到 5 排 11 号机柜的 A03 号设备"。即一端写"本端位置"，另一端写"对端位置"，看到任何一端都能定位整条链路。

## 11.3 标签打印

- 批量打印：大型标签打印机；少量打印：手持便携式标签打印机；
- 标签纸规格必须与打印机型号匹配（两种打印机的标签纸不通用）；
- 制作和打印使用打印机配套的软件完成。

## 11.4 粘贴规范

**服务器端**：

1. 先认清标签信息与颜色，**不要贴错**；
2. 粘贴位置：**距离端口约 5 厘米处**；
3. 标签角度必须**与光模块垂直**（上排朝上、下排朝下，粘贴在同一位置成一条直线）；
4. 不能露白、不贴歪，两端粘贴整齐（防止被空调风吹开口变形）。

**交换机端**：

- 标签角度垂直于线缆中部，所有标签连成一条直线；
- 不熟练时可先用记号笔在线缆上做好位置标记再贴；
- **千万不要漏贴**（贴完才发现少一张最麻烦）；
- 贴前看清交换机端口类型和序号（上单下双还是下单上双），**避免贴反导致全部报废**；
- 交换机位于机柜出风口上方，标签易受风干扰，务必完全粘合，避免开口、变形。

**贴错的处理**：少量贴错能改就马上改；不能改的立即与项目负责人协商重新打印，避免大面积更改影响美观。不要盲目追求速度，要在看清标签的前提下又快又好地完成。

---

<a id="p4-12"></a>
# 第 12 章 综合布线项目验收标准

布线完成后需按以下四类标准验收。运维工程师一要**用标准要求自己**，二要在第三方布线团队进自己机房时**用标准验收别人**。

## 12.1 施工工艺标准（铜缆与光纤）

**铜缆（网线）部分**：

| 项目 | 控制要求 |
|---|---|
| 机架内铜缆尾部长度 | **50cm ± 2cm**，且满足连接到本机位任意网卡位置 |
| 理线架到交换机长度 | **15–20cm** |
| 机柜内线缆归属 | 同一机柜的线缆必须放在同一机柜内；跨机柜线缆走桥架 |
| 折弯 | 不允许出现 **180° 对折** |

**光纤部分**：

| 项目 | 控制要求 |
|---|---|
| 光纤尾部（接交换机） | 接入模块后余量 **10–20cm** |
| ODF 端（光纤配线架） | 接入模块后余量 **10–20cm** |
| 路由 | 走专用桥架，用尼龙扎带捆扎固定；不允许 180° 对折 |

**数量核对**：依据布线需求核对机位内铜缆、光缆数量是否与施工设计一致；特殊布线需求（如预提交约定的路由）其线缆数量、标签位置也要与设计一致。

施工标准还包括：服务器上架、电源线捆扎、网线捆扎、电源线标签、服务器电源扣、机架卫生等（各公司有详细表格）。

## 12.2 线缆测试标准与方法

**性能标准（课程通用参考）**：

| 项目 | 标准 |
|---|---|
| 内网铜缆 | 六类（Cat6）UTP 非屏蔽线，满足**千兆**要求 |
| 管理网铜缆 | 超五类（Cat5e）非屏蔽网线，满足**百兆**要求 |
| 光纤：内网尾纤上联 | 损耗 ≤ **2dB** |
| 光纤：多模预端接光缆 | 波长 850nm，线路损耗 ≤ **1.5dB** |
| 光纤：单模预端接光缆 | 波长 1310nm，线路损耗 ≤ **3dB** |

**双绞线测试（测线仪）**：

- 直通线（普通业务线）：A 端 1→8、B 端 1→8，两端指示灯**顺序相同**；
- 交叉线：A 端 1→8、B 端 8→1，两端指示灯**顺序相反**。

**光纤测试**：

- **红光笔（VFL，Visual Fault Locator，可视故障定位仪）**：A 端发光，B 端能观察到光即通；
- **光功率计（optical power meter）**：精确测损耗，课程参考值为衰耗 0–0.75dB（多模典型；单模损耗更低，典型约 0–0.35dB）。

## 12.3 标签与端口验收

- 所有标签必须与原需求信息**一致**、**格式统一**；
- 铜缆与所接入的交换机端口对应关系，必须与资源信息表 **100% 匹配**；
- ODF 系统所有通路的标签对应关系，与资源信息表 **100% 匹配**；
- 按施工需求形成**光纤资源信息表、铜缆资源信息表**等文档。

## 12.4 机房环境验收

| 项目 | 要求 |
|---|---|
| 清洁 | 地面、机柜内、架顶、机柜前后门无尘土杂物 |
| 网线 | 剪除多余长度，清除机房任何地方的网线头 |
| 飞线 | 机柜顶部无飞线（飞线指未走线槽/桥架的裸拉线） |
| 绑扎 | 所有线缆全部绑扎 |
| 备线 | 所有备线收到机架顶处，捆扎固定在架顶 L 立柱旁 |
| 标签 | 机架位标签、线缆标签粘贴牢固 |

一句话总结：**离开之前，干净整洁。**

---

<a id="p4-13"></a>
# 第 13 章 数据中心管理规定

> 以下为根据大型互联网企业和银行数据中心归纳的通用版制度。各公司数据中心会制定自己的规定，**以所在机房实际规定为准**。

## 13.1 总体管理规定（四条红线级要求）

1. **人员进出全部需要授权**，禁止未经授权的人员和设备进出机房；运营商、设备厂商人员在机房内操作时，**驻场工程师需全程陪同**。
2. 机房任何区域**严禁吸烟**；机房内部、仓库内部**严禁饮食**；不带液体进机房；**非授权设备严禁操作**（仅授权人员允许操作已授权设备）；**严禁无工单的任何操作**。
3. **禁止携带包裹进入**；**禁止未经授权拍照**；禁止私接电器使用机柜电源。
4. 机房和仓库**门禁必须随时保持关闭**。

## 13.2 机房值班管理制度（九条）

1. 严禁无关人员进入机房；
2. 维护终端和网管主机**专机专用**，不得安装游戏等无关软件；
3. 做好数据安全保护，未经批准不得擅自查看、修改网络数据和用户数据；
4. 不能随意关闭设备告警；
5. 不得人为切断或私人占用电话，保证机房测试电话和值班电话畅通；
6. 个人通讯工具 24 小时正常开机；
7. 遵守故障和投诉处理规定：准确迅速处理申告和故障，及时查询、汇报、核对，不得以任何理由推诿、拖延；
8. 及时准确填写值班日志和各种指定报告；
9. 两人同时值班时，应指定一人负责值班期间的全面工作。

## 13.3 物理安全管理规定

1. **人员进出**：入室时间根据工作内容申请；入室权限遵循**最小化原则**并定期审核；做好人员进出登记和现场管理。
2. **物品进出**：禁止携带危险物品、具备拍照摄像功能的工具进入机房；特殊物品需审批（运维人员签字确认、全程陪同）；未经允许不准携带设备进出生产机房。
3. **车辆管理**：外部车辆不允许进入数据中心，特殊原因需提前申请报备。
4. **门禁卡管理**：**一人一卡、本人使用、不得转借**；卡证权限严格按权限审批；遗失及时挂失补办。
5. **监控管理**：各出入口、IT 设备间、库房要求 **7×24 小时无盲点监控**（进入设施间等区域不强制）；监控画面清晰可视；**监控记录保存 90 天以上**。

## 13.4 信息安全管理规定

1. 所有办公终端正确安装防病毒系统，及时更新病毒库；
2. 及时安装系统补丁；
3. 不得私自装配使用可读写光盘、磁带机、光盘机、USB 硬盘等外部存储设备；
4. 办公终端不得私自转给他人使用（防信息泄露、数据破坏）；
5. 移动办公终端外出办公时不得处于无人看管状态；
6. 不得私自安装盗版软件和与工作无关的软件；
7. 不允许向外部发送涉及公司秘密、机密和绝密的信息。

## 13.5 机房安全施工规定（十条）

1. 施工现场必须做好安全措施再施工；
2. 无安全措施不允许带电作业；
3. 没有使用安全防护用品不允许操作；
4. 不准使用不安全的设备；
5. 技术考核不通过不准独立作业；
6. 临时用工不准私招乱雇；
7. 不准酒后作业；
8. 计划用工未经培训发证不准上岗；
9. 不准擅自更改工程设计；
10. 机房仓库内不准抛掷工具和耗材。

## 13.6 红线与禁令

**红线**（一旦触碰必受处罚）：

1. **无工单操作**：无工单操作必须有邮件申请确认；
2. 操作前必须核实工单设备信息（机房号、机柜前后编号、位置号、SN 号等）；
3. **严禁触碰操作工单以外的设备**；
4. 禁止任何人未经授权私自用网线、光纤互联设备；
5. 禁止机房现场运维人员脱岗。

**安全禁令**：

1. 严禁不穿戴或未正确穿戴安全绝缘防护用品作业；
2. 严禁在未检查并落实现场安全措施的情况下作业；
3. 严禁使用不安全、不合格的工具器材作业；
4. 严禁酒后作业；
5. 严禁违规、不按工作程序操作。

---

<a id="p4-14"></a>
# 第 14 章 数据中心日常运维流程

## 14.1 运维工作总框架：一切以工单为准

无论是设备故障处理还是上下架，都以**工单（work order / ticket）**驱动：

```
需求方在平台发起工单（客户或同事；工单平台或邮件）
        ↓
驻场工程师接收工单 → 判断是否涉及"非操作类流程"
        ├─ 是 → 匹配相应非操作类流程（如人员进出、拍照审批等）
        └─ 否 → 与发单人确认是否可以开始（需要时）→ 匹配操作流程
        ↓
执行操作（遇到问题及时反馈；客观原因无法解决的与需求方沟通后挂起工单）
        ↓
操作完成 → 自检质检 → 与客户确认 → 结束工单 → 客户评价
```

**职责划分**：发起工单、确认操作、验收评价是**需求方**的工作；中间的工单处理是**现场运维工程师**的工作。除工单外，**日常巡检与定期盘点**也是运维工程师的常规工作。

## 14.2 人员进出管理流程

**常规流程**：

1. 发送人员进出需求工单 + 提交人员入职申请（姓名、电话、邮箱、身份证号等）；
2. 运营商进行授权审核，确认人员入职授权；人员类别包括客户方、现场方、第三方工程师等；
3. 在授权时限内到达机房（**超时需重新提交授权**）；
4. 核对人员信息并登记；
5. 学习数据中心安全管理条例；
6. 值班工程师确认来访人员是否具备操作权限及权限范围；
7. 具备权限方可进入机房操作，操作全程**运维工程师陪同**；
8. 完成后归还门禁卡，登记离开时间。

**紧急入室流程**：

1. 第一时间将紧急入室情况反馈客户接口人；
2. 客户接口人确认紧急情况及人员信息，将授权信息发至现场；
3. **未收到客户确认信息前，绝对禁止人员进入机房操作**；
4. 与运营商核实授权信息，对入室人员身份证拍照留底；
5. 登记后进入机房操作，全程陪同。

> 教训提示：紧急≠免流程。再急也要走完流程，否则出问题责任就是现场工程师的。

## 14.3 例行巡检流程

**目的**：及时发现机房故障、第一时间快速处理，确保机房安全平稳运行。

- **频率**：各机房不同，每天 1–2 次不等；
- **内容**：机房温湿度、机柜电力是否正常、IT 设备运行情况（服务器、交换机、防火墙、路由器）、各设备组件与线缆状态；
- 发现紧急情况 → 立即走紧急通报流程（见 14.4）；发现异常 → 记录；巡检完成 → **填写巡检报告**。

## 14.4 紧急情况通报流程

**触发条件**：空调故障、机柜掉电、回风温度 ≥ 28℃、机房照明掉电。

**处理步骤**：记录故障位置和影响区域 → 联系运营商 → 电话通知客户方协调处理。**现场工程师在无授权情况下不得私自处理。**

**通报时限**：

| 紧急情况 | 通报时限 |
|---|---|
| 机架掉电 | **10 分钟内** |
| 温度过高 | **15 分钟内** |

**通报方式**：电话 + 邮件。

**通报内容**：发现时间、具体位置、影响范围、故障原因、处理方法、预计恢复时间；备注故障原因并**拍照记录**；反馈是否需要调运营商处理。

**恢复之后**：检查设备硬件是否损坏，邮件反馈客户方。

## 14.5 拍照管理流程

- 数据中心机房**默认禁止拍照**；有需求必须走拍照申请流程；
- 合法需求：现场运维过程中必要的取证拍照、客户或合作单位的拍照需求；
- 现场需求由现场提出**书面申请**；客户需求需接到**工单或书面通知**后方可拍照；
- 拍照后需**备案**，照片使用完毕**要求删除**，**严禁泄露**。

---

<a id="p4-15"></a>
# 第 15 章 服务器操作流程

> 所有流程的共同骨架：**客户发单 → 工程师核对信息 → 按工单操作 → 关键步骤与发单人确认 → 完成反馈 → 客户确认后结单**。以下展开典型场景。自家机房则"客户"即发起工单的内部同事。

## 15.1 新服务器到货上架流程

1. 客户发起到货上架通知，驻场工程师收到工单并确认上架内容；
2. 前期准备：发起人员入室工单、设备搬入工单、加电工单；
3. 到货当天验收：**监督厂商在指定区域拆箱**，拆箱问题拍照记录反馈；
4. 检查服务器外观和数量；
5. **硬件检查**：配置是否与出厂要求一致（抽检比例一般为到货数量的 **10%**）；
6. **测电（通电测试）**：拉到测试区测试（比例 **100%**）；
7. **软检**：检查 BIOS（Basic Input/Output System）配置是否与出厂要求一致（比例 **10%**）；
8. 测试有问题 → 通知厂商解决 → 重新抽检和测电；
9. 全部通过后监督厂商人员**上架绑线**，完成后进行**绑线验收**（重点：电源线是否插在 **A/B 双路**）；
10. 与客户确认可以加电 → 联系运营商进行机柜加电；
11. 加电后验收，填写**验收报告**发送给工单发起人；
12. 再次与客户确认，结束工单。

## 15.2 服务器上线流程

1. 客户发起上线工单，工程师检查工单内容（哪些设备、什么位置）；
2. 确定目标服务器和目标机位是否符合上架条件；
3. 上架到指定位置；机位未绑扎电源线的按要求重新绑好；
4. 旧服务器重新上线需按要求**重装系统**；
5. 检查网络连通性，测试能否正常访问外网；
6. 遇到问题立即反馈；无问题与客户沟通确认后结单。

## 15.3 服务器故障处理流程

**通则**：收到工单先确认操作信息；操作前确认设备 **UID 指示灯**（Unit Identification Light，设备定位灯，由客户远程点亮后现场才能准确定位机器——"亮灯的就是这台"）。

### 更换硬盘（最常见）

1. 确认 UID 灯已点亮、**确认故障硬盘槽位（千万不要拔错盘）**；
2. 与发单人确认使用哪台设备的备盘，还是仓库新盘；
3. 更换时注意新盘**参数与原盘一致**：型号、容量、转速（RPM，Revolutions Per Minute）等；
4. 更换后观察硬盘指示灯状态；
5. 将结果告知发单人，故障盘交由厂商报修；
6. 发单人确认后结单。

### 更换内存（Memory）

1. 确认 UID 灯、确认设备为**关机状态**；
2. 拔电源：**先断一路（A 路），再拔另一路（B 路）**；
3. 设备下架 → 打开机箱 → 拆下导流罩 → 更换故障内存条；
4. 逆向还原：装导流罩 → 盖机盖 → 上架 → 连电源线 → 按原线序恢复网线光纤；
5. 加电后查看内存是否被识别；
6. 客户确认后结单。

### 更换网卡（NIC，Network Interface Card）

1. 确认 UID 灯与关机状态后拔电源（先一路后另一路）；
2. 下架开箱，拆掉挡板，安装网卡；
3. 盖机盖、上架、连接线缆；
4. 加电查看能否进入系统，联系客户确认后结单。

### 更换电源（PSU，Power Supply Unit）

1. 确认 UID 灯；移除电源线；
2. 拔出故障电源模块（**保证另一路在用电源正常连接**——这就是双路冗余的意义）；
3. 插入新电源模块、连接电源线；
4. 观察电源模块指示灯及设备告警灯是否异常；
5. 与发单人确认后结单。

## 15.4 在线服务器重启流程

1. 客户通过工单平台发起重启工单；
2. 工程师定位设备，检查服务器是否有报错；
3. 执行重启：
   - **软重启**：按 `Ctrl + Alt + Delete` 组合键；
   - **硬重启**：按住服务器电源按钮约 **6 秒**强制关机，再按一下电源键开机；
4. 正常启动后完成连通性检测；
5. 无问题与客户确认结单；
6. 若重启过程出现文件系统报错 → 进入**单用户模式做磁盘扫描**（见第 7 章）。

## 15.5 服务器下线、迁移与重装系统

**下线**：确认设备处于关机状态 → 移除网线、电源线 → 服务器下架 → 问题及时反馈 → 完成后客户确认结单。

**迁移**：确认设备状态 → 移除网线/光纤/电源线 → 迁移（同地或异地机房）→ 到新机房完成检查和验收后上架 → 加电后连通性检测 → **做资产变更记录** → 确认结单。

**重装系统**：确认目标服务器为可操作状态 → 按要求重装系统 → 完成连通性检测 → 问题立即反馈客户 → 完成后确认结单。

---

<a id="p4-16"></a>
# 第 16 章 网络设备操作流程

> 网络设备（交换机、路由器等）影响面比单台服务器大得多，操作务必**小心谨慎**：**每一次关键操作（拔线、断电、插线）都要与发单人/客户确认**。

## 16.1 到货上架流程

客户发起操作工单 → 工程师查看确认到货信息 → 到货前准备（人员设备入室工单、上架加电工单等）→ 到货验收（**监督厂商拆箱**）→ 监督厂商组装、测电、初始化和测试 → 根据工单监督厂商上架和入库 → 核实设备信息并反馈发单人 → 确认后结束工单。

## 16.2 加电流程

确认可以加电 → 插上电源线加电 → 与客户确认接线操作（**千万不要插错端口**）→ 线缆接入后与客户确认 → 操作完成后邮件或工单反馈确认 → 结束工单，客户评价。

## 16.3 整机替换流程

1. 客户发起整机替换工单，工程师核对工单信息、定位设备位置；
2. 向资产岗申请**同型号设备**，办理出库手续；
3. 对新设备初始化（需要预配置的先做好配置）；
4. 备机搬到故障设备位置；
5. **记录原设备线序** → 断开线缆 → 新设备上替换、加电；
6. 与客户确认后**逐步恢复线缆：先接管理网和上联线缆，再接下联（服务器）线缆，每一步都电话确认**；
7. 与客户确认电路正常 → 邮件或工单反馈 → 结单。

## 16.4 板卡故障处理流程

1. 客户发起工单，确定故障板卡位置；
2. 向资产岗申请同型号板卡，办理出库手续，**记录 SN（Serial Number，序列号）**并与客户确认；
3. **记录原板卡线序** → 移除线缆、光模块；
4. 更换板卡：故障板卡**放入防静电袋（ESD，Electro-Static Discharge 防护）**，取出新板卡、移除尾部保护板，替换上机；
5. 反馈客户检查板卡状态，等待识别通知；
6. 板卡正常后逐步恢复线缆，与客户确认；
7. 把 SN 发送给客户，确认后结单评价。

## 16.5 光模块及链路故障处理流程

1. 客户发起工单，核实信息、定位设备，与客户确认可以操作；
2. **光模块故障**：申领新模块办理领用手续，核对模块信息后更换；
3. **光纤链路故障**：有冗余光纤（备纤）直接替换使用；没有布放备纤则**重新布线**；
4. 完成后反馈，客户确认结单评价。

## 16.6 网络设备配置流程

客户发起工单 → 查看工单信息 → 定位设备位置 → 与客户确认可以开始 → 非初始化配置需**客户提供配置信息** → 写入新配置 → 配置有问题反馈发单人修正 → 无问题与客户确认结单。

---

<a id="p4-a"></a>
# 附录 A 术语中英文对照总表

## A.1 行业与硬件术语

| 缩写/术语 | 英文全称 | 中文含义 |
|---|---|---|
| IDC | Internet Data Center | 互联网数据中心 |
| IT | Information Technology | 信息技术 |
| O&M | Operation and Maintenance | 运维 |
| PC | Personal Computer | 个人电脑 |
| OS | Operating System | 操作系统 |
| FHS | Filesystem Hierarchy Standard | 文件系统层次标准 |
| BIOS | Basic Input/Output System | 基本输入输出系统 |
| CPU | Central Processing Unit | 中央处理器 |
| RAM | Random Access Memory | 内存 |
| HDD / SSD | Hard Disk Drive / Solid State Drive | 机械硬盘 / 固态硬盘 |
| SATA / SAS / SCSI | Serial ATA / Serial Attached SCSI / Small Computer System Interface | 硬盘接口类型 |
| NVMe | Non-Volatile Memory Express | 高性能固态盘接口协议 |
| NIC | Network Interface Card | 网卡 |
| PSU | Power Supply Unit | 电源模块 |
| RPM | Revolutions Per Minute | 硬盘转速（转/分钟） |
| SN | Serial Number | 序列号 |
| UID 灯 | Unit Identification (Light) | 服务器定位指示灯 |
| iDRAC / iLO / IPMI | Integrated Dell Remote Access Controller / Integrated Lights-Out / Intelligent Platform Management Interface | 服务器带外管理技术 |
| OOB | Out-of-Band Management | 带外管理 |
| PDU | Power Distribution Unit | 机柜电源分配单元 |
| UPS | Uninterruptible Power Supply | 不间断电源 |
| ESD | Electro-Static Discharge | 静电放电（防静电袋由此得名） |

## A.2 网络术语

| 缩写/术语 | 英文全称 | 中文含义 |
|---|---|---|
| IP / IPv4 / IPv6 | Internet Protocol / version 4 / version 6 | 互联网协议 |
| TCP / UDP | Transmission Control Protocol / User Datagram Protocol | 传输控制协议 / 用户数据报协议 |
| ICMP | Internet Control Message Protocol | 互联网控制报文协议（ping 用） |
| DHCP | Dynamic Host Configuration Protocol | 动态主机配置协议 |
| DNS | Domain Name System | 域名系统 |
| MAC | Media Access Control | 网卡物理地址 |
| HTTP / HTTPS | HyperText Transfer Protocol / HTTP Secure | 网页传输协议 / 其加密版 |
| FTP | File Transfer Protocol | 文件传输协议 |
| Telnet | — | 早期远程登录协议（明文） |
| SSH | Secure Shell | 安全外壳协议 |
| VLAN | Virtual Local Area Network | 虚拟局域网 |
| STP | Spanning Tree Protocol | 生成树协议（防环路） |
| OSPF | Open Shortest Path First | 开放式最短路径优先（路由协议） |
| ACL | Access Control List | 访问控制列表 |
| PXE | Preboot eXecution Environment | 预启动执行环境（网络装机） |
| GFW | Great Firewall | 防火长城 |
| lo | loopback | 回环网卡（127.0.0.1） |
| UUID | Universally Unique Identifier | 通用唯一识别码 |
| CDN | Content Delivery Network | 内容分发网络 |

## A.3 Linux 命令与文件术语

| 命令/文件 | 英文全称 | 含义 |
|---|---|---|
| tar | Tape Archive | 归档（打包） |
| gzip | GNU Zip | GNU 压缩工具（.gz） |
| useradd / userdel / usermod | add / delete / modify user | 建用户 / 删用户 / 改用户 |
| groupadd / groupdel / groupmod | add / delete / modify group | 建组 / 删组 / 改组 |
| passwd | password | 密码 |
| gpasswd | group password | 组管理 |
| su | switch user（习惯说法） | 切换用户 |
| sudo | superuser do（习惯说法） | 以管理员身份执行 |
| id | identity | 查看用户身份信息 |
| whoami | who am I | 当前用户名 |
| chmod | Change Mode | 改权限 |
| chown | Change Owner | 改所有者 |
| chgrp | Change Group | 改所属组 |
| umask | user file-creation mask | 默认权限掩码 |
| SUID / SGID | Set User ID / Set Group ID | 特殊权限 |
| ls / cd / cp / mv / rm | list / change directory / copy / move / remove | 基础文件操作 |
| mkdir | make directory | 创建目录 |
| cat | concatenate | 查看文件内容 |
| man | manual | 帮助手册 |
| fdisk | fixed disk | 分区工具 |
| partprobe | partition probe | 重读分区表 |
| mkfs | Make File System | 格式化 |
| fsck | File System Check | 文件系统检查 |
| mount / umount | mount / unmount | 挂载 / 卸载 |
| df | Disk Free | 磁盘空间查看 |
| fstab | File System Table | 开机挂载配置表 |
| LVM | Logical Volume Manager | 逻辑卷管理 |
| MBR / GPT | Master Boot Record / GUID Partition Table | 分区表格式 |
| ifconfig | Interface Configuration | 网卡配置查看（net-tools） |
| ip | —（iproute2 工具包） | 新一代网络配置命令 |
| netstat | Network Statistics | 网络连接统计 |
| ss | Socket Statistics | netstat 替代品 |
| ping | Packet Internet Groper（习惯说法） | 连通性测试 |
| systemctl | system control | systemd 服务管理 |
| service | — | SysV 时代服务管理 |
| GRUB | GRand Unified Bootloader | 引导加载程序 |
| init | initialization | 初始化进程（SysV） |
| systemd | system daemon | 系统管理守护进程 |
| runlevel | — | 运行级别 |
| chroot | change root | 切换根目录 |
| sshd | SSH daemon | SSH 服务端守护进程 |
| scp | Secure Copy | 安全复制 |
| sftp | SSH File Transfer Protocol | SSH 文件传输 |
| RSA / DSA | Rivest–Shamir–Adleman / Digital Signature Algorithm | 非对称加密/签名算法 |
| ext2/3/4 | Extended File System 2/3/4 | 扩展文件系统 |
| XFS | X File System | 高性能日志文件系统 |
| swap | — | 交换分区 |

## A.4 布线与机房术语

| 缩写/术语 | 英文全称 | 中文含义 |
|---|---|---|
| PDS | Premises Distribution System | 综合布线系统 |
| UTP / STP | Unshielded / Shielded Twisted Pair | 非屏蔽 / 屏蔽双绞线 |
| Cat5e / Cat6 / Cat6a | Category 5 enhanced / 6 / 6 augmented | 超5类 / 6类 / 6A类网线 |
| T568A / T568B | TIA/EIA-568 线序标准 A / B | 双绞线压线线序标准 |
| RJ45 | Registered Jack 45 | 网线水晶头 |
| SMF / MMF | Single-Mode / Multi-Mode Fiber | 单模 / 多模光纤 |
| nm | nanometer | 纳米（光波长单位） |
| dB | decibel | 分贝（光损耗单位） |
| ODF | Optical Distribution Frame | 光纤配线架 |
| SFP / QSFP | Small Form-factor Pluggable / Quad SFP | 光模块封装 |
| VFL | Visual Fault Locator | 红光笔/可视故障定位仪 |
| PVC | Polyvinyl Chloride | 聚氯乙烯（保护管材质） |
| LC / SC / ST / FC | — | 光纤连接器类型 |
| 工单 | work order / ticket | 运维操作凭证 |
| 驻场工程师 | on-site engineer | 常驻客户机房的运维工程师 |
| A/B 路 | A/B power feeds | 双路供电 |
| 飞线 | — | 未走正规线槽的临时裸线 |
| 理线架 | cable management panel | 整理网线的架件 |
| 桥架 | cable tray | 机房走线架 |

---

<a id="p4-b"></a>
# 附录 B 常用命令速查表

```bash
# ---------- 归档压缩 ----------
tar -zcf bak.tar.gz 文件...        # 打包并 gzip 压缩
tar -jcf bak.tar.bz2 文件...       # 打包并 bzip2 压缩
tar -Jcf bak.tar.xz  文件...       # 打包并 xz 压缩（体积最小）
tar -zxf bak.tar.gz                # 解压（bz2 用 -jxf，xz 用 -Jxf）
tar -tf bak.tar.gz                 # 不解压查看内容
tar -zxf bak.tar.gz -C /目标目录    # 解压到指定目录

# ---------- 用户与组 ----------
useradd -u 1003 -g kai -s /bin/bash dianwei   # 建用户并指定属性
userdel -r kai                      # 删除用户及家目录、邮件
usermod -u 1314 kai                 # 修改用户 UID
passwd kai                          # 设置密码
su - kai                            # 切换用户（带环境）
id kai                              # 查 UID/GID
groupadd -g 1010 game               # 建组
gpasswd -a kai game                 # 加用户进组（-d 移出）
getent passwd kai                   # 查用户配置行

# ---------- 权限 ----------
ls -l                               # 查看权限详情
chmod 755 目录                       # 数字法
chmod u+x,g-w,a+r 文件               # 字母法
chown kai:kai 文件                   # 同时改所有者和组
chgrp kai 文件                       # 只改组
umask                               # 查看默认权限掩码

# ---------- 磁盘 ----------
lsblk                               # 树状查看块设备
fdisk -l                            # 查看所有硬盘分区
fdisk /dev/sdb                      # 交互分区（n 建 p 打印 d 删 w 保存 q 退出）
partprobe /dev/sdb                  # 重读分区表
mkfs.xfs /dev/sdb1                  # 格式化为 XFS
mkdir /data                         # 建挂载点
mount /dev/sdb1 /data               # 临时挂载
umount /data                        # 卸载（先退出该目录）
df -Th                              # 查看挂载与空间
vim /etc/fstab                      # 永久挂载配置（6 字段，写错开不了机）
mount -a                            # 按 fstab 重新挂载全部

# ---------- 网络 ----------
ip a                                # 查看 IP（或 ifconfig）
vim /etc/sysconfig/network-scripts/ifcfg-eth0   # 网卡配置文件
#   BOOTPROTO=static  IPADDR=  NETMASK=  GATEWAY=  DNS1=  ONBOOT=yes
ifconfig eth1 192.168.10.10 netmask 255.255.255.0   # 临时配 IP
cat /etc/resolv.conf                # 查看DNS
cat /etc/hosts                      # 本地域名解析表
systemctl restart network           # 重启网卡服务
service network restart             # 5/6 兼容写法
ping 192.168.1.1                    # 测连通（Ctrl+C 停止）
netstat -nultp                      # 查看监听端口（新版：ss -nultp）

# ---------- 防火墙 / SELinux ----------
getenforce                          # 查看 SELinux 模式
setenforce 0                        # 临时切宽容（1 切强制）
vim /etc/selinux/config             # 永久修改 SELINUX=enforcing|permissive|disabled
systemctl stop firewalld            # 停防火墙服务（示例）

# ---------- root 密码重置（CentOS 7）----------
# 开机 GRUB 界面按 e → linux16 行把 rhgb quiet 改为 rd.break → Ctrl+X
mount -o remount,rw /sysroot
chroot /sysroot
passwd root
touch /.autorelabel                # SELinux 开启时必须
exit
exit

# ---------- SSH ----------
ssh root@192.168.3.108              # 密码登录
ssh-keygen                          # 生成密钥对
ssh-copy-id root@192.168.3.108      # 一键部署公钥免密登录
chmod 600 ~/.ssh/authorized_keys    # 权限不对无法登录
systemctl restart sshd              # 重启 SSH 服务
scp file root@192.168.1.1:/tmp/     # 传文件
```

---

<a id="p4-c"></a>
# 附录 C 课程自测题与参考答案

以下为课程各章结尾的作业题整理（部分为开放选项题，此处给出要点式答案）。

## C.1 Linux 系统管理（第 75 讲作业）

1. **CentOS 7 的网卡配置文件在哪个目录下？**
   答：`/etc/sysconfig/network-scripts/`（文件名形如 ifcfg-eth0 / ifcfg-ens33）。
2. **哪个命令可以查看磁盘剩余空间？**
   答：`df -h`（带文件系统类型用 `df -Th`）。
3. **哪个命令可以设置某个文件的读写权限？**
   答：`chmod`（数字法或字母法）。
4. **哪个用户是 Linux 的超级管理员？**
   答：root，UID 为 0。

## C.2 Linux 初始化（第 78 讲作业）

1. **CentOS 7 默认采用哪种防火墙？**
   答：firewalld（管理工具；真正的过滤由内核 netfilter 完成）。
2. **SSH 服务默认使用哪个端口连接？**
   答：TCP 22 端口。
3. **免密登录需要把密钥放在服务器的哪个目录？**
   答：目标用户家目录下的 `~/.ssh/` 隐藏目录，公钥内容追加进 `authorized_keys` 文件，且权限须为 600。
4. **哪个运行级别属于单用户模式？**
   答：1 级。

## C.3 综合布线（第 82 讲作业）

1. **光纤的一张标签至少应包含哪些信息？**
   答：能定位线缆两端的完整信息——机房/机列/机柜/机位/设备/端口，且两端标签命名一致（一端写本端位置，另一端写对端位置）。
2. **标签一般粘贴在距离线头几厘米处？**
   答：约 5 厘米（距端口约 5cm，且垂直于光模块/端口方向）。
3. **布线规范说法错误的是？（要点回顾）**
   正确的说法：绑扎间距 ≤10cm；走线夹固定间距 ≤2m；弯曲半径 ≥ 线径 8 倍；预留约 60cm；强电弱电分离；光纤网线分侧布放。凡与之相反即错误。
4. **综合布线整体验收的内容有哪些？**
   答：施工工艺标准（铜缆/光纤长度与路由）、线缆测试标准、标签与端口对应验收、机房环境验收四大类。

## C.4 数据中心管理规定（第 83 讲作业）

1. **哪些东西不允许带入机房？**
   答：食物、饮料等液体，烟草火种，包裹，危险物品，未授权的拍照摄像设备等。
2. **哪些操作在 IDC 里是被禁止的？**
   答：无工单操作、吸烟饮食、私接电器使用机柜电源、未经授权拍照、私自用线缆互联设备、触碰工单以外设备等。
3. **操作工单前要核对哪些信息？**
   答：机房号、机柜前后编号、位置号、SN 号等设备信息。
4. **信息安全说法错误的？（要点回顾）**
   正确要点：终端装防病毒软件并更新病毒库；及时打补丁；禁用可读写光驱/USB 硬盘等外设；不装盗版和无关软件；不外发涉密信息；终端不转借他人。凡与之相反即错误。

## C.5 运维流程（第 86 讲作业）

1. **关于工单操作说法错误的？（要点回顾）**
   正确要点：一切操作以工单为依据；操作前核对设备信息；关键步骤与发单人确认；完成后反馈确认结单。凡"可以不按工单操作""完成后无需确认"等即错误。
2. **哪些情况需要紧急通报？**
   答：空调故障、机柜掉电、回风温度 ≥28℃、机房照明掉电（机架掉电 10 分钟内通报，温度过高 15 分钟内通报）。
3. **李白想进机房维修服务器，说法错误的？（要点回顾）**
   正确要点：需先提交进出需求工单和人员信息获得授权；到机房核对登记、学习安全条例；有操作权限才能进入；操作需运维工程师全程陪同；未收到客户确认的紧急入室也禁止放行。凡"可以先进去再补流程""有人认识就不用登记"等即错误。
4. **关于服务器故障处理说法错误的？（要点回顾）**
   正确要点：换硬盘先确认 UID 灯和故障盘槽位、新盘参数（型号/容量/转速）一致；换内存/网卡先确认关机、按"先一路后一路"断电；换电源保证另一路正常供电。凡"硬盘随便拔""带电换内存"等即错误。

---

<a id="p4-end"></a>
# 结语：从初级到进阶

本课程 8 大章节（数据中心介绍、数据中心硬件介绍、数据中心网络基础、网络设备基础配置、服务器配置、服务器系统管理、综合布线、管理规范）到这里全部结束，对应初级运维工程师应掌握的技能。

**中级工程师方向（课程预告，可提前自学）**：

- **Linux 系统应用工程师**：PXE 网络批量装机的搭建与所需服务配置；
- **网络工程师**：VLAN、STP、OSPF、ACL 等网络设备协议与配置；
- 本笔记第 4 章提到的 LVM、第 6 章提到的 firewall-cmd 具体命令，也是进阶必修。

**给初学者的三点学习建议**：

1. **命令靠动手**：搭一台虚拟机（VMware/VirtualBox + CentOS 7），把附录 B 的命令全部敲一遍，光看记不住。
2. **数字靠口诀**：布线与管理规范里的关键数字（10cm 间距、8 倍弯曲半径、5cm 标签距离、90 天监控、10/15 分钟通报时限）建议做成卡片反复记忆。
3. **流程靠理解**：所有操作流程的骨架都是"工单 → 核对 → 操作 → 确认 → 结单"，理解了这一点，遇到任何新流程都能举一反三。

> 原课程结尾引《礼记·学记》："独学而无友，则孤陋而寡闻。"——一个人闷头学是行不通的，多与同伴交流、取长补短，才能真正提升认知。共勉。