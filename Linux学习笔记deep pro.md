# Linux 学习笔记

> 本笔记基于 GeekHour《30 分钟 Linux 入门教程》整理，并在原教程基础上补充了大量未涉及或未展开的知识点，方便系统学习和随时查阅。

***

## 目录

1. [Linux 简介](#一linux-简介)
2. [Linux 系统架构与组成](#二linux-系统架构与组成)
3. [Linux 发行版](#三linux-发行版)
4. [安装与配置 Linux 环境](#四安装与配置-linux-环境)
5. [Vi / Vim 编辑器](#五vi--vim-编辑器)
6. [常用 Linux 命令](#六常用-linux-命令)
7. [补充内容：教程未讲到的核心知识](#七补充内容教程未讲到的核心知识)

***

## 一、Linux 简介

### 1.1 什么是 Linux

Linux 是一种**开源、免费**的类 Unix 操作系统。

- 内核由 **Linus Torvalds（林纳斯·托瓦兹）** 于 1991 年首次发布。

- 系统名称「Linux」正是取自作者的名字。

- Linus Torvalds 同时也是分布式版本控制系统 **Git** 的作者。

### 1.2 开源许可：GPL

Linux 是开源软件发展史上最著名的例子。只要遵循 **GNU 通用公共许可证（GPL，GNU General Public License）**，任何人和机构都可以：

- 自由使用 Linux 的全部底层源代码；

- 自由修改和再发布。

正因为如此，很多公司、组织都会基于 Linux 内核开发自己的操作系统，也就是我们常说的「Linux 发行版」。

### 1.3 补充：需要了解的名词

| 名词          | 说明                                      |
| ----------- | --------------------------------------- |
| GNU         | 一个自由软件计划，目标是创建一个类 Unix 的自由操作系统          |
| GPL         | GNU 通用公共许可证，是自由软件使用最广的许可证之一（copyleft 型） |
| 内核（Kernel）  | 操作系统最核心的部分，负责直接管理硬件                     |
| 发行版（Distro） | 内核 + 软件包 + 工具 + 库等组成的完整可用系统             |

> 补充说明：严格来说「Linux」只指内核，完整的操作系统应称为「GNU/Linux」。但日常习惯上都直接叫 Linux。

***

## 二、Linux 系统架构与组成

Linux 是一个**多层次结构**的系统，从里到外大致包含：

```
应用程序 (Applications)
        ▲
    Shell（命令行解释器）
        ▲
    系统库 (System Libraries)
        ▲
    内核 (Kernel)
        ▲
    硬件 (Hardware)
```

### 2.1 内核（Kernel）

- Linux 系统的**核心与基础**，负责管理硬件、提供最基本的系统服务。

- 包含的关键组件：

  - 设备驱动程序

  - 进程管理

  - 内存管理

  - 文件系统

  - 网络协议栈

  - 调度器等底层功能

### 2.2 系统库（System Libraries）

- 一组用于支持应用程序开发的软件库，提供常用的函数和接口。

- 最典型的例子：**C 语言标准库（libc）**。

### 2.3 Shell（壳 / 外壳）

- 一个**命令行解释器**。

- 相对内核而言，它就像「套在内核外面的一个壳」，是用户使用 Linux 系统的接口。

- 接受用户输入的命令，将其传递给操作系统执行。

- 补充：常见 Shell 有 `bash`、`zsh`、`sh`、`fish` 等，其中 `bash` 是绝大多数发行版的默认 Shell。

### 2.4 应用程序（Applications）

- 平时使用的各类软件：浏览器、编辑器、办公软件等。

- 在服务器上通常是专业服务软件，例如：

  - **Nginx** —— 提供网页访问服务的 Web 服务器

  - **MySQL** —— 提供数据库服务

***

## 三、Linux 发行版

### 3.1 什么是发行版

Linux 发行版 = **Linux 内核** + **软件包** + **系统工具** + **库文件**等组成的一个完整操作系统。它提供了一个预先配置好的 Linux 环境，让我们可以方便地安装、配置和使用。

每个发行版都有自己的**包管理器**、桌面环境和特色工具。

### 3.2 常见发行版（教程 + 补充）

| 发行版                        | 特点 / 适用场景                 |
| -------------------------- | ------------------------- |
| Ubuntu                     | 最流行、易上手，适合个人桌面用户（教程使用它讲解） |
| Kali                       | 用于网络安全和渗透测试，内置大量安全工具      |
| Alpine                     | 体积小巧，常用于容器化应用（Docker 镜像）  |
| Debian                     | Ubuntu 的上游，稳定，适合服务器       |
| CentOS / Rocky / AlmaLinux | 企业级服务器，兼容 Red Hat 生态      |
| Red Hat (RHEL)             | 商业企业发行版，提供付费支持            |
| Fedora                     | Red Hat 社区版，新技术先行         |
| Arch Linux                 | 滚动更新、极简、高度可定制，适合进阶用户      |
| openSUSE                   | 稳定易用，YaST 管理工具闻名          |

### 3.3 如何选择发行版

- 选择发行版取决于**用户需求**和**个人偏好**。

- 无论选哪个发行版，都基于 Linux 内核，**基础命令和操作基本一致**。

***

## 四、安装与配置 Linux 环境

### 4.1 方式总览

| 方式                       | 优点                       | 缺点           |
| ------------------------ | ------------------------ | ------------ |
| 虚拟机（VMware / VirtualBox） | 不影响现有系统，随时开关             | 有一定性能损耗      |
| WSL（Windows 子系统）         | 在 Windows 内直接使用 Linux，方便 | Windows 专属   |
| Multipass                | 命令行快速创建 Ubuntu，可脚本化      | 默认只支持 Ubuntu |
| Parallels Desktop（PD）    | Mac 上体验好，可跑 Windows      | 收费软件         |
| UTM                      | 可模拟多种 CPU 架构             | 小众，模拟架构有性能损耗 |
| Docker                   | 一条命令即用，轻量                | 本质是容器不是完整系统  |
| 双系统                      | 原生性能                     | 安装麻烦，切换需重启   |
| 云服务器                     | 系统预装、随时随地访问              | 需要付费         |

### 4.2 Windows 环境

- 推荐 **VMware** 或 **VirtualBox**：成熟、流行、界面友好。

- 新建虚拟机 → 加载 ISO 镜像 → 一路下一步即可。

- 也可使用 **WSL**，非常方便。

### 4.3 Mac 环境

- **Multipass**：轻量快速，支持命令行操作，适合脚本化多节点自动化（如自动化搭建 K8s 集群）。默认只支持 Ubuntu。

- **Parallels Desktop (PD)**：Mac 上使用较多的虚拟机，但收费；常用于在 Mac 上安装 Windows。

- **UTM**：基于 QEMU，可模拟 x86、ARM、MIPS 等多种 CPU 架构，适合跨架构需求（如 CTF 二进制漏洞题目多为 x86），但模拟架构有性能损耗。

### 4.4 Multipass 常用命令（补充整理）

| 命令                                                       | 作用                                |
| -------------------------------------------------------- | --------------------------------- |
| `multipass launch -n <name> -c <cpu> -m <mem> -d <disk>` | 创建并启动虚拟机（name=名字，c/m/d=CPU/内存/磁盘） |
| `multipass list`                                         | 查看虚拟机列表                           |
| `multipass shell <name>`                                 | 进入指定虚拟机                           |
| `multipass stop <name>`                                  | 停止虚拟机                             |
| `multipass start <name>`                                 | 启动虚拟机                             |
| `multipass delete <name>`                                | 删除虚拟机（可 `--purge` 彻底清除）           |
| `multipass info <name>`                                  | 查看虚拟机信息                           |

***

## 五、Vi / Vim 编辑器

### 5.1 为什么要学 Vi / Vim

- 服务器环境通常**没有图形界面**，也没有 VS Code 这类图形化编辑器，只能通过命令行操作。

- Vi 是此类场景下**最常用**的编辑器，用于查看日志、定位错误、修改配置文件。

- 掌握 Vi 能大幅提升运维/开发时排查问题的效率。

### 5.2 Vi 与 Vim 的关系

- **Vi**：Unix 系统下的文本编辑器。

- **Vim**：Vi Improved（Vi 增强版），在 Vi 基础上增加了新功能和改进。

- 二者使用方法一致；Linux 上通常安装的是 Vim，输入 `vi` 也会直接启动 Vim。

### 5.3 三种常用模式

| 模式         | 作用                  | 进入方式                     |
| ---------- | ------------------- | ------------------------ |
| 命令模式       | 默认模式，复制/粘贴/删除/移动/查找 | 启动 Vim 即进入；按 `Esc` 返回    |
| 插入模式       | 编辑文本内容              | 按 `i` / `a` / `o`（及大写变体） |
| 尾行模式（末行模式） | 保存、退出、替换、设置等        | 命令模式下输入 `:`              |

> 补充：还有其他模式，如**可视模式（v）**、**替换模式（R）**，但使用较少。

### 5.4 基本流程

```bash
vi hello.txt       # 打开或创建 hello.txt 文件
```

- 启动后默认处于**命令模式**。

- 输入 `:q`（quit）退出；输入 `:wq` 保存并退出；输入 `:q!` 不保存强制退出。

### 5.5 插入模式快捷键

| 命令  | 作用              |
| --- | --------------- |
| `i` | 在光标**前**插入      |
| `a` | 在光标**后**插入      |
| `o` | 在光标**下一行**插入新行  |
| `I` | 在当前行**行首**插入    |
| `A` | 在当前行**行尾**插入    |
| `O` | 在当前行**上一行**插入新行 |

### 5.6 光标移动

| 命令        | 作用      | 记忆技巧             |
| --------- | ------- | ---------------- |
| `h`       | 左移一个字符  | h 在最左            |
| `l`       | 右移一个字符  | l 在最右            |
| `j`       | 下移一行    | 扑克牌「勾 < K」，所以勾=下 |
| `k`       | 上移一行    | K=上              |
| `0` 或 `^` | 跳到行首    | <br />           |
| `$`       | 跳到行尾    | <br />           |
| `w` / `b` | 按单词前后移动 | 补充               |
| `方向键`     | 也可以控制光标 | 效果相同             |

> `^` 表示行首、`$` 表示行尾，这两个符号在正则表达式和查找替换中也很常用。

### 5.7 复制、粘贴、删除

| 命令   | 作用        | 类比 Windows |
| ---- | --------- | ---------- |
| `yy` | 复制当前行     | Ctrl + C   |
| `dd` | 删除（剪切）当前行 | Ctrl + X   |
| `p`  | 粘贴到光标下一行  | Ctrl + V   |
| `P`  | 粘贴到光标上一行  | 补充         |
| `x`  | 删除光标处单个字符 | 补充         |

- **加数字**表示次数：`2yy` 复制两行，`3p` 粘贴 3 次，`100p` 粘贴 100 次。

### 5.8 翻页与跳转

| 命令         | 作用              |
| ---------- | --------------- |
| `Ctrl + f` | 向前翻一页（forward）  |
| `Ctrl + b` | 向后翻一页（backward） |
| `Ctrl + d` | 向下翻半页（down）     |
| `Ctrl + u` | 向上翻半页（up）       |
| `G`        | 跳到文件最后一行        |
| `gg`       | 跳到文件第一行         |
| `100G`     | 跳到第 100 行       |
| `:50`      | 跳到第 50 行        |

### 5.9 查找与替换

**查找：**

| 命令         | 作用                        |
| ---------- | ------------------------- |
| `/hello`   | 从光标处**向下**查找 hello        |
| `?hello`   | 从光标处**向上**查找 hello        |
| `n`        | 按当前查找方向找下一个（next）         |
| `N`        | 按相反方向找下一个                 |
| `/hello\c` | 忽略大小写查找（\c = ignore case） |

**替换：**

```
:[起始行,结束行]s/查找内容/替换内容/[g]
```

| 命令                    | 作用                       |
| --------------------- | ------------------------ |
| `:s/hello/world`      | 替换当前行第 1 个 hello 为 world |
| `:s/hello/world/g`    | 替换当前行所有 hello 为 world    |
| `:1,5s/hello/world/g` | 替换 1\~5 行所有 hello        |
| `:1,$s/hello/world/g` | 替换整个文件（`$` 表示最后一行）       |
| `:%s/hello/world/g`   | 替换整个文件（百分号写法，补充）         |

### 5.10 撤销与其他设置

| 操作                            | 作用                    |
| ----------------------------- | --------------------- |
| `u`                           | 撤销（undo），相当于 Ctrl + Z |
| `Ctrl + r`                    | 重做（redo，补充）           |
| `:set number` / `:set nu`     | 显示行号                  |
| `:set nonumber` / `:set nonu` | 关闭行号                  |
| `:set ignorecase`             | 全局忽略大小写查找（补充）         |

### 5.11 .vimrc 配置文件

- 文件 `.vimrc` 用于保存 Vim 的配置信息：快捷键、颜色、插件、行号、忽略大小写等。

- 每次打开 Vim 会自动加载，无需重复设置。

- 教程提到配置行号、忽略大小写都可写入此文件。

- 补充：可用 `:syntax on`、`set autoindent`、`set tabstop=4` 等常用配置。

***

## 六、常用 Linux 命令

> 说明：Linux 命令一般由「命令名 + 参数」组成，几乎所有命令都可接受一个或多个参数来指定具体操作。

### 6.1 文件与目录查看

| 命令     | 作用             | 常用参数                                                             |
| ------ | -------------- | ---------------------------------------------------------------- |
| `ls`   | 列出目录内容         | `-l` 详细信息、`-a` 含隐藏文件、`-h` 人性化大小、`-t` 按时间排序、`-r` 逆序、`-i` 显示 inode |
| `pwd`  | 显示当前目录（绝对路径）   | <br />                                                           |
| `cat`  | 查看文件内容         | `-n` 显示行号                                                        |
| `du`   | 查看文件/目录大小      | `-h` 人性化显示                                                       |
| `tree` | 树状图显示目录结构（需安装） | <br />                                                           |

- 参数可组合，如 `ls -ltr` = 按修改时间逆序显示详细信息。

- Linux 中以 `.` 开头的文件/目录是**隐藏**的，如 `.vimrc`、`.bashrc`、`.ssh`。

> 补充：`ls -l` 输出各字段含义：
> `-rw-r--r-- 1 user group 1234 Jan 1 00:00 file.txt`
> 依次为：权限 - 硬链接数 - 所有者 - 所属组 - 大小 - 修改时间 - 文件名。

### 6.2 切换目录与路径

| 命令 / 符号 | 作用               |
| ------- | ---------------- |
| `cd /`  | 切换到根目录           |
| `cd ..` | 切换到上一级目录         |
| `cd -`  | 切换到上一次所在的目录      |
| `cd ~`  | 切换到当前用户的 home 目录 |
| `.`     | 表示当前目录           |
| `..`    | 表示上一级目录          |
| `~`     | 表示当前用户 home 目录   |
| `/`     | 根目录符号            |

**绝对路径 vs 相对路径：**

- 绝对路径：从根目录 `/` 开始的完整路径，如 `/root`。

- 相对路径：相对于当前目录的路径，使用 `.` 和 `..`。

### 6.3 创建文件与目录

| 命令                        | 作用               | 说明               |
| ------------------------- | ---------------- | ---------------- |
| `touch file.txt`          | 创建空文件 / 更新文件修改时间 | 文件存在则更新时间        |
| `echo "hello" > file.txt` | 输出文本并重定向写入文件     | `>` 为覆盖，`>>` 为追加 |
| `mkdir dir`               | 创建目录             | `-p` 创建多级目录      |
| `vi file.txt`             | 用 Vim 创建/编辑文件    | <br />           |

### 6.4 复制、移动、删除

| 命令                | 作用         | 说明         |
| ----------------- | ---------- | ---------- |
| `cp src dst`      | 复制文件       | 复制目录需 `-r` |
| `cp -r dir1 dir2` | 递归复制目录     | <br />     |
| `mv src dst`      | 移动 / 重命名文件 | <br />     |
| `rm file`         | 删除文件       | 不可逆、无回收站   |
| `rm -r dir`       | 递归删除目录     | 危险操作       |
| `rmdir dir`       | 删除空目录      | 目录非空会报错    |

> ⚠️ `rm` 命令非常危险：Linux 删除**不可逆**，没有回收站，使用 `-r` 时尤其要小心。`rm -rf /` 是灾难级命令，切勿执行。

### 6.5 链接文件：ln

链接文件分为**软链接**和**硬链接**两种。

#### 软链接（符号链接，`ln -s`）

```bash
ln -s 目标文件 链接文件
```

- 类似 Windows 的「快捷方式」。

- 本身不存储内容，只指向目标，因此**体积很小**。

- 可以指向**文件或目录**。

- 目标被删除后，软链接**失效**（`ls` 显示为红色）。

#### 硬链接（`ln`，不加 -s）

```bash
ln 源文件 链接文件
```

- 本质是指向文件系统中同一文件的**指针**，与源文件**共享相同的 inode**。

- 修改其中一个，另一个内容同步变化；大小、修改时间一致。

- 删除其中一个，另一个仍可正常访问。

- **只能指向文件，不能指向目录**。

#### 关于 inode

- inode（索引节点）是文件系统中非常重要的概念。

- Linux 中每个文件/目录都有唯一的 inode，存储文件的**元数据**（权限、所有者、大小、修改时间等）。

- 系统通过 inode 识别文件；硬链接共享 inode，因此是「同一文件的不同名字」。

- 用 `ls -i` 可查看文件的 inode 号。`ls -l` 第二列数字即硬链接数。

### 6.6 文件权限：chmod

#### 权限表示

`ls -l` 最左边 10 个字符，如 `drwxr-xr-x`：

- 第 1 个字符：文件类型（`-` 普通文件、`d` 目录、`l` 链接文件）

- 后 9 个字符分 3 组，每组 3 个（`rwx`）：

  - 第 1 组：文件**所有者**权限

  - 第 2 组：文件**所属组**权限

  - 第 3 组：**其他用户**权限

每组含义：

| 字符  | 权限          |
| --- | ----------- |
| `r` | 读（read）     |
| `w` | 写（write）    |
| `x` | 执行（execute） |
| `-` | 无此权限        |

#### 用符号修改

| 命令                | 作用            |
| ----------------- | ------------- |
| `chmod +x file`   | 给所有用户添加执行权限   |
| `chmod -x file`   | 删除执行权限        |
| `chmod u+x file`  | 仅所有者添加执行权限    |
| `chmod ug+x file` | 所有者和所属组添加执行权限 |
| `chmod o-w file`  | 其他用户删除写权限     |

- `u`=所有者(user)、`g`=所属组(group)、`o`=其他用户(other)、`a`=所有人(all)，`+`=添加、`-`=删除。

#### 用数字修改

| 权限  | 数值 |
| --- | -- |
| `r` | 4  |
| `w` | 2  |
| `x` | 1  |

将每组三个权限数值相加，如：

| 数值          | 权限含义 |
| ----------- | ---- |
| `7` = 4+2+1 | 读写执行 |
| `6` = 4+2   | 读写   |
| `5` = 4+1   | 读执行  |
| `4`         | 只读   |
| `0`         | 无权限  |

常用示例：

- `chmod 644 file` → 所有者读写，组和其他人只读

- `chmod 755 file` → 所有者读写执行，组和其他人读执行

- `chmod 777 file` → 所有人读写执行

***

## 七、补充内容：教程未讲到的核心知识

> 以下内容是对教程的补充，帮你完整掌握 Linux 基础，日常开发运维都很常用。

### 7.1 根目录结构（FHS 补充）

| 目录                | 作用                                   |
| ----------------- | ------------------------------------ |
| `/bin`            | 系统基本命令和二进制可执行文件（binary），如 `ls`、`cat` |
| `/sbin`           | 系统管理员使用的命令                           |
| `/etc`            | 系统和软件的配置文件（如 Nginx、MySQL 配置）         |
| `/home`           | 普通用户的 home 目录                        |
| `/root`           | root 用户的 home 目录                     |
| `/usr`            | 用户安装的软件、库和文档                         |
| `/var`            | 经常变化的数据：日志、缓存                        |
| `/tmp`            | 临时文件                                 |
| `/dev`            | 设备文件                                 |
| `/proc`           | 虚拟文件系统，反映系统运行状态                      |
| `/boot`           | 启动相关文件                               |
| `/lib`            | 系统库文件                                |
| `/opt`            | 可选软件包（第三方应用）                         |
| `/mnt` / `/media` | 挂载点                                  |

### 7.2 文件内容查看与处理（补充）

| 命令                        | 作用              |
| ------------------------- | --------------- |
| `less file` / `more file` | 分页查看文件          |
| `head -n 10 file`         | 查看前 10 行        |
| `tail -n 10 file`         | 查看后 10 行        |
| `tail -f file`            | 实时追踪文件变化（看日志神器） |
| `grep "关键字" file`         | 在文件中搜索关键字       |
| `grep -r "关键字" dir`       | 递归搜索目录          |
| `wc -l file`              | 统计行数/字数/字节数     |
| `sort file`               | 排序              |
| `uniq`                    | 去重              |
| `diff file1 file2`        | 对比文件差异          |

### 7.3 重定向、管道与通配符（补充）

**重定向：**

| 符号   | 作用           |
| ---- | ------------ |
| `>`  | 输出重定向到文件（覆盖） |
| `>>` | 输出重定向到文件（追加） |
| `<`  | 从文件读取输入      |
| `2>` | 错误输出重定向      |

**管道** **`|`：** 把前一个命令的输出作为后一个命令的输入。

```bash
ls -l | grep "txt"     # 列出含 txt 的行
ps aux | grep nginx    # 查找 nginx 进程
```

**通配符：**

| 符号      | 作用        |
| ------- | --------- |
| `*`     | 匹配任意多个字符  |
| `?`     | 匹配任意单个字符  |
| `[abc]` | 匹配括号内任一字符 |

### 7.4 用户与权限管理（补充）

| 命令              | 作用             |
| --------------- | -------------- |
| `whoami`        | 查看当前用户         |
| `id`            | 查看用户 UID、组 GID |
| `useradd 用户名`   | 新建用户           |
| `passwd 用户名`    | 设置/修改密码        |
| `su 用户名`        | 切换用户           |
| `sudo 命令`       | 以管理员权限执行命令     |
| `chown 用户:组 文件` | 修改文件所有者/所属组    |
| `chgrp 组 文件`    | 修改所属组          |
| `groups`        | 查看用户所属组        |

> `root` 是超级管理员，UID 为 0，拥有最高权限。

### 7.5 进程管理（补充）

| 命令                   | 作用                |
| -------------------- | ----------------- |
| `ps`                 | 查看进程快照            |
| `ps aux`             | 查看所有进程详细信息        |
| `top`                | 实时查看进程和资源占用（q 退出） |
| `htop`               | top 的增强版（需安装）     |
| `kill PID`           | 结束指定进程            |
| `kill -9 PID`        | 强制结束进程            |
| `pkill 名称`           | 按名字结束进程           |
| `jobs` / `bg` / `fg` | 查看/后台/前台任务控制      |

前台命令加 `&` 可放到后台运行；`Ctrl + C` 终止命令、`Ctrl + Z` 挂起命令。

### 7.6 网络相关（补充）

| 命令                           | 作用               |
| ---------------------------- | ---------------- |
| `ping -c 4 域名/IP`            | 测试网络连通性（-c 指定次数） |
| `ip addr` / `ifconfig`       | 查看 IP 地址         |
| `curl 网址`                    | 请求 URL、下载内容      |
| `wget 网址`                    | 下载文件             |
| `ssh user@host`              | 远程登录服务器          |
| `scp 文件 user@host:路径`        | 远程拷贝文件           |
| `netstat -tlnp` / `ss -tlnp` | 查看监听端口           |
| `nslookup 域名` / `dig`        | DNS 解析           |

### 7.7 软件包管理（补充）

不同发行版包管理器不同：

| 发行版体系            | 包管理器      | 示例                                                         |
| ---------------- | --------- | ---------------------------------------------------------- |
| Debian / Ubuntu  | apt       | `sudo apt update`、`sudo apt install 包`、`sudo apt remove 包` |
| Red Hat / CentOS | yum / dnf | `sudo yum install 包`                                       |
| Alpine           | apk       | `apk add 包`                                                |
| Arch             | pacman    | `pacman -S 包`                                              |

常用操作：更新索引 → 安装 → 卸载 → 升级。

### 7.8 压缩与解压（补充）

| 命令                                  | 作用        |
| ----------------------------------- | --------- |
| `tar -czvf 文件.tar.gz 目录`            | 打包并压缩     |
| `tar -xzvf 文件.tar.gz`               | 解压        |
| `tar -xvf 文件.tar`                   | 解包（不压缩）   |
| `gzip 文件` / `gunzip 文件.gz`          | 压缩/解压 .gz |
| `zip -r 文件.zip 目录` / `unzip 文件.zip` | zip 压缩/解压 |

### 7.9 系统信息与环境变量（补充）

| 命令                 | 作用         |
| ------------------ | ---------- |
| `uname -a`         | 查看系统内核信息   |
| `df -h`            | 查看磁盘使用情况   |
| `free -h`          | 查看内存使用情况   |
| `uptime`           | 查看运行时间     |
| `date`             | 查看/设置时间    |
| `history`          | 查看命令历史     |
| `env` / `printenv` | 查看环境变量     |
| `echo $PATH`       | 查看 PATH 变量 |
| `export VAR=value` | 设置环境变量     |

### 7.10 帮助命令（补充）

| 命令          | 作用           |
| ----------- | ------------ |
| `man 命令`    | 查看命令手册（q 退出） |
| `命令 --help` | 查看命令简要帮助     |
| `which 命令`  | 查看命令所在路径     |
| `type 命令`   | 判断命令类型       |

### 7.11 实用小技巧（补充）

- **命令历史**：方向键 ↑ / ↓ 翻找历史命令，再回车执行。

- **自动补全**：输入命令/路径开头后按 `Tab` 自动补全。

- **清屏**：`clear` 或 `Ctrl + L`。

- **多命令执行**：`&&`（前成功才执行后）与 `;`（顺序执行，不看结果）。

- **历史搜索**：`Ctrl + R` 反向搜索历史命令。

***

## 八、总结

1. **本质**：Linux 是开源免费的类 Unix 系统，内核由 Linus Torvalds 于 1991 年发布，遵循 GPL 许可。
2. **架构**：内核 → 系统库 → Shell → 应用程序的多层结构。
3. **发行版**：内核 + 软件包构成的完整系统，Ubuntu / Kali / Alpine 等各有定位，但底层命令一致。
4. **环境**：可通过虚拟机、WSL、Multipass、Docker、云服务器等方式搭建。
5. **Vim**：命令模式 / 插入模式 / 尾行模式三模式，掌握移动、增删、查找替换、撤销即可高效编辑。
6. **命令**：`ls / cat / ln / chmod / echo / touch / pwd / cd / cp / mv / rm / mkdir` 等是日常高频命令。
7. **延伸**：目录结构、文本处理、重定向管道、进程、网络、包管理、压缩等能力共同构成完整技能树。

> 学习建议：命令不能只靠背，多动手敲、多查 `man` / `--help`，结合真实场景（看日志、改配置、装软件）练习，进步最快。

